---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
---

# 多自由度系统的强迫振动

## 阻尼的表达与处理

至今仅限于讨论多自由度无阻尼系统的振动分析。如果系统的阻尼很小，而激励频率又远离共振频率，阻尼效应影响很小，可以略去不计。但当激励频率接近固有频率时，阻尼的影响显著，便不能忽略其影响。

由于阻尼机理很复杂，通常采用黏性阻尼假设，即假设系统的阻尼力大小与速度的一次方成正比，于是，系统的运动微分方程式为

$$
\boldsymbol{M} \ddot{\boldsymbol{q}} + \boldsymbol{C} \dot{\boldsymbol{q}} + \boldsymbol{K} \boldsymbol{q} = \boldsymbol{F}(t)
$$ (eq:2-94)

式中，$\boldsymbol{C}$——阻尼矩阵，与 $\boldsymbol{M}$、$\boldsymbol{K}$ 同样为对称矩阵，其元素 $C_{ij}\ (i,j=1,2,\dots,n)$ 成为阻尼影响系数。

在结构动力分析时，经常采用的是以下的瑞利阻尼假设，即阻尼矩阵 $\boldsymbol{C}$ 为如下形式：

$$
\boldsymbol{C} = \alpha \boldsymbol{M} + \beta \boldsymbol{K}
$$ (eq:2-95)

式中，$\alpha$ 与 $\beta$——比例常数。上式表示阻尼矩阵正比于质量矩阵（$\beta=0$）或正比于刚度矩阵（$\alpha=0$），或者正比于它们的线性组合，称这种阻尼为比例阻尼。

如果用无阻尼系统的固有振型，便可使 $\boldsymbol{C}$ 对角化。设 $\boldsymbol{\varPhi}$ 为正则化振型，则

$$
\boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{C} \boldsymbol{\varPhi} = \alpha \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{M} \boldsymbol{\varPhi} + \beta \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{K} \boldsymbol{\varPhi} = \alpha \boldsymbol{I} + \beta \boldsymbol{\varLambda}
$$ (eq:2-96)

式中，$\boldsymbol{I}$——单位矩阵；$\boldsymbol{\varLambda}$——对角矩阵，

$$
\boldsymbol{\varLambda} = \mathrm{diag}\left( \omega_{\mathrm{n}r}^2 \right)
$$ (eq:2-97)

令

$$
\alpha + \beta \omega_{\mathrm{n}r}^2 = 2\omega_{\mathrm{n}r} \zeta_r
$$ (eq:2-98)

或写成

$$
\zeta_r = \frac{\alpha + \beta \omega_{\mathrm{n}r}^2}{2\omega_{\mathrm{n}r}} = \frac{\alpha}{2\omega_{\mathrm{n}r}} + \frac{\beta \omega_{\mathrm{n}r}}{2}
$$ (eq:2-99)

称 $\zeta_r$ 为振型（或模态）比例阻尼比。由式 {eq}`eq:2-99` 可见，当阻尼矩阵正比于质量矩阵（$\beta=0$）时，阻尼比 $\zeta_r$ 与频率 $\omega_{\mathrm{n}r}$ 成反比，因此在系统的总响应中，低阶振型起的作用较小。而反之，若阻尼矩阵正比于刚度矩阵，即 $\alpha=0$，阻尼比 $\zeta_r$ 正比于频率 $\omega_{\mathrm{n}r}$，则高阶振型起的作用较小。因此，适当地选取 $\alpha$ 与 $\beta$，可以近似地反映实际振动系统中阻尼的影响。

由于阻尼的机理很复杂，至今并不完全清楚，故精确地确定阻尼矩阵是困难的，通常采用实验测定方法近似地确定阻尼比 $\zeta_r$。由于系统并非总是有比例阻尼，因此，非对角元素不总是全为零，但当阻尼比很小时，可以令非对角元素为零，建立对角阻尼矩阵。

这样，对应正则坐标的阻尼矩阵为对角阵，

$$
\boldsymbol{C} =
\begin{bmatrix}
2\omega_{\mathrm{n}1}\zeta_1 & & & & \\
& 2\omega_{\mathrm{n}2}\zeta_2 & & & \\
& & \ddots & & \\
& & & 2\omega_{\mathrm{n}r}\zeta_r & \\
& & & & \ddots \\
& & & & & 2\omega_{\mathrm{n}n}\zeta_n
\end{bmatrix}
$$ (eq:2-100)

对于这样的阻尼矩阵，可以对方程式解耦，给求解多自由度系统的响应带来很大的便利。

## 系统对任意激励的响应

线性多自由度系统有阻尼强迫振动的运动方程式{eq}`eq:2-94`为

$$
\boldsymbol{M} \ddot{\boldsymbol{q}} + \boldsymbol{C} \dot{\boldsymbol{q}} + \boldsymbol{K} \boldsymbol{q} = \boldsymbol{F}(t)
$$ (eq:2-101)

一般情况下，$\boldsymbol{M}$、$\boldsymbol{C}$ 和 $\boldsymbol{K}$ 这三个矩阵中有非零的耦合项，如 $K_{ij}=K_{ji}\neq0$。因而需要解有 $n$ 个未知数的常微分方程组。利用广义坐标 $\boldsymbol{q}$ 和主坐标 $\boldsymbol{p}$ 之间的变换关系<!--式{eq}`eq:2-51`-->，可以将广义坐标的响应用主振型和主坐标乘积的级数和来描述，这就是展开定理，而用此求响应的方法称为振型叠加法。主振型取无阻尼时的固有振型，主坐标是待求的，从而可将用广义坐标描述的振动方程转换为用主坐标表示的振动方程表示，求出主坐标的响应式，然后再反回求出广义坐标的响应。这是一种求多自由度系统响应的有效方法。

为此，先要求解自由振动中的特征值问题：

$$
\left( \boldsymbol{K} - \omega_{\mathrm{n}r}^2 \boldsymbol{M} \right) \boldsymbol{A}_r = \boldsymbol{0}
$$ (eq:2-102)

由此确定固有振型 $\boldsymbol{A}_r$ 和固有频率 $\omega_{\mathrm{n}r}(r=1,2,\dots,n)$。然后，作坐标变换

$$
\boldsymbol{q} = \boldsymbol{\varPhi} \boldsymbol{p}
$$ (eq:2-103)

为取更普遍化的形式，上式中 $\boldsymbol{\varPhi}$ 取未正则化的振型，$\boldsymbol{p}$ 为系统的主坐标矢量。再将此变换式代入运动方程式 {eq}`eq:2-101` 并前乘 $\boldsymbol{\varPhi}^\mathrm{T}$，可得

$$
\widetilde{\boldsymbol{M}} \ddot{\boldsymbol{p}} + \widetilde{\boldsymbol{C}} \dot{\boldsymbol{p}} + \widetilde{\boldsymbol{K}} \boldsymbol{p} = \widetilde{\boldsymbol{F}}(t)
$$ (eq:2-104)

式中，

$$
\left.
\begin{aligned}
&\widetilde{\boldsymbol{M}} = \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{M} \boldsymbol{\varPhi} \text{——模态质量矩阵} \\
&\widetilde{\boldsymbol{C}} = \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{C} \boldsymbol{\varPhi} \text{——模态阻尼矩阵} \\
&\widetilde{\boldsymbol{K}} = \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{K} \boldsymbol{\varPhi} \text{——模态刚度矩阵} \\
&\widetilde{\boldsymbol{F}}(t) = \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{F}(t) \text{——模态力矢量}
\end{aligned}
\right\}
$$ (eq:2-105)

由正交条件知，$\boldsymbol{M}$ 和 $\boldsymbol{K}$ 是对角矩阵，若 $\widetilde{\boldsymbol{C}}$ 是黏性阻尼形式的对角阵，则式 {eq}`eq:2-104` 解耦为主坐标描述的振动方程：

$$
\ddot{p}_r + 2\zeta_r \omega_{\mathrm{n}r} \dot{p}_r + \omega_{\mathrm{n}r}^2 p_r = \frac{\widetilde{F}_r(t)}{M_r} \quad (r=1,2,\dots,n)
$$ (eq:2-106)

式中，

$$
M_r = \boldsymbol{\varphi}^{(r)\mathrm{T}} \boldsymbol{M} \boldsymbol{\varphi}^{(r)}, \quad \widetilde{F}_r(t) = \boldsymbol{\varphi}^{(r)\mathrm{T}} \boldsymbol{F}(t)
$$ (eq:2-107)

问题归结为求解上述单自由度系统的振动，即主振动。总响应为初始条件引起的自由振动响应与激励力响应之和：

$$
\begin{aligned}
p_r(t) &= \mathrm{e}^{-\zeta_r \omega_{\mathrm{n}r} t} \left[ p_r(0)\cos\omega_{\mathrm{d}r} t + \frac{\dot{p}_r(0) + \zeta_r \omega_{\mathrm{n}r} p_r(0)}{\omega_{\mathrm{d}r}} \sin\omega_{\mathrm{d}r} t \right] + \\
&\quad \frac{1}{\omega_{\mathrm{d}r}} \int_{0}^{t} \frac{\widetilde{F}_r(\tau)}{M_r} \mathrm{e}^{-\zeta_r \omega_{\mathrm{n}r} (t-\tau)} \sin\omega_{\mathrm{d}r} (t-\tau) \mathrm{d}\tau \quad (r=1,2,\dots,n)
\end{aligned}
$$ (eq:2-108)

若广义坐标的初始条件为 $t=0$ 时，$\boldsymbol{q}=\boldsymbol{q}(0)$，$\dot{\boldsymbol{q}}=\dot{\boldsymbol{q}}(0)$，它们与主坐标的初始条件 $\boldsymbol{p}(0)$ 和 $\dot{\boldsymbol{p}}(0)$ 之间的关系不难由式 {eq}`eq:2-103` 写出：

$$
\boldsymbol{q}(0) = \boldsymbol{\varPhi} \boldsymbol{p}(0), \quad \dot{\boldsymbol{q}}(0) = \boldsymbol{\varPhi} \dot{\boldsymbol{p}}(0)
$$ (eq:2-109)

然后前乘式 {eq}`eq:2-109` 以 $\boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{M}$，得

$$
\boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{M} \boldsymbol{q}(0) = \widetilde{\boldsymbol{M}} \boldsymbol{p}(0), \quad \boldsymbol{\varPhi}^\mathrm{T} \boldsymbol{M} \dot{\boldsymbol{q}}(0) = \widetilde{\boldsymbol{M}} \dot{\boldsymbol{p}}(0)
$$ (eq:2-110)

因为 $\widetilde{\boldsymbol{M}}$ 是对角阵，故由上式可得主坐标的初始条件为

$$
p_r(0) = \left( \frac{1}{M_r} \right) \boldsymbol{\varphi}^{(r)\mathrm{T}} \boldsymbol{M} \boldsymbol{q}(0), \quad \dot{p}_r(0) = \left( \frac{1}{M_r} \right) \boldsymbol{\varphi}^{(r)\mathrm{T}} \boldsymbol{M} \dot{\boldsymbol{q}}(0)
$$ (eq:2-111)

<!--式{eq}`eq:2-61`和{eq}`eq:2-62`与此处的区别是，由于前者采用正则振型，经过了归一化处理，故广义质量 $M_r$ 值为 1。-->
求得主坐标响应式 {eq}`eq:2-108` 后，再代入式 {eq}`eq:2-103` 便得广义坐标表示的响应表达式。

## 交互探索：解耦与模态叠加

下方交互探索以对称两自由度系统为对象，展示物理坐标 $\boldsymbol{q}$ 与主坐标 $\boldsymbol{p}$ 之间的关系。切换激励类型和受力位置后，可以同时观察物理力、模态力、主坐标响应以及总响应，从而看到“耦合系统如何在模态空间中解耦为两个单自由度问题”。

```{code-cell} ipython3
:tags: [remove-input]

from IPython.display import HTML, display

html = r"""
<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>
<style>
  #modal-superposition-demo input[type="range"] {
    width: 100%;
    accent-color: #0f766e;
  }
  #modal-superposition-demo button,
  #modal-superposition-demo select {
    border-radius: 12px;
    border: 1px solid rgba(15,118,110,0.18);
    background: #ffffff;
    color: #0f172a;
    font-weight: 700;
  }
  #modal-superposition-demo button {
    padding: 0.58rem 0.82rem;
    cursor: pointer;
  }
  #modal-superposition-demo button.primary {
    border: none;
    background: #0f766e;
    color: #ffffff;
  }
  #modal-superposition-demo table {
    width: 100%;
    border-collapse: collapse;
    font-size: 12px;
  }
  #modal-superposition-demo td {
    border: 1px solid rgba(148,163,184,0.18);
    padding: 0.32rem 0.45rem;
    text-align: center;
  }
</style>
<div id="modal-superposition-demo" style="border:1px solid rgba(15,118,110,0.14); border-radius:20px; padding:1rem; background:linear-gradient(180deg,#ecfeff 0%,#ffffff 100%);">
  <div style="display:grid; grid-template-columns:repeat(5,minmax(0,1fr)); gap:0.75rem; margin-bottom:0.95rem;">
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">一阶固有频率</div>
      <div id="ms-wn1" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f766e;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">二阶固有频率</div>
      <div id="ms-wn2" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f766e;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">当前激励</div>
      <div id="ms-force-state" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">模态力峰值</div>
      <div id="ms-modal-force" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">当前状态</div>
      <div id="ms-state" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">播放中</div>
    </div>
  </div>

  <div style="display:grid; grid-template-columns:minmax(0,1fr); gap:1rem; align-items:start;">
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:14px; font-weight:800; color:#0f172a; margin-bottom:0.65rem;">系统与激励参数</div>
      <div id="ms-controls" style="display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:0.72rem 0.9rem;"></div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:0.7rem; margin-top:0.8rem;">
        <label style="font-size:13px; font-weight:800; color:#334155;">激励类型
          <select id="ms-force-type" style="width:100%; margin-top:0.35rem; padding:0.5rem 0.6rem;">
            <option value="pulse">矩形脉冲</option>
            <option value="step">阶跃载荷</option>
            <option value="harmonic">简谐激励</option>
          </select>
        </label>
        <label style="font-size:13px; font-weight:800; color:#334155;">作用位置
          <select id="ms-force-dof" style="width:100%; margin-top:0.35rem; padding:0.5rem 0.6rem;">
            <option value="0">x₁</option>
            <option value="1">x₂</option>
          </select>
        </label>
      </div>
      <div style="display:flex; gap:0.5rem; flex-wrap:wrap; margin-top:0.9rem;">
        <button class="primary" id="ms-play">暂停</button>
        <button id="ms-reset">重播</button>
        <button id="ms-preset-pulse">脉冲激励</button>
        <button id="ms-preset-harmonic">近共振</button>
      </div>
    </div>

    <div style="padding:0.9rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <canvas id="ms-canvas" width="820" height="250" style="width:100%; aspect-ratio:820 / 250; height:auto; display:block;"></canvas>
    </div>
    <div style="display:grid; grid-template-columns:1fr 1fr; gap:1rem;">
      <div style="padding:0.9rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
        <div style="font-size:13px; font-weight:800; color:#0f172a; margin-bottom:0.45rem;">物理空间矩阵</div>
        <div id="ms-matrix-physical"></div>
      </div>
      <div style="padding:0.9rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
        <div style="font-size:13px; font-weight:800; color:#0f172a; margin-bottom:0.45rem;">模态空间矩阵</div>
        <div id="ms-matrix-modal"></div>
      </div>
    </div>
  </div>

  <div style="display:grid; grid-template-columns:minmax(0,1fr); gap:1rem; margin-top:1rem;">
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div id="ms-force-plot" style="width:100%; height:250px;"></div>
    </div>
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div id="ms-modal-plot" style="width:100%; height:300px;"></div>
    </div>
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div id="ms-response-plot" style="width:100%; height:320px;"></div>
    </div>
  </div>
</div>

<script>
(() => {
  const container = document.querySelector("#modal-superposition-demo");
  if (!container || container.dataset.ready) return;
  container.dataset.ready = "true";

  const controls = [
    ["m", "质量 m", 0.5, 5, 0.1, 1.0],
    ["k", "弹簧刚度 k", 0.5, 8, 0.1, 1.0],
    ["zeta", "模态阻尼比 ζ", 0.00, 0.20, 0.01, 0.05],
    ["amp", "载荷幅值 F₀", 0.2, 3.0, 0.05, 1.0],
    ["duration", "脉冲持续时间", 0.2, 6.0, 0.1, 1.2],
    ["ratio", "简谐频率比 ω/ω₁", 0.4, 2.5, 0.02, 0.9],
  ];
  const state = {};
  let playing = true;
  let t = 0;
  let lastTime = null;
  let cached = null;

  function addSlider(item) {
    const [key, label, min, max, step, value] = item;
    state[key] = value;
    const row = document.createElement("div");
    row.style.minWidth = "0";
    row.innerHTML = `
      <label style="display:flex; justify-content:space-between; gap:0.8rem; font-size:13px; color:#334155; font-weight:800;">
        <span>${label}</span><span id="ms-${key}-value">${value.toFixed(2)}</span>
      </label>
      <input id="ms-${key}" type="range" min="${min}" max="${max}" step="${step}" value="${value}">
    `;
    container.querySelector("#ms-controls").appendChild(row);
    row.querySelector("input").addEventListener("input", (event) => {
      state[key] = Number(event.target.value);
      row.querySelector(`#ms-${key}-value`).textContent = state[key].toFixed(2);
      t = 0;
      updateAll();
    });
  }
  controls.forEach(addSlider);
  state.forceType = "pulse";
  state.forceDof = 1;
  container.querySelector("#ms-force-type").value = state.forceType;
  container.querySelector("#ms-force-dof").value = String(state.forceDof);

  const phi = [
    [1 / Math.sqrt(2), 1 / Math.sqrt(2)],
    [1 / Math.sqrt(2), -1 / Math.sqrt(2)],
  ];

  function setValue(key, value) {
    state[key] = value;
    const input = container.querySelector(`#ms-${key}`);
    const label = container.querySelector(`#ms-${key}-value`);
    if (input) input.value = String(value);
    if (label) label.textContent = Number(value).toFixed(2);
  }

  function matrices() {
    const m = state.m;
    const k = state.k;
    const zeta = state.zeta;
    const wn = [Math.sqrt(k / m), Math.sqrt(3 * k / m)];
    const wd = wn.map((w) => w * Math.sqrt(Math.max(0, 1 - zeta * zeta)));
    const M = [[m, 0], [0, m]];
    const C = [[2 * zeta * m * wn[0], 0], [0, 2 * zeta * m * wn[1]]];
    const K = [[2 * k, -k], [-k, 2 * k]];
    return {wn, wd, M, C, K};
  }

  function externalForce(time, wn1) {
    const amp = state.amp;
    if (state.forceType === "pulse") return time <= state.duration ? amp : 0;
    if (state.forceType === "step") return amp;
    return amp * Math.sin(state.ratio * wn1 * time);
  }

  function computeResponse() {
    const {wn, wd, M, K} = matrices();
    const dt = 0.02;
    const tMax = 22;
    const steps = Math.round(tMax / dt) + 1;
    const time = [];
    const f1 = [];
    const f2 = [];
    const fr1 = [];
    const fr2 = [];
    const p1 = [];
    const p2 = [];
    const q1 = [];
    const q2 = [];
    let modalX = [0, 0];
    let modalV = [0, 0];
    for (let i = 0; i < steps; i++) {
      const tt = i * dt;
      const force = externalForce(tt, wn[0]);
      const F = state.forceDof === 0 ? [force, 0] : [0, force];
      const Ft = [
        phi[0][0] * F[0] + phi[0][1] * F[1],
        phi[1][0] * F[0] + phi[1][1] * F[1],
      ];
      for (let r = 0; r < 2; r++) {
        const a = Ft[r] - 2 * state.zeta * wn[r] * modalV[r] - wn[r] * wn[r] * modalX[r];
        modalV[r] += a * dt;
        modalX[r] += modalV[r] * dt;
      }
      const q = [
        phi[0][0] * modalX[0] + phi[1][0] * modalX[1],
        phi[0][1] * modalX[0] + phi[1][1] * modalX[1],
      ];
      time.push(tt);
      f1.push(F[0]);
      f2.push(F[1]);
      fr1.push(Ft[0]);
      fr2.push(Ft[1]);
      p1.push(modalX[0]);
      p2.push(modalX[1]);
      q1.push(q[0]);
      q2.push(q[1]);
    }
    return {wn, wd, M, K, time, f1, f2, fr1, fr2, p1, p2, q1, q2, tMax};
  }

  function renderMatrix(el, label, matrix) {
    el.innerHTML = `<div style="font-size:12px; color:#64748b; font-weight:700; margin:0.15rem 0 0.3rem;">${label}</div>` +
      `<table>${matrix.map((row) => `<tr>${row.map((v) => `<td>${typeof v === "number" ? v.toFixed(3) : v}</td>`).join("")}</tr>`).join("")}</table>`;
  }

  function updatePlots(data) {
    container.querySelector("#ms-wn1").textContent = `ω₁ = ${data.wn[0].toFixed(3)}`;
    container.querySelector("#ms-wn2").textContent = `ω₂ = ${data.wn[1].toFixed(3)}`;
    container.querySelector("#ms-force-state").textContent = `${state.forceType === "pulse" ? "脉冲" : state.forceType === "step" ? "阶跃" : "简谐"} @ x${state.forceDof + 1}`;
    const modalPeak = Math.max(...data.fr1.map((v) => Math.abs(v)), ...data.fr2.map((v) => Math.abs(v)));
    container.querySelector("#ms-modal-force").textContent = modalPeak.toFixed(3);

    renderMatrix(container.querySelector("#ms-matrix-physical"), "M", data.M);
    container.querySelector("#ms-matrix-physical").innerHTML += '<div style="height:0.55rem;"></div>';
    renderMatrix(container.querySelector("#ms-matrix-physical"), "K", data.K);

    const modalMass = [[1, 0], [0, 1]];
    const modalDamp = [[2 * state.zeta * data.wn[0], 0], [0, 2 * state.zeta * data.wn[1]]];
    const modalStiff = [[data.wn[0] * data.wn[0], 0], [0, data.wn[1] * data.wn[1]]];
    const modalHtml = container.querySelector("#ms-matrix-modal");
    renderMatrix(modalHtml, "Φ", [[phi[0][0], phi[1][0]], [phi[0][1], phi[1][1]]]);
    modalHtml.innerHTML += '<div style="height:0.55rem;"></div>';
    renderMatrix(modalHtml, "M~", modalMass);
    modalHtml.innerHTML += '<div style="height:0.55rem;"></div>';
    renderMatrix(modalHtml, "C~", modalDamp);
    modalHtml.innerHTML += '<div style="height:0.55rem;"></div>';
    renderMatrix(modalHtml, "K~", modalStiff);

    const forceLimit = Math.max(0.25, ...data.f1.concat(data.f2, data.fr1, data.fr2).map((v) => Math.abs(v))) * 1.15;
    Plotly.react(container.querySelector("#ms-force-plot"), [
      {x: data.time, y: data.f1, mode: "lines", name: "F₁(t)", line: {color: "#0891b2", width: 2.4}},
      {x: data.time, y: data.f2, mode: "lines", name: "F₂(t)", line: {color: "#0f766e", width: 2.4}},
      {x: data.time, y: data.fr1, mode: "lines", name: "F~₁(t)", line: {color: "#f59e0b", width: 2.2, dash: "dot"}},
      {x: data.time, y: data.fr2, mode: "lines", name: "F~₂(t)", line: {color: "#dc2626", width: 2.2, dash: "dot"}},
    ], {
      margin: {l: 48, r: 18, t: 18, b: 42},
      xaxis: {title: "t", range: [0, data.tMax], autorange: false, fixedrange: true},
      yaxis: {title: "力 / 模态力", range: [-forceLimit, forceLimit], autorange: false, fixedrange: true},
      legend: {x: 0.98, y: 0.98, xanchor: "right", yanchor: "top", bgcolor: "rgba(255,255,255,0.82)", bordercolor: "rgba(148,163,184,0.25)", borderwidth: 1},
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff"
    }, {displayModeBar: false, responsive: true});

    const modalLimit = Math.max(0.25, ...data.p1.concat(data.p2).map((v) => Math.abs(v))) * 1.15;
    Plotly.react(container.querySelector("#ms-modal-plot"), [
      {x: data.time, y: data.p1, mode: "lines", name: "p₁(t)", line: {color: "#f59e0b", width: 2.5}},
      {x: data.time, y: data.p2, mode: "lines", name: "p₂(t)", line: {color: "#dc2626", width: 2.5}},
    ], {
      margin: {l: 48, r: 18, t: 18, b: 42},
      xaxis: {title: "t", range: [0, data.tMax], autorange: false, fixedrange: true},
      yaxis: {title: "主坐标 p", range: [-modalLimit, modalLimit], autorange: false, fixedrange: true},
      legend: {x: 0.98, y: 0.98, xanchor: "right", yanchor: "top", bgcolor: "rgba(255,255,255,0.82)", bordercolor: "rgba(148,163,184,0.25)", borderwidth: 1},
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff"
    }, {displayModeBar: false, responsive: true});

    const responseLimit = Math.max(0.25, ...data.q1.concat(data.q2).map((v) => Math.abs(v))) * 1.15;
    Plotly.react(container.querySelector("#ms-response-plot"), [
      {x: data.time, y: data.q1, mode: "lines", name: "x₁(t)", line: {color: "#0891b2", width: 2.5}},
      {x: data.time, y: data.q2, mode: "lines", name: "x₂(t)", line: {color: "#0f766e", width: 2.5}},
    ], {
      margin: {l: 48, r: 18, t: 18, b: 42},
      xaxis: {title: "t", range: [0, data.tMax], autorange: false, fixedrange: true},
      yaxis: {title: "物理位移 q", range: [-responseLimit, responseLimit], autorange: false, fixedrange: true},
      legend: {x: 0.98, y: 0.98, xanchor: "right", yanchor: "top", bgcolor: "rgba(255,255,255,0.82)", bordercolor: "rgba(148,163,184,0.25)", borderwidth: 1},
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff"
    }, {displayModeBar: false, responsive: true});
  }

  function currentIndex(data) {
    return Math.max(0, Math.min(data.time.length - 1, Math.round((t / data.tMax) * (data.time.length - 1))));
  }

  function draw(data) {
    const canvas = container.querySelector("#ms-canvas");
    const ctx = canvas.getContext("2d");
    const idx = currentIndex(data);
    const q = [data.q1[idx], data.q2[idx]];
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.lineWidth = 3;
    ctx.strokeStyle = "#94a3b8";
    ctx.fillStyle = "#f8fafc";
    const labelFont = '700 24px "Microsoft YaHei", "PingFang SC", "Noto Sans SC", sans-serif';
    const infoFont = '600 18px "Microsoft YaHei", "PingFang SC", "Noto Sans SC", sans-serif';
    ctx.textBaseline = "middle";
    ctx.fillStyle = "#cbd5e1";
    ctx.fillRect(25, 44, 18, 156);
    ctx.fillRect(777, 44, 18, 156);
    ctx.strokeStyle = "#64748b";
    ctx.strokeRect(25, 44, 18, 156);
    ctx.strokeRect(777, 44, 18, 156);
    const y = 126;
    const scale = 46;
    const x1 = 255 + scale * q[0];
    const x2 = 560 + scale * q[1];
    function spring(x0, x1p) {
      ctx.beginPath();
      ctx.moveTo(x0, y);
      for (let i = 1; i <= 18; i++) {
        const xp = x0 + (x1p - x0) * i / 19;
        const yp = y + (i % 2 ? -16 : 16);
        ctx.lineTo(xp, yp);
      }
      ctx.lineTo(x1p, y);
      ctx.stroke();
    }
    spring(43, x1 - 40);
    spring(x1 + 40, x2 - 40);
    spring(x2 + 40, 777);
    [["x₁", x1, "#0891b2"], ["x₂", x2, "#0f766e"]].forEach(([label, x, color]) => {
      ctx.fillStyle = color;
      ctx.fillRect(x - 40, y - 32, 80, 64);
      ctx.fillStyle = "#fff";
      ctx.font = labelFont;
      ctx.textAlign = "center";
      ctx.fillText(label, x, y + 2);
    });
    const force = state.forceDof === 0 ? data.f1[idx] : data.f2[idx];
    const arrowX = state.forceDof === 0 ? x1 : x2;
    const arrowDir = force >= 0 ? 1 : -1;
    const arrowBase = arrowX - 92 * arrowDir;
    const arrowTip = arrowX - 48 * arrowDir;
    const arrowY = y - 54;
    ctx.strokeStyle = "#ef4444";
    ctx.fillStyle = "#ef4444";
    ctx.lineWidth = 4;
    ctx.beginPath();
    ctx.moveTo(arrowBase, arrowY);
    ctx.lineTo(arrowTip, arrowY);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(arrowTip, arrowY);
    ctx.lineTo(arrowTip - 14 * arrowDir, arrowY - 10);
    ctx.lineTo(arrowTip - 14 * arrowDir, arrowY + 10);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = "#0f172a";
    ctx.font = infoFont;
    ctx.fillText(`F = ${force.toFixed(2)}`, arrowX, arrowY - 16);
  }

  function updateAll() {
    cached = computeResponse();
    updatePlots(cached);
    draw(cached);
  }

  container.querySelector("#ms-force-type").addEventListener("input", (event) => {
    state.forceType = event.target.value;
    t = 0;
    updateAll();
  });
  container.querySelector("#ms-force-dof").addEventListener("input", (event) => {
    state.forceDof = Number(event.target.value);
    t = 0;
    updateAll();
  });
  container.querySelector("#ms-play").addEventListener("click", () => {
    playing = !playing;
    container.querySelector("#ms-play").textContent = playing ? "暂停" : "播放";
    container.querySelector("#ms-state").textContent = playing ? "播放中" : "已暂停";
  });
  container.querySelector("#ms-reset").addEventListener("click", () => {
    t = 0;
    updateAll();
  });
  container.querySelector("#ms-preset-pulse").addEventListener("click", () => {
    state.forceType = "pulse";
    state.forceDof = 1;
    setValue("duration", 1.2);
    setValue("amp", 1.0);
    container.querySelector("#ms-force-type").value = "pulse";
    container.querySelector("#ms-force-dof").value = "1";
    t = 0;
    updateAll();
  });
  container.querySelector("#ms-preset-harmonic").addEventListener("click", () => {
    state.forceType = "harmonic";
    state.forceDof = 0;
    setValue("ratio", 0.98);
    setValue("zeta", 0.03);
    container.querySelector("#ms-force-type").value = "harmonic";
    container.querySelector("#ms-force-dof").value = "0";
    t = 0;
    updateAll();
  });

  function animate(timestamp) {
    if (lastTime === null) lastTime = timestamp;
    const dt = Math.min(0.05, (timestamp - lastTime) / 1000);
    lastTime = timestamp;
    if (playing && cached) {
      t = (t + dt) % cached.tMax;
      draw(cached);
    }
    requestAnimationFrame(animate);
  }

  updateAll();
  requestAnimationFrame(animate);
})();
</script>
"""

display(HTML(html))
```

```{figure} figures/fig2-15.png
---
name: fig:2-15
width: 75%
---
两自由度强迫振动系统
```

```{note}
**例2.15** 试求{numref}`fig:2-15`所示的两自由度系统的强迫振动响应。其中 $F_1(t)=0$，$F_2(t)$ 是一脉冲载荷：

$$
F_2(t) =
\begin{cases}
F_0 & (0 \leqslant t < t_1) \\
0 & (t \geqslant t_1)
\end{cases}
$$

解：该系统的运动方程式为

$$
\begin{bmatrix}M_1 & 0 \\0 & M_2\end{bmatrix}\begin{Bmatrix}\ddot{x}_1 \\\ddot{x}_2\end{Bmatrix}+\begin{bmatrix}C_1 + C_2 & -C_2 \\-C_2 & C_2 + C_3\end{bmatrix}\begin{Bmatrix}\dot{x}_1 \\\dot{x}_2\end{Bmatrix}+\begin{bmatrix}K_1 + K_2 & -K_2 \\-K_2 & K_2 + K_3\end{bmatrix}\begin{Bmatrix}x_1 \\x_2\end{Bmatrix}=\begin{Bmatrix}0 \\F_2(t)\end{Bmatrix}
$$

前已求得，当 $M_1 = M_2, K_1 = K_2 = K_3$ 时，系统的固有频率和固有振型分别为

$$
\omega_{\mathrm{n}1} = \sqrt{\frac{K_1}{M_1}}, \quad \omega_{\mathrm{n}2} = \sqrt{\frac{3K_1}{M_1}}
$$

和

$$
\boldsymbol{p}^{(1)} =
\begin{Bmatrix}
1 \\
1
\end{Bmatrix}, \quad
\boldsymbol{p}^{(2)} =
\begin{Bmatrix}
1 \\
-1
\end{Bmatrix}
$$

故其正则振型为

$$
\boldsymbol{\varphi}^{(1)} = \frac{1}{\sqrt{\boldsymbol{\rho}^{(1)\mathrm{T}} \boldsymbol{M} \boldsymbol{\rho}^{(1)}}}\begin{Bmatrix}1 \\1\end{Bmatrix} = \frac{1}{\sqrt{2M_1}}\begin{Bmatrix}1 \\1\end{Bmatrix}
$$

$$
\boldsymbol{\varphi}^{(2)} = \frac{1}{\sqrt{\boldsymbol{\rho}^{(2)\mathrm{T}} \boldsymbol{M} \boldsymbol{\rho}^{(2)}}}\begin{Bmatrix}1 \\-1\end{Bmatrix} = \frac{1}{\sqrt{2M_1}}\begin{Bmatrix}1 \\-1\end{Bmatrix}
$$

设 $C_1 = C_2 = C_3$，则运动方程式化为

$$
\begin{bmatrix}M_1 & 0 \\0 & M_2\end{bmatrix}\begin{Bmatrix}\ddot{x}_1 \\\ddot{x}_2\end{Bmatrix}+\begin{bmatrix}2C_1 & -C_1 \\-C_1 & 2C_1\end{bmatrix}\begin{Bmatrix}\dot{x}_1 \\\dot{x}_2\end{Bmatrix}+\begin{bmatrix}2K_1 & -K_1 \\-K_1 & 2K_1\end{bmatrix}\begin{Bmatrix}x_1 \\x_2\end{Bmatrix}=\begin{Bmatrix}0 \\F_2(t)\end{Bmatrix}
$$

令

$$
\boldsymbol{x}(t)=\boldsymbol{\varPhi}\boldsymbol{p}(t)
$$

代入上式并前乘 $\boldsymbol{\varPhi}^\mathrm{T}$ 可得

$$
\ddot{\boldsymbol{p}}+\boldsymbol{\varPhi}^\mathrm{T}\boldsymbol{C}\boldsymbol{\varPhi}\dot{\boldsymbol{p}}+\boldsymbol{\Lambda}\boldsymbol{p}=\boldsymbol{\varPhi}^\mathrm{T}\boldsymbol{F}=\widetilde{\boldsymbol{F}}(t)
$$

式中：

$$
\boldsymbol{\Lambda}=\mathrm{diag}\left(\omega_{\mathrm{n}1}^2,\omega_{\mathrm{n}2}^2\right)
$$

$$
\widetilde{\boldsymbol{C}}=\boldsymbol{\varPhi}^\mathrm{T}\boldsymbol{C}\boldsymbol{\varPhi}=\frac{C_1}{2M_1}\begin{bmatrix}1 & 1 \\1 & -1\end{bmatrix}\begin{bmatrix}2 & -1 \\-1 & 2\end{bmatrix}\begin{bmatrix}1 & 1 \\1 & -1\end{bmatrix}=\frac{C_1}{M_1}\begin{bmatrix}1 & 0 \\0 & 3\end{bmatrix}
$$

$\widetilde{\boldsymbol{C}}$ 是对角阵，因本例的阻尼矩阵 $\boldsymbol{C}$ 是比例阻尼 $\boldsymbol{C}=\alpha\boldsymbol{M}+\beta\boldsymbol{K}$ 在 $\alpha=0$ 及 $\beta=\frac{C_1}{K_1}$ 时的特例，故 $\widetilde{\boldsymbol{C}}$ 必为对角阵。于是可得

$$
2\zeta_1\omega_{\mathrm{n}1}=\frac{C_1}{M_1},\quad 2\zeta_2\omega_{\mathrm{n}2}=\frac{3C_1}{M_1}
$$

或

$$
\zeta_1=\frac{C_1}{2M_1\omega_{\mathrm{n}1}},\quad \zeta_2=\frac{3C_1}{2M_1\omega_{\mathrm{n}2}}
$$

$$
\widetilde{\boldsymbol{F}}(t)=\boldsymbol{\varPhi}^\mathrm{T}\boldsymbol{F}=\frac{1}{\sqrt{2M_1}}\begin{bmatrix}1 & 1 \\1 & -1\end{bmatrix}\begin{Bmatrix}0 \\F_2(t)\end{Bmatrix}=\frac{1}{\sqrt{2M_1}}\begin{Bmatrix}F_2(t) \\-F_2(t)\end{Bmatrix}
$$

因而

$$
\ddot{p}_1(t)+2\zeta_1\omega_{\mathrm{n}1}\dot{p}_1+\omega_{\mathrm{n}1}^2p_1=\frac{1}{\sqrt{2M_1}}F_2(t)
$$

$$
\ddot{p}_2(t)+2\zeta_2\omega_{\mathrm{n}2}\dot{p}_2+\omega_{\mathrm{n}2}^2p_2=\frac{-1}{\sqrt{2M_1}}F_2(t)
$$

可用杜哈梅积分求得任意激励 $F_2(t)$ 作用下的主坐标响应。为简单计，在计算时略去阻尼，并考虑零初始条件，于是当 $0 \leqslant t \leqslant t_1$ 时，

$$
p_1(t)=\frac{F_0}{\omega_{\mathrm{n}1}^2}\cdot\frac{1}{\sqrt{2M_1}}(1-\cos\omega_{\mathrm{n}1}t)
$$

$$
p_2(t)=\frac{F_0}{\omega_{\mathrm{n}2}^2}\cdot\frac{1}{\sqrt{2M_1}}(1-\cos\omega_{\mathrm{n}2}t)
$$

当 $t > t_1$ 时，

$$
p_1(t)=\frac{F_0}{\sqrt{2M_1}\omega_{\mathrm{n}1}^2}\cdot2\sin\frac{\omega_{\mathrm{n}1}t_1}{2}\sin\omega_{\mathrm{n}1}\left(t-\frac{t_1}{2}\right)
$$

$$
p_2(t)=\frac{F_0}{\sqrt{2M_1}\omega_{\mathrm{n}2}^2}\cdot2\sin\frac{\omega_{\mathrm{n}2}t_1}{2}\sin\omega_{\mathrm{n}2}\left(t-\frac{t_1}{2}\right)
$$

故最终可由叠加原理求得

$$
\begin{Bmatrix}x_1(t) \\x_2(t)\end{Bmatrix}=\frac{1}{\sqrt{2M_1}}\begin{bmatrix}1 & 1 \\1 & -1\end{bmatrix}\begin{Bmatrix}p_1(t) \\p_2(t)\end{Bmatrix}=\begin{Bmatrix}\frac{1}{\sqrt{2M_1}}[p_1(t)+p_2(t)] \\\frac{1}{\sqrt{2M_1}}[p_1(t)-p_2(t)]\end{Bmatrix}
$$

因而两个阶段的系统物理坐标的响应分别为：

当 $0\leqslant t\leqslant t_1$ 时，

$$
x_1(t)=\frac{F_0}{2M_1}\left[\frac{1-\cos\omega_{\mathrm{n}1}t}{\omega_{\mathrm{n}1}^2}+\frac{1-\cos\omega_{\mathrm{n}2}t}{\omega_{\mathrm{n}2}^2}\right]
$$

$$
x_2(t)=\frac{F_0}{2M_1}\left[\frac{1-\cos\omega_{\mathrm{n}1}t}{\omega_{\mathrm{n}1}^2}-\frac{1-\cos\omega_{\mathrm{n}2}t}{\omega_{\mathrm{n}2}^2}\right]
$$

当 $t>t_1$ 时，

$$
x_1(t)=\frac{F_0}{M_1}\left[\frac{1}{\omega_{\mathrm{n}1}^2}\sin\frac{\omega_{\mathrm{n}1}t_1}{2}\sin\omega_{\mathrm{n}1}\left(t-\frac{t_1}{2}\right)+\frac{1}{\omega_{\mathrm{n}2}^2}\sin\frac{\omega_{\mathrm{n}2}t_1}{2}\sin\omega_{\mathrm{n}2}\left(t-\frac{t_1}{2}\right)\right]
$$

$$
x_2(t)=\frac{F_0}{M_1}\left[\frac{1}{\omega_{\mathrm{n}1}^2}\sin\frac{\omega_{\mathrm{n}1}t_1}{2}\sin\omega_{\mathrm{n}1}\left(t-\frac{t_1}{2}\right)-\frac{1}{\omega_{\mathrm{n}2}^2}\sin\frac{\omega_{\mathrm{n}2}t_1}{2}\sin\omega_{\mathrm{n}2}\left(t-\frac{t_1}{2}\right)\right]
$$
```

## 振型叠加法求系统对正弦激励的响应

若系统受到简谐激励 $\boldsymbol{F}(t)=\boldsymbol{F}_0\sin\omega t$ 作用，则式 {eq}`eq:2-106` 可写为

$$
\ddot{p}_r+2\zeta_r\omega_{\mathrm{n}r}\dot{p}_r+\omega_{\mathrm{n}r}^2p_r=\left(\frac{1}{M_r}\right)\widetilde{F}_r\sin\omega t
$$

式中，

$$
\widetilde{F}_r=\boldsymbol{\varphi}^{(r)\mathrm{T}}\boldsymbol{F}_0
$$ (eq:2-112)

如果将正弦力用复数力表示，则

$$
\ddot{\overline{p}}_r+2\zeta_r\omega_{\mathrm{n}r}\dot{\overline{p}}_r+\omega_{\mathrm{n}r}^2\overline{p}_r=\left(\frac{1}{M_r}\right)\widetilde{F}_r\mathrm{e}^{\mathrm{i}\omega t}
$$ (eq:2-113)

主坐标 $\overline{p}_r$ 的复数稳态解可写为如下形式：

$$
\overline{p}_r=\overline{H}(\omega)\widetilde{F}_r\mathrm{e}^{\mathrm{i}\omega t}
$$ (eq:2-114)

式中，$\overline{H}(\omega)$——主坐标的复频响应函数，其中第 $r$ 个元素为

$$
\overline{H}_r(\omega)=\frac{1/K_r}{(1-\gamma_r^2)+\mathrm{i}(2\zeta_r\gamma_r)},\quad K_r=M_r\omega_{\mathrm{n}r}^2\text{ 和 }\gamma_r=\frac{\omega}{\omega_{\mathrm{n}r}}
$$ (eq:2-115)

因此，物理坐标的频率响应可写为以下复数形式：

$$
\overline{\boldsymbol{q}}=\boldsymbol{\Phi}\overline{\boldsymbol{p}}(t)=\sum_{r=1}^n\boldsymbol{\varphi}^{(r)}\overline{p}_r(t)
$$

$$
=\sum_{r=1}^n\left(\frac{\boldsymbol{\varphi}^{(r)}\boldsymbol{\varphi}^{(r)\mathrm{T}}\boldsymbol{F}_0}{K_r}\right)\left[\frac{1}{(1-\gamma_r^2)+\mathrm{i}(2\zeta_r\gamma_r)}\right]\mathrm{e}^{\mathrm{i}\omega t}
$$ (eq:2-116)

因而，在坐标 $q_j$ 处受单位简谐力作用，而在坐标 $q_i$ 处的复数响应

$$
\overline{H}_{ij}(\omega)=\sum_{r=1}^n\left(\frac{\varphi_j^{(r)}\varphi_i^{(r)}}{K_r}\right)\left[\frac{1}{(1-\gamma_r^2)+\mathrm{i}(2\zeta_r\gamma_r)}\right]
$$ (eq:2-117)

称为复频响应函数，有时又称为传递函数，常常用于实验确定系统振动特性的场合。由式 {eq}`eq:2-117` 不难写出其实部和虚部。激励力是正弦形式，取式 {eq}`eq:2-116` 的虚部，便得到稳态响应的最终解：

$$
\boldsymbol{q}(t)=\sum_{r=1}^n\left(\frac{\boldsymbol{\varphi}^{(r)}\boldsymbol{\varphi}^{(r)\mathrm{T}}\boldsymbol{F}_0}{K_r}\right)\left[\frac{1}{\sqrt{(1-\gamma_r^2)^2+(2\zeta_r\gamma_r)^2}}\right]\sin(\omega t-\alpha_r)
$$ (eq:2-118)

式中，

$$
\alpha = \arctan\frac{2\zeta_r\gamma_r}{1-\gamma_r^2}
$$ (eq:2-119)

## 交互探索：扫频频响与模态贡献

下方交互探索基于两自由度对称系统的模态频响函数展开式。可以切换输入坐标与输出坐标，观察总频响函数与各阶模态贡献在扫频过程中如何叠加，并实时查看当前频率下的实部、虚部、幅值与相位。

```{code-cell} ipython3
:tags: [remove-input]

from IPython.display import HTML, display

html = r"""
<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>
<style>
  #frf-contribution-demo input[type="range"] {
    width: 100%;
    accent-color: #b45309;
  }
  #frf-contribution-demo button,
  #frf-contribution-demo select {
    border-radius: 12px;
    border: 1px solid rgba(180,83,9,0.18);
    background: #ffffff;
    color: #0f172a;
    font-weight: 700;
  }
  #frf-contribution-demo button {
    padding: 0.58rem 0.82rem;
    cursor: pointer;
  }
</style>
<div id="frf-contribution-demo" style="border:1px solid rgba(180,83,9,0.14); border-radius:20px; padding:1rem; background:linear-gradient(180deg,#fffbeb 0%,#ffffff 100%);">
  <div style="display:grid; grid-template-columns:repeat(6,minmax(0,1fr)); gap:0.75rem; margin-bottom:0.95rem;">
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">一阶频率</div>
      <div id="frf-wn1" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#b45309;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">二阶频率</div>
      <div id="frf-wn2" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#b45309;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">当前频率</div>
      <div id="frf-w" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">|H<sub>ij</sub>|</div>
      <div id="frf-mag" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">Re(H<sub>ij</sub>)</div>
      <div id="frf-real" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">-</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">Im(H<sub>ij</sub>)</div>
      <div id="frf-imag" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">-</div>
    </div>
  </div>

  <div style="display:grid; grid-template-columns:minmax(0,1fr); gap:1rem; align-items:start;">
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div id="frf-controls" style="display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:0.72rem 0.9rem;"></div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:0.7rem; margin-top:0.8rem;">
        <label style="font-size:13px; font-weight:800; color:#334155;">输入坐标 j
          <select id="frf-input" style="width:100%; margin-top:0.35rem; padding:0.5rem 0.6rem;">
            <option value="0">x₁</option>
            <option value="1">x₂</option>
          </select>
        </label>
        <label style="font-size:13px; font-weight:800; color:#334155;">输出坐标 i
          <select id="frf-output" style="width:100%; margin-top:0.35rem; padding:0.5rem 0.6rem;">
            <option value="0">x₁</option>
            <option value="1">x₂</option>
          </select>
        </label>
      </div>
      <div style="display:flex; gap:0.5rem; flex-wrap:wrap; margin-top:0.9rem;">
        <button id="frf-reset">重置参数</button>
        <button id="frf-drive-11">看 H₁₁</button>
        <button id="frf-drive-21">看 H₂₁</button>
      </div>
    </div>
    <div style="padding:0.9rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <canvas id="frf-canvas" width="820" height="250" style="width:100%; aspect-ratio:820 / 250; height:auto; display:block;"></canvas>
    </div>
  </div>

  <div style="display:grid; grid-template-columns:minmax(0,1fr); gap:1rem; margin-top:1rem;">
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div id="frf-mag-plot" style="width:100%; height:320px;"></div>
    </div>
    <div style="padding:0.95rem; border-radius:18px; background:#fff; border:1px solid rgba(148,163,184,0.18);">
      <div id="frf-complex-plot" style="width:100%; height:300px;"></div>
    </div>
  </div>
</div>

<script>
(() => {
  const container = document.querySelector("#frf-contribution-demo");
  if (!container || container.dataset.ready) return;
  container.dataset.ready = "true";

  const sliders = [
    ["m", "质量 m", 0.5, 5, 0.1, 1.0],
    ["k1", "端部刚度 k₁", 0.5, 8, 0.1, 1.0],
    ["k2", "耦合刚度 k₂", 0.2, 5, 0.1, 1.0],
    ["zeta", "模态阻尼比 ζ", 0.005, 0.10, 0.005, 0.02],
    ["wratio", "当前频率比 ω/ω₁", 0.2, 2.2, 0.01, 0.8],
  ];
  const state = {input: 0, output: 0};
  const phi = [
    [1 / Math.sqrt(2), 1 / Math.sqrt(2)],
    [1 / Math.sqrt(2), -1 / Math.sqrt(2)],
  ];

  function addSlider(item) {
    const [key, label, min, max, step, value] = item;
    state[key] = value;
    const row = document.createElement("div");
    row.style.minWidth = "0";
    row.innerHTML = `
      <label style="display:flex; justify-content:space-between; gap:0.8rem; font-size:13px; color:#334155; font-weight:800;">
        <span>${label}</span><span id="frf-${key}-value">${value.toFixed(3)}</span>
      </label>
      <input id="frf-${key}" type="range" min="${min}" max="${max}" step="${step}" value="${value}">
    `;
    container.querySelector("#frf-controls").appendChild(row);
    row.querySelector("input").addEventListener("input", (event) => {
      state[key] = Number(event.target.value);
      row.querySelector(`#frf-${key}-value`).textContent = state[key].toFixed(3);
      updateAll();
    });
  }
  sliders.forEach(addSlider);

  function setValue(key, value) {
    state[key] = value;
    const input = container.querySelector(`#frf-${key}`);
    const label = container.querySelector(`#frf-${key}-value`);
    if (input) input.value = String(value);
    if (label) label.textContent = Number(value).toFixed(3);
  }

  function systemData() {
    const m = state.m;
    const k1 = state.k1;
    const k2 = state.k2;
    const wn = [Math.sqrt(k1 / m), Math.sqrt((k1 + 2 * k2) / m)];
    return {m, k1, k2, wn};
  }

  function frfTerm(i, j, r, omega, wn) {
    const coeff = (phi[r][i] * phi[r][j]) / (wn[r] * wn[r]);
    const gamma = omega / wn[r];
    const denRe = 1 - gamma * gamma;
    const denIm = 2 * state.zeta * gamma;
    const denom = denRe * denRe + denIm * denIm;
    return {
      re: coeff * denRe / denom,
      im: -coeff * denIm / denom,
      mag: Math.abs(coeff) / Math.sqrt(denom),
    };
  }

  function compute() {
    const {wn, m, k1, k2} = systemData();
    const omegaMax = wn[1] * 1.9;
    const omega = [];
    const totalMag = [];
    const real = [];
    const imag = [];
    const modal1 = [];
    const modal2 = [];
    for (let idx = 0; idx < 480; idx++) {
      const w = omegaMax * idx / 479;
      const t1 = frfTerm(state.output, state.input, 0, w, wn);
      const t2 = frfTerm(state.output, state.input, 1, w, wn);
      omega.push(w);
      totalMag.push(Math.hypot(t1.re + t2.re, t1.im + t2.im));
      real.push(t1.re + t2.re);
      imag.push(t1.im + t2.im);
      modal1.push(t1.mag);
      modal2.push(t2.mag);
    }
    const currentW = state.wratio * wn[0];
    const c1 = frfTerm(state.output, state.input, 0, currentW, wn);
    const c2 = frfTerm(state.output, state.input, 1, currentW, wn);
    const current = {
      w: currentW,
      re: c1.re + c2.re,
      im: c1.im + c2.im,
      mag: Math.hypot(c1.re + c2.re, c1.im + c2.im),
      phase: Math.atan2(c1.im + c2.im, c1.re + c2.re),
      modal1: c1,
      modal2: c2,
    };
    return {m, k1, k2, wn, omega, totalMag, real, imag, modal1, modal2, current, omegaMax};
  }

  function draw(data) {
    const canvas = container.querySelector("#frf-canvas");
    const ctx = canvas.getContext("2d");
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.lineWidth = 3;
    ctx.strokeStyle = "#94a3b8";
    ctx.fillStyle = "#f8fafc";
    const labelFont = '700 24px "Microsoft YaHei", "PingFang SC", "Noto Sans SC", sans-serif';
    const infoFont = '600 18px "Microsoft YaHei", "PingFang SC", "Noto Sans SC", sans-serif';
    ctx.textBaseline = "middle";
    ctx.fillStyle = "#cbd5e1";
    ctx.fillRect(25, 44, 18, 156);
    ctx.fillRect(777, 44, 18, 156);
    ctx.strokeStyle = "#64748b";
    ctx.strokeRect(25, 44, 18, 156);
    ctx.strokeRect(777, 44, 18, 156);
    const y = 126;
    const x1 = 255;
    const x2 = 560;
    function spring(x0, x1p) {
      ctx.beginPath();
      ctx.moveTo(x0, y);
      for (let i = 1; i <= 18; i++) {
        const xp = x0 + (x1p - x0) * i / 19;
        const yp = y + (i % 2 ? -16 : 16);
        ctx.lineTo(xp, yp);
      }
      ctx.lineTo(x1p, y);
      ctx.stroke();
    }
    spring(43, x1 - 40);
    spring(x1 + 40, x2 - 40);
    spring(x2 + 40, 777);
    [["x₁", x1, "#b45309"], ["x₂", x2, "#0f766e"]].forEach(([label, x, color]) => {
      ctx.fillStyle = color;
      ctx.fillRect(x - 40, y - 32, 80, 64);
      ctx.fillStyle = "#fff";
      ctx.font = labelFont;
      ctx.textAlign = "center";
      ctx.fillText(label, x, y + 2);
    });
    ctx.strokeStyle = "#2563eb";
    ctx.fillStyle = "#2563eb";
    ctx.lineWidth = 4;
    const inputX = state.input === 0 ? x1 : x2;
    const inputArrowY = y - 54;
    ctx.beginPath();
    ctx.moveTo(inputX - 92, inputArrowY);
    ctx.lineTo(inputX - 48, inputArrowY);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(inputX - 48, inputArrowY);
    ctx.lineTo(inputX - 62, inputArrowY - 10);
    ctx.lineTo(inputX - 62, inputArrowY + 10);
    ctx.closePath();
    ctx.fill();

    const ampScale = 70;
    const responseX = state.output === 0 ? x1 : x2;
    const response = data.current.mag * Math.sin(data.current.phase);
    ctx.strokeStyle = "#dc2626";
    ctx.lineWidth = 5;
    ctx.beginPath();
    ctx.moveTo(responseX - ampScale * response, 210);
    ctx.lineTo(responseX + ampScale * response, 210);
    ctx.stroke();
    ctx.fillStyle = "#0f172a";
    ctx.font = infoFont;
    ctx.textAlign = "left";
    ctx.fillText(`输入: x${state.input + 1}  输出: x${state.output + 1}`, 560, 34);
    ctx.fillText(`激励: F0 sinωt`, 560, 56);
    ctx.fillText(`相位: ${data.current.phase.toFixed(2)} rad`, 560, 78);
  }

  function updatePlots(data) {
    container.querySelector("#frf-wn1").textContent = `ω₁ = ${data.wn[0].toFixed(3)}`;
    container.querySelector("#frf-wn2").textContent = `ω₂ = ${data.wn[1].toFixed(3)}`;
    container.querySelector("#frf-w").textContent = data.current.w.toFixed(3);
    container.querySelector("#frf-mag").textContent = data.current.mag.toExponential(2);
    container.querySelector("#frf-real").textContent = data.current.re.toExponential(2);
    container.querySelector("#frf-imag").textContent = data.current.im.toExponential(2);

    const magMax = Math.max(...data.totalMag, ...data.modal1, ...data.modal2) * 1.08;
    Plotly.react(container.querySelector("#frf-mag-plot"), [
      {x: data.omega, y: data.totalMag, mode: "lines", name: "|Hᵢⱼ| 总和", line: {color: "#0f172a", width: 3}},
      {x: data.omega, y: data.modal1, mode: "lines", name: "一阶贡献", line: {color: "#b45309", width: 2.4}},
      {x: data.omega, y: data.modal2, mode: "lines", name: "二阶贡献", line: {color: "#0f766e", width: 2.4}},
      {x: [data.current.w], y: [data.current.mag], mode: "markers", name: "当前频率", marker: {size: 10, color: "#2563eb"}}
    ], {
      margin: {l: 48, r: 18, t: 18, b: 42},
      xaxis: {title: "ω", range: [0, data.omegaMax], autorange: false, fixedrange: true},
      yaxis: {title: "|Hᵢⱼ(ω)|", range: [0, magMax], autorange: false, fixedrange: true},
      legend: {x: 0.98, y: 0.98, xanchor: "right", yanchor: "top", bgcolor: "rgba(255,255,255,0.82)", bordercolor: "rgba(148,163,184,0.25)", borderwidth: 1},
      shapes: [
        {type: "line", x0: data.wn[0], x1: data.wn[0], y0: 0, y1: magMax, line: {color: "rgba(180,83,9,0.35)", dash: "dash"}},
        {type: "line", x0: data.wn[1], x1: data.wn[1], y0: 0, y1: magMax, line: {color: "rgba(15,118,110,0.35)", dash: "dash"}},
      ],
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff"
    }, {displayModeBar: false, responsive: true});

    const cMax = Math.max(...data.real.map((v) => Math.abs(v)), ...data.imag.map((v) => Math.abs(v))) * 1.12;
    Plotly.react(container.querySelector("#frf-complex-plot"), [
      {x: data.omega, y: data.real, mode: "lines", name: "Re(Hᵢⱼ)", line: {color: "#b45309", width: 2.5}},
      {x: data.omega, y: data.imag, mode: "lines", name: "Im(Hᵢⱼ)", line: {color: "#0f766e", width: 2.5}},
      {x: [data.current.w], y: [data.current.re], mode: "markers", name: "当前 Re", marker: {size: 9, color: "#b45309"}},
      {x: [data.current.w], y: [data.current.im], mode: "markers", name: "当前 Im", marker: {size: 9, color: "#0f766e"}}
    ], {
      margin: {l: 48, r: 18, t: 18, b: 42},
      xaxis: {title: "ω", range: [0, data.omegaMax], autorange: false, fixedrange: true},
      yaxis: {title: "复频响分量", range: [-cMax, cMax], autorange: false, fixedrange: true},
      legend: {x: 0.98, y: 0.98, xanchor: "right", yanchor: "top", bgcolor: "rgba(255,255,255,0.82)", bordercolor: "rgba(148,163,184,0.25)", borderwidth: 1},
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff"
    }, {displayModeBar: false, responsive: true});
  }

  function updateAll() {
    const data = compute();
    updatePlots(data);
    draw(data);
  }

  container.querySelector("#frf-input").addEventListener("input", (event) => {
    state.input = Number(event.target.value);
    updateAll();
  });
  container.querySelector("#frf-output").addEventListener("input", (event) => {
    state.output = Number(event.target.value);
    updateAll();
  });
  container.querySelector("#frf-reset").addEventListener("click", () => {
    setValue("m", 1.0);
    setValue("k1", 1.0);
    setValue("k2", 1.0);
    setValue("zeta", 0.02);
    setValue("wratio", 0.8);
    state.input = 0;
    state.output = 0;
    container.querySelector("#frf-input").value = "0";
    container.querySelector("#frf-output").value = "0";
    updateAll();
  });
  container.querySelector("#frf-drive-11").addEventListener("click", () => {
    state.input = 0;
    state.output = 0;
    container.querySelector("#frf-input").value = "0";
    container.querySelector("#frf-output").value = "0";
    updateAll();
  });
  container.querySelector("#frf-drive-21").addEventListener("click", () => {
    state.input = 0;
    state.output = 1;
    container.querySelector("#frf-input").value = "0";
    container.querySelector("#frf-output").value = "1";
    updateAll();
  });

  updateAll();
})();
</script>
"""

display(HTML(html))
```

```{figure} figures/fig2-16.png
---
name: fig:2-16
width: 90%
---
例2.16的两自由度系统及受力图
```

```{note}
**例2.16** 如{numref}`fig:2-16`(a)所示的两自由度系统，已知 $Q_1(t)=Q_0\cos\omega t,K_1=987,K_2=217,C_1=0.6284,C_2=0.0628$。求：

1. 无阻尼的固有频率 $f_1$、$f_2$ 和固有振型。

2. 复频率响应函数 $H_{11}(\omega)$ 及 $H_{21}(\omega)$ 的模态展开式。

解：(1) 由{numref}`fig:2-16`(b)所示的受力图，写出运动微分方程为

$$
\begin{bmatrix}M & 0 \cr 0 & M\end{bmatrix}\begin{Bmatrix}\ddot{x}_1 \cr \ddot{x}_2\end{Bmatrix}+\begin{bmatrix}(C_1+C_2) & -C_2 \cr -C_2 & (C_1+C_2)\end{bmatrix}\begin{Bmatrix}\dot{x}_1 \cr \dot{x}_2\end{Bmatrix}+
$$

$$
\begin{bmatrix}(K_1+K_2) & -K_2 \cr -K_2 & (K_1+K_2)\end{bmatrix}\begin{Bmatrix}x_1 \cr x_2\end{Bmatrix}=\begin{Bmatrix}Q_0\cos\omega t \cr 0\end{Bmatrix} \tag{1}
$$

由特征值问题

$$
\left[\begin{bmatrix}(K_1+K_2) & -K_2 \cr -K_2 & (K_1+K_2)\end{bmatrix}-\omega_{\mathrm{n}}^2\begin{bmatrix}M & 0 \cr 0 & M\end{bmatrix}\right]\begin{Bmatrix}A_1 \cr A_2\end{Bmatrix}=\begin{Bmatrix}0 \cr 0\end{Bmatrix} \tag{2}
$$

求得

$$
\omega_{\mathrm{n}1}^2=\frac{K}{M}=987,\quad (\boldsymbol{\rho}^{(1)})^\mathrm{T}=\{1\quad 1\}
$$ 

$$
\omega_{\mathrm{n}2}^2=\frac{K_1+2K_2}{M}=1421,\quad (\boldsymbol{\rho}^{(2)})^\mathrm{T}=\{1\quad -1\}  \tag{3}
$$

于是有

$$
f_1=\frac{\omega_{\mathrm{n}1}}{2\pi}=\frac{31.42}{2\pi}=5.00\mathrm{Hz}
$$ 

$$
f_2=\frac{\omega_{\mathrm{n}2}}{2\pi}=\frac{37.70}{2\pi}=6.00\mathrm{Hz}  \tag{4}
$$

并经正则化，求出正则振型为

$$
\boldsymbol{\varphi}^{(1)}=\begin{Bmatrix}0.707 \cr 0.707\end{Bmatrix},\quad \boldsymbol{\varphi}^{(2)}=\begin{Bmatrix}0.707 \cr -0.707\end{Bmatrix} \tag{5}
$$

(2) 求复频率响应函数 $H_{11}(\omega)$、$H_{21}(\omega)$。由式 {eq}`eq:2-119` 得

$$
H_{ij}(\omega)=\sum_{r=1}^2\frac{\varphi_i^{(r)}\varphi_j^{(r)}}{\omega_{\mathrm{n}r}^2}\frac{1}{1-\left(\frac{\omega}{\omega_{\mathrm{n}r}}\right)^2+\mathrm{i}2\zeta_r\left(\frac{\omega}{\omega_{\mathrm{n}r}}\right)} \tag{6}
$$

即有

$$
\begin{aligned}
H_{11}(\omega) &= \frac{(0.707)(0.707)}{987}\left[\frac{1}{1-\frac{\omega^2}{(31.42)^2}+\mathrm{i}2(0.01)\frac{\omega}{31.42}}\right] \cr
&\quad + \frac{(0.707)(0.707)}{1421}\left[\frac{1}{1-\frac{\omega^2}{(37.7)^2}+\mathrm{i}2(0.01)\frac{\omega}{37.7}}\right] \cr
&= \frac{(5.064)\times10^{-4}}{1-\frac{\omega^2}{(31.42)^2}+\mathrm{i}(0.02)\frac{\omega}{31.42}} + \frac{(3.58)\times10^{-4}}{1-\frac{\omega^2}{(31.42)^2}+\mathrm{i}(0.02)\frac{\omega}{37.7}}
\end{aligned}
\tag{7}
$$

同样有

$$
\begin{aligned}
H_{21}(\omega) &= \frac{(0.707)(0.707)}{987}\left[\frac{1}{1-\frac{\omega^2}{(31.42)^2}+\mathrm{i}2(0.01)\frac{\omega}{31.42}}\right] \cr
&\quad + \frac{(-0.707)(0.707)}{1421}\left[\frac{1}{1-\frac{\omega^2}{(37.7)^2}+\mathrm{i}2(0.01)\frac{\omega}{37.7}}\right] \cr
&= \frac{(5.064)\times10^{-4}}{1-\frac{\omega^2}{(31.42)^2}+\mathrm{i}(0.02)\frac{\omega}{31.42}} - \frac{(3.519)\times10^{-4}}{1-\frac{\omega^2}{(37.7)^2}+\mathrm{i}(0.02)\frac{\omega}{37.7}}
\end{aligned}
\tag{8}
$$

将式(6)的实部和虚部表示如下：

$$
R(H_{ij})=\sum_{r=1}^2\left(\frac{\varphi_i^{(r)}\varphi_j^{(r)}}{\omega_{\mathrm{n}r}^2}\right)\left[\frac{1-\left(\frac{\omega}{\omega_{\mathrm{n}r}}\right)^2}{\left(1-\left(\frac{\omega}{\omega_{\mathrm{n}r}}\right)^2\right)^2+\left(2\zeta_r\frac{\omega}{\omega_{\mathrm{n}r}}\right)^2}\right] \tag{9}
$$

$$
I(H_{ij})=\sum_{r=1}^2\left(\frac{\varphi_i^{(r)}\varphi_j^{(r)}}{\omega_{\mathrm{n}r}^2}\right)\left[\frac{-2\zeta_r\frac{\omega}{\omega_{\mathrm{n}r}}}{\left(1-\left(\frac{\omega}{\omega_{\mathrm{n}r}}\right)^2\right)^2+\left(2\zeta_r\frac{\omega}{\omega_{\mathrm{n}r}}\right)^2}\right] \tag{10}
$$

式(9)和式(10)均是激励频率 $\omega$ 的实函数。若将 $H_{11}(\omega)$ 的实部和虚部分别用式(9)和式(10)表示，并令 $f=\frac{\omega}{2\pi}$，则可以分别绘出 $R(H_{11})$ 和 $I(H_{11})$ 的函数曲线，如{numref}`fig:2-17`所示。由图可见，在固有频率 $f_1$ 和 $f_2$ 处，$R(H_{11})$ 接近等于零，而 $I(H_{11})$ 有最大值。此种曲线在振动试验中很有用。
```

```{figure} figures/fig2-17.png
---
name: fig:2-17
width: 75%
---
频率响应函数的实部和虚部
```

```{figure} figures/fig2-18.png
---
name: fig:2-18
width: 70%
---
三质量弹簧系统
```

```{note}
**例2.17** {numref}`fig:2-18`示三质量弹簧系统，在其最外端质量 $M$ 上作用了常值阶跃激励力 $p_0$。若不计系统阻尼，试求系统的零初值位移响应。

解：(1) 先行求得该系统的主模态，即模态频率（主频率）为

$$
\omega_{\mathrm{n}1}^2=0.198\frac{K}{M};\quad \omega_{\mathrm{n}2}^2=1.555\frac{K}{M};\quad \omega_{\mathrm{n}3}^2=3.247\frac{K}{M}
$$

模态矩阵为

$$
\boldsymbol{\rho}=\begin{bmatrix}1 & 1 & 1 \\1.802 & 0.445 & -1.247 \\2.247 & -0.802 & 0.555\end{bmatrix}
$$

模态质量为

$$
M^{(1)}=9.296;\quad M^{(2)}=1.841;\quad M^{(3)}=2.863
$$

(2) 计算广义力。

因为

$$
\boldsymbol{F} = \begin{bmatrix}0 & 0 & p_0\end{bmatrix}^\mathrm{T}
$$

则广义力

$$
F_r(t)=\boldsymbol{\rho}^{(r)}_{(1\times3)}\boldsymbol{F}_{(3\times1)}\ (r=1,2,3)
$$

$$
F_1(t)=2.247p_0
$$

$$
F_2(t)=-0.802p_0
$$

$$
F_3(t)=0.555p_0
$$

(3) 计算主坐标的位移。

$$
p_i=\frac{F_i}{m^{(i)}\omega_{\mathrm{n}i}^2}(1-\cos\omega_{\mathrm{n}i}t)\quad (t>0,i=1,2,3)
$$

故

$$
\boldsymbol{p}=\begin{Bmatrix}p_1 \\p_2 \\p_3\end{Bmatrix}=\begin{Bmatrix}1.221(1-\cos\omega_{\mathrm{n}1}t) \\-0.280(1-\cos\omega_{\mathrm{n}2}t) \\0.060(1-\cos\omega_{\mathrm{n}3}t)\end{Bmatrix}\frac{p_0}{K}
$$

(4) 由振型叠加法求得系统物理坐标的响应。

$$
\boldsymbol{q}=\boldsymbol{\rho}\boldsymbol{p}=\begin{bmatrix}1 & 1 & 1 \\1.802 & 0.445 & -1.247 \\2.247 & -0.802 & 0.555\end{bmatrix}\begin{Bmatrix}1.221(1-\cos\omega_{\mathrm{n}1}t) \\-0.280(1-\cos\omega_{\mathrm{n}2}t) \\0.060(1-\cos\omega_{\mathrm{n}3}t)\end{Bmatrix}\frac{p_0}{K}
$$
```

### 直接解法求系统对正弦激励的响应

当系统受到简谐激励作用时，可以用复数方法直接求解。设简谐激励力为 $F_0\mathrm{e}^{\mathrm{i}\omega t}$，则运动方程式为

$$
\boldsymbol{M}\ddot{\boldsymbol{q}}+\boldsymbol{C}\dot{\boldsymbol{q}}+\boldsymbol{K}\boldsymbol{q}=\boldsymbol{F}_0\mathrm{e}^{\mathrm{i}\omega t}
$$ (eq:2-120)

式中，$\boldsymbol{F}_0=[F_{01},F_{02},\dots,F_{0n}]^\mathrm{T}$——激励力的幅值分量。

设其稳态响应为

$$
\boldsymbol{q}(t)=\boldsymbol{q}_0\mathrm{e}^{\mathrm{i}\omega t}
$$ (eq:2-121)

代入式{eq}`eq:2-120`，并消去 $\mathrm{e}^{\mathrm{i}\omega t}$ 后，便得到矩阵运算式：

$$
(\boldsymbol{K}-\omega^2\boldsymbol{M}+\mathrm{i}\omega\boldsymbol{C})\boldsymbol{q}_0=\boldsymbol{F}_0
$$ (eq:2-122)

设矩阵

$$
\boldsymbol{G}=(\boldsymbol{K}-\omega^2\boldsymbol{M}+\mathrm{i}\omega\boldsymbol{C})^{-1}
$$ (eq:2-123)

代入上式即可得

$$
\boldsymbol{q}_0=\boldsymbol{G}\boldsymbol{F}_0
$$ (eq:2-124)

则式{eq}`eq:2-120`的稳态响应为

$$
\boldsymbol{q}(t)=\boldsymbol{G}\boldsymbol{F}_0\mathrm{e}^{\mathrm{i}\omega t}
$$ (eq:2-125)

由以上两式可知，矩阵 $\boldsymbol{G}$ 建立了系统的激励（又称输入）$\boldsymbol{F}_0$ 和位移响应（又称输出）$\boldsymbol{q}_0$ 之间的联系，因此称为传递函数矩阵。由于其元素是随激励频率 $\omega$ 变化的复函数，又因其具有柔度的量纲，故也可称为动柔度矩阵，有时亦称为复频接受率矩阵或导纳矩阵。

$\boldsymbol{G}$ 的物理意义是，由单位简谐力所引起的系统响应。其元素 $g_{ij}$ 表示在 $j$ 坐标上作用一个圆频率为 $\omega$ 的单位简谐力时，在 $i$ 坐标所引起的稳态响应（包括振幅和相位的响应）。在工程中，常由实验方法确定 $g_{ij}$，从而确定系统的固有频率。传递函数的测试与分析，广泛地应用于工程中。
