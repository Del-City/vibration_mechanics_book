---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
---

# 简谐载荷作用下的强迫振动

本节讨论系统受到简谐变化的干扰力作用下的强迫振动响应问题。这种干扰力是经常碰到的，如旋转的机械对楼板的作用。首先讨论不考虑阻尼作用的情况，然后讨论考虑阻尼作用的强迫振动。

## 无阻尼强迫振动

假定荷载 $F_{\mathrm{e}}(t)$ 为如下的简谐形式

$$
F_{\mathrm{e}}(t)=P\cos\theta t
$$ (eq:1-54)

由式{eq}`eq:1-3`可知无阻尼强迫振动方程为

$$
m\ddot{x}(t)+kx(t)=P\cos\theta t
$$ (eq:1-55)

或写为

$$
\ddot{x}(t)+\omega^{2}x(t)=\frac{P}{m}\cos\theta t
$$ (eq:1-56)

方程{eq}`eq:1-56`的通解可由齐次方程的通解和非齐次方程的特解叠加而得。由 $F_{\mathrm{e}}(t)$ 的形式, 我们不难找到它的一个特解

$$
x_{\mathrm{p}}(t)=\frac{P}{m(\omega^{2}-\theta^{2})}\cos\theta t
$$ (eq:1-57)

对应齐次方程的通解已由式{eq}`eq:1-12`给定, 则微分方程{eq}`eq:1-56`的通解为

$$
x(t)=A_{1}\sin\omega t+A_{2}\cos\omega t+\frac{P}{m(\omega^{2}-\theta^{2})}\cos\theta t
$$ (eq:1-58)

由初始条件 $x(0)=x_{0}$ 及 $\dot{x}(0)=\dot{x}_{0}$, 不难求得

$$
A_{1}=\frac{\dot{x}_{0}}{\omega},\quad A_{2}=x_{0}-\frac{P}{m(\omega^{2}-\theta^{2})}
$$ (eq:1-59)

则式{eq}`eq:1-58`可写为

$$
x(t)=\frac{\dot{x}_{0}}{\omega}\sin\omega t+x_{0}\cos\omega t-\frac{P}{m(\omega^{2}-\theta^{2})}\cos\omega t+\frac{P}{m(\omega^{2}-\theta^{2})}\cos\theta t
$$ (eq:1-60)

在式{eq}`eq:1-60`中, 前 3 项是振动频率为 $\omega$ 的自由振动。其中前 2 项的系数决定于初始条件, 当初始位移及初始速度都等于零时, 此 2 项也就等于零, 通常称它们为决定于初始条件的自由振动。第 3 项则不管初始条件如何, 都将伴随干扰力的作用而产生, 故可称为伴生的自由振动。至于第 4 项, 则完全按照干扰力频率进行振动, 故称为纯强迫振动。式{eq}`eq:1-60`还可以写成

$$
x(t)=\frac{\dot{x}_{0}}{\omega}\sin\omega t+x_{0}\cos\omega t+\frac{P}{m(\omega^{2}-\theta^{2})}(\cos\theta t-\cos\omega t)
$$ (eq:1-61)

下面, 我们分以下几种情况对式{eq}`eq:1-60`或式{eq}`eq:1-61`进行讨论：

1. 当外部干扰力的频率小于系统的自振频率(即 $\theta<\omega$)时, 系统的运动如{numref}`fig:1-11`所示。

```{figure} figures/fig1-11.png
---
name: fig:1-11
width: 80%
---
$\theta<\omega$ 时的无阻尼强迫振动
```

2. 当外部干扰力的频率大于系统的自振频率(即 $\theta>\omega$)时, 系统的运动如{numref}`fig:1-12`所示。

```{figure} figures/fig1-12.png
---
name: fig:1-12
width: 80%
---
$\theta>\omega$ 时的无阻尼强迫振动
```

3. 当外部干扰力的频率接近系统的自振频率(即 $\omega-\theta$ 非常小, 但不等于 0)时, 可以观察到一种称为拍(Beating)的现象。如令 $x_{0}=\dot{x}_{0}=0$, $\omega-\theta=2\varepsilon$($\varepsilon$ 为一非常小的数), 此时, 式{eq}`eq:1-61`变为

$$
\begin{aligned}
x(t) &=\frac{P}{m(\omega^{2}-\theta^{2})}(\cos\theta t-\cos\omega t) \\
&=-\frac{2P}{m(\omega^{2}-\theta^{2})}\sin\frac{(\theta-\omega)t}{2}\sin\frac{(\theta+\omega)t}{2}
=\frac{P}{2m\varepsilon\theta}\sin\varepsilon t\sin\theta t
\end{aligned}
$$ (eq:1-62)

因为式{eq}`eq:1-62`最后一个等式中的 $\varepsilon$ 非常小, 所以函数 $\sin\varepsilon t$ 变化缓慢, 它的周期等于 $\frac{2\pi}{\varepsilon}$, 该值很大。因此, 式{eq}`eq:1-62`可以视为周期为 $\frac{2\pi}{\theta}$、可变振幅等于 $\frac{P}{2m\varepsilon\theta}\sin\varepsilon t$ 的振动, 这种振动按{numref}`fig:1-13`所示的规则形式增大和缩小。拍的周期等于 $\frac{\pi}{\varepsilon}$。当 $\omega$ 接近 $\theta$ (即 $\varepsilon\to0$)时, 拍的周期增大; 当 $\varepsilon=0$ 时, 拍的周期成为无穷大, 其增大是连续的, 这种现象将会在下面看到。

```{figure} figures/fig1-13.png
---
name: fig:1-13
width: 80%
---
拍现象
```

4. 当外部干扰力的频率等于系统的自振频率时, 由于式{eq}`eq:1-61`的右端第 3 项分母为零, 无法直接应用该式, 故采用 L'Hospital 法则, 即

$$
\begin{aligned}
x(t) &=\frac{\dot{x}_{0}}{\omega}\sin\omega t+x_{0}\cos\omega t
-\lim_{\theta\to\omega}\frac{2P\sin\frac{(\theta-\omega)t}{2}\sin\frac{(\theta+\omega)t}{2}}{m(\omega-\theta)(\omega+\theta)} \\
&=\frac{\dot{x}_{0}}{\omega}\sin\omega t+x_{0}\cos\omega t+\frac{Pt}{2m\omega}\sin\omega t
\end{aligned}
$$ (eq:1-63)

式{eq}`eq:1-63`右端第 3 项的系数含有时间 $t$, 因此, $x(t)$ 的幅值将随时间 $t$ 而线性地增长, 如{numref}`fig:1-14`所示。这种现象称为共振(Resonance)。在工程中出现这种共振现象是非常危险的, 必须尽量设法避免。

```{figure} figures/fig1-14.png
---
name: fig:1-14
width: 70%
---
共振
```

式{eq}`eq:1-60`是在未考虑阻尼的影响下导出的。实际上由于阻尼的存在, 自由振动部分将很快衰减掉。在运动的起始阶段, 自由振动和强迫振动同时存在, 这段时间内的振动称为**瞬态**(Transient)振动。当自由振动部分消失后, 只剩下纯强迫振动时, 就称为**稳态**(Steady State)振动。在简谐力作用下, 常常着重研究系统的稳态振动。

若以 $X_{\mathrm{dyn}}$ 表示稳态振动的动位移, 则

$$
X_{\mathrm{dyn}}=\frac{P}{m(\omega^{2}-\theta^{2})}
=\frac{P}{m\omega^{2}\left(1-\frac{\theta^{2}}{\omega^{2}}\right)}
=\frac{P}{k(1-r^{2})}
$$ (eq:1-64)

式中, $r=\frac{\theta}{\omega}$ 为干扰力频率与系统自振频率之比。上式还用到了关系式{eq}`eq:1-10`, 即 $\omega^{2}=\frac{k}{m}$。若以 $x_{\mathrm{st}}$ 表示由于干扰力的幅值 $P$ 所引起的静力位移, 则

$$
x_{\mathrm{st}}=\frac{P}{k}
$$ (eq:1-65)

因此式{eq}`eq:1-64`可写为

$$
X_{\mathrm{dyn}}=\frac{1}{1-r^{2}}x_{\mathrm{st}}
$$ (eq:1-66)

若定义**动力放大系数**(Dynamic Magnification Factor)为强迫振动的振幅 $|X_{\mathrm{dyn}}|$ 与干扰力最大值 $P$ 所引起的静力位移 $x_{\mathrm{st}}$ 之比, 并记为 $\beta$, 则 $\beta$ 可表示为

$$
\beta=\frac{|X_{\mathrm{dyn}}|}{x_{\mathrm{st}}}=\left|\frac{1}{1-r^{2}}\right|
$$ (eq:1-67)

$\beta$ 与 $r$ 的关系如{numref}`fig:1-15`所示。

```{figure} figures/fig1-15.png
---
name: fig:1-15
width: 70%
---
无阻尼系统的动力放大系数
```

由{numref}`fig:1-15`可见, 动力放大系数 $\beta$ 与 $r$ 有很大的关系。参照{numref}`fig:1-15`讨论以下 3 种情况：

1. 如果 $\theta\ll\omega$, 即 $r\approx0$, 则

$$
\beta\approx1,\quad X_{\mathrm{dyn}}\approx x_{\mathrm{st}}
$$

由此可见, 缓慢变化的简谐力的作用, 就产生的振幅大小而言, 接近于静力作用。

2. 如果 $\theta\gg\omega$, 即 $r\to\infty$, 则

$$
\beta\approx0,\quad X_{\mathrm{dyn}}\approx0
$$

可见, 当干扰力的频率远高于系统的自振频率时, 系统强迫振动的振幅非常微小。

3. 如果 $\theta\to\omega$, 即 $r\approx1$, 则

$$
\beta\to\infty,\quad X_{\mathrm{dyn}}\to\pm\infty
$$

即发生了共振。此时不管干扰力的大小如何, 动力振幅都会变到无穷大。事实当然不会如此。之所以得到这样的结论, 是因为没有考虑阻尼的缘故(此外, 当变形太大, 超过弹性极限后, 振动的线性性质也会改变)。阻尼作用在共振区域附近, 对于降低动力放大系数 $\beta$ 的值, 起着很大的作用。很小的阻尼就会使共振时的动力放大系数 $\beta$ 的值降低很多。所以对于共振现象来说, 不计阻尼作用的计算公式是没有实用意义的。但即便考虑了阻尼, 在共振区域附近, $\beta$ 值往往仍相当大(在后面的讨论中将会看到)。

我们还观察到, 当 $\theta<\omega$ 时, 动力放大系数 $\beta$ 的值总是不小于 1 的; 而当 $\theta>\omega$ 时, 只有当 $1<r\leq\sqrt{2}$ 时, $\beta$ 的值才是不小于 1 的; 当 $r>\sqrt{2}$ 时, $\beta$ 的值总小于 1。

```{figure} figures/fig1-16.png
---
name: fig:1-16
width: 70%
---
简支梁受发动机荷载
```

```{note}
**例1-5** 如{numref}`fig:1-16`所示一简支梁, 跨度 $L=4\ \mathrm{m}$。梁截面为矩形, 高 $h=20\ \mathrm{cm}$, 宽 $b=13.32\ \mathrm{cm}$。若在梁的中点放一发动机, 质量 $m$ 为 $3500\ \mathrm{kg}$, 比梁的自重大很多。今设发动机的转速为 $500\ \mathrm{r/min}$, 转动时产生离心力的竖直分力的力幅为 $P=19.6\ \mathrm{kN}$, 其变化按简谐规律。若不考虑自由振动的影响, 试求中点的最大挠度。

解 由例1-3, 可知系统的自振频率为

$$
\omega=\sqrt{\frac{48EJ}{mL^3}} \tag{1}
$$

其中

$$
E=2.058\times10^{11}\ \mathrm{N/m^2}
$$

$$
J=\frac{bh^3}{12}=\frac{13.32\times10^{-2}\times(20\times10^{-2})^3}{12}=8.88\times10^{-5}\ \mathrm{m^4}
$$

$$
m=3500\ \mathrm{kg},\quad L=4\ \mathrm{m}
$$

因此

$$
\omega=\sqrt{\frac{48\times2.058\times10^{11}\times8.88\times10^{-5}}{3500\times4^3}}=62.6\ \mathrm{s^{-1}} \tag{2}
$$

又离心力的频率

$$
\theta=\frac{2\pi n}{60}=\frac{2\pi\times500}{60}=52.3\ \mathrm{s^{-1}} \tag{3}
$$

由式\eqref{eq:1-67}可知动力放大系数为

$$
\beta=\left|\frac{1}{1-\frac{\theta^2}{\omega^2}}\right|=\frac{1}{1-\frac{52.3^2}{62.6^2}}\approx3.3 \tag{4}
$$

在梁中点的最大挠度, 应包括静力与动力荷载所引起的挠度总和, 即

$$
\begin{aligned}
x_{\max} &= x_{\mathrm{st}} + X_{\mathrm{dyn}} = \frac{mgL^3}{48EJ} + \beta\frac{PL^3}{48EJ} \cr
&= \frac{(3500\times9.81 + 3.3\times19600)\times4^3}{48\times2.058\times10^{11}\times8.88\times10^{-5}} = 0.00722\ \mathrm{m}
\end{aligned}
\tag{5}
$$
```

## 交互探索：无阻尼强迫振动

```{tip}
改变频率比 $r=\theta/\omega$，可以直接观察简谐载荷频率与系统自振频率的相对关系：
* 当 $r<1$ 时，系统响应以低频激励为主，位移与载荷大体同向变化。
* 当 $r\approx1$ 时，自由振动和强迫振动会持续交换能量，响应幅值不断增大，即共振。
* 当 $r>1$ 时，激励变化很快，系统位移响应变小，并逐渐表现出反相特征。
```

请点击或拖动下方滑动条，改变简谐激励频率与系统自振频率之比 $r=\theta/\omega$，观察弹簧振子动画与位移响应曲线如何随时间演化。

```{code-cell} python
:tags: [remove-input]

from IPython.display import display, HTML

html = r"""
<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>
<style>
  #undamped-forced-demo input[type="range"] {
    -webkit-appearance: none;
    appearance: none;
    width: 100%;
    height: 8px;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.34);
    outline: none;
  }
  #undamped-forced-demo input[type="range"]::-webkit-slider-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: #2563eb;
    border: 3px solid #ffffff;
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.30);
    cursor: pointer;
  }
  #undamped-forced-demo input[type="range"]::-moz-range-thumb {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: #2563eb;
    border: 3px solid #ffffff;
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.30);
    cursor: pointer;
  }
</style>

<div id="undamped-forced-demo" style="
    max-width: 1080px; margin: 1.4rem auto; padding: 1.1rem;
    border-radius: 22px; background: linear-gradient(135deg, #f8fafc 0%, #eef6ff 100%);
    box-shadow: 0 18px 42px rgba(15, 23, 42, 0.09);
    border: 1px solid rgba(148, 163, 184, 0.20);
    font-family: 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
">
  <div style="display:grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap:0.8rem; margin-bottom:0.95rem;">
    <div style="padding:0.8rem 0.95rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(37,99,235,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">频率比</div>
      <div id="uf-r-value" style="margin-top:0.25rem; font-size:20px; font-weight:800; color:#1d4ed8;">r = 0.70</div>
    </div>
    <div style="padding:0.8rem 0.95rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(37,99,235,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">激励圆频率</div>
      <div id="uf-theta-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">θ = 0.70ω</div>
    </div>
    <div style="padding:0.8rem 0.95rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(37,99,235,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">动力放大系数</div>
      <div id="uf-beta-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">β = 1.96</div>
    </div>
    <div style="padding:0.8rem 0.95rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(37,99,235,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">当前状态</div>
      <div id="uf-state-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">低于共振</div>
    </div>
  </div>

  <div style="padding:0.4rem 0.55rem 1rem;">
    <label for="uf-r-slider" style="display:flex; justify-content:space-between; gap:1rem; flex-wrap:wrap; font-size:14px; color:#334155; font-weight:700;">
      <span>调节频率比 r = θ / ω</span>
      <span style="font-weight:500; color:#64748b;">0.10 ~ 3.00</span>
    </label>
    <input id="uf-r-slider" type="range" min="0.10" max="3.00" step="0.01" value="0.70">
  </div>

  <div style="display:grid; grid-template-columns:minmax(360px, 1.45fr) minmax(260px, 0.75fr); gap:1rem; align-items:stretch;">
    <div style="padding:0.9rem; border-radius:18px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
      <svg id="uf-oscillator" viewBox="0 0 560 220" style="width:100%; height:250px; display:block; border-radius:14px; background:linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);">
        <rect x="0" y="0" width="560" height="220" rx="14" fill="transparent"></rect>
        <rect x="22" y="50" width="18" height="134" rx="5" fill="#94a3b8"></rect>
        <line x1="40" y1="120" x2="520" y2="120" stroke="rgba(148,163,184,0.32)" stroke-width="6" stroke-linecap="round"></line>
        <line x1="340" y1="32" x2="340" y2="188" stroke="rgba(249,115,22,0.45)" stroke-width="2" stroke-dasharray="6 5"></line>
        <polyline id="uf-spring" fill="none" stroke="#2563eb" stroke-width="4" stroke-linejoin="round" stroke-linecap="round"></polyline>
        <rect id="uf-mass" x="306" y="86" width="68" height="68" rx="12" fill="#60a5fa" stroke="#1d4ed8" stroke-width="3"></rect>
        <line id="uf-force-arrow" x1="392" y1="70" x2="450" y2="70" stroke="#ef4444" stroke-width="5" stroke-linecap="round"></line>
        <polygon id="uf-force-head" points="450,70 438,62 438,78" fill="#ef4444"></polygon>
        <text x="292" y="204" fill="#64748b" style="font-size:13px;">平衡位置</text>
        <text id="uf-force-label" x="392" y="54" fill="#ef4444" style="font-size:13px; font-weight:700;">F(t)</text>
      </svg>
    </div>
    <div style="display:grid; grid-template-columns:1fr; gap:0.75rem;">
      <div style="padding:0.85rem; border-radius:16px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
        <div style="font-size:12px; color:#64748b; font-weight:700;">无量纲时间</div>
        <div id="uf-time-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">τ = 0.00</div>
      </div>
      <div style="padding:0.85rem; border-radius:16px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
        <div style="font-size:12px; color:#64748b; font-weight:700;">归一化位移</div>
        <div id="uf-x-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">x / x<sub>st</sub> = 0.000</div>
      </div>
      <div style="display:flex; gap:0.65rem;">
        <button id="uf-play" style="flex:1; padding:0.85rem 0.7rem; border:none; border-radius:999px; background:#2563eb; color:white; font-weight:800; cursor:pointer;">播放</button>
        <button id="uf-reset" style="flex:1; padding:0.85rem 0.7rem; border:1px solid rgba(37,99,235,0.20); border-radius:999px; background:white; color:#0f172a; font-weight:800; cursor:pointer;">重播</button>
      </div>
    </div>
  </div>

  <div style="margin-top:1rem; padding:0.95rem; border-radius:18px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
    <div id="uf-plot" style="width:100%; height:390px;"></div>
  </div>

  <div style="margin-top:1rem; padding:0.95rem; border-radius:18px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
    <div style="display:flex; justify-content:space-between; align-items:center; gap:1rem; margin-bottom:0.55rem; flex-wrap:wrap;">
      <div style="font-size:14px; font-weight:800; color:#0f172a;">动力放大系数采样图</div>
      <button id="uf-beta-reset" style="padding:0.6rem 0.95rem; border:1px solid rgba(37,99,235,0.20); border-radius:999px; background:white; color:#0f172a; font-weight:800; cursor:pointer;">重置采样点</button>
    </div>
    <div id="uf-beta-plot" style="width:100%; height:360px;"></div>
  </div>
</div>

<script>
(function() {
  const container = document.getElementById("undamped-forced-demo");
  if (!container || container.dataset.initialized === "true") return;
  container.dataset.initialized = "true";

  const steps = 2000;
  const playbackDuration = 16000;
  const accent = "#2563eb";
  const equilibriumX = 340;
  const massWidth = 68;
  const baseMotionScale = 28;
  const maxVisualTravel = 120;

  const slider = container.querySelector("#uf-r-slider");
  const rValue = container.querySelector("#uf-r-value");
  const thetaValue = container.querySelector("#uf-theta-value");
  const betaValue = container.querySelector("#uf-beta-value");
  const stateValue = container.querySelector("#uf-state-value");
  const timeValue = container.querySelector("#uf-time-value");
  const xValue = container.querySelector("#uf-x-value");
  const playBtn = container.querySelector("#uf-play");
  const resetBtn = container.querySelector("#uf-reset");
  const betaResetBtn = container.querySelector("#uf-beta-reset");
  const plotEl = container.querySelector("#uf-plot");
  const betaPlotEl = container.querySelector("#uf-beta-plot");
  const springEl = container.querySelector("#uf-spring");
  const massEl = container.querySelector("#uf-mass");
  const arrowEl = container.querySelector("#uf-force-arrow");
  const headEl = container.querySelector("#uf-force-head");
  const forceLabel = container.querySelector("#uf-force-label");

  let r = Number(slider.value);
  let tauMax = getTauMax(r);
  let currentIndex = 0;
  let isPlaying = false;
  let startTimestamp = null;
  let rafId = null;
  let response = computeResponse(r);
  let betaSamples = [];

  function classify(value) {
    if (Math.abs(value - 1) < 0.04) return "接近共振";
    if (value < 1) return "低于共振";
    return "高于共振";
  }

  function getTauMax(freqRatio) {
    const forcePeriodWindow = 16 * 2 * Math.PI / freqRatio;
    return Math.max(45, Math.min(160, forcePeriodWindow));
  }

  function computeResponse(freqRatio) {
    const tau = [];
    const y = [];
    const force = [];
    const dt = tauMax / steps;
    let pos = 0;
    let vel = 0;

    function acc(time, x) {
      return Math.cos(freqRatio * time) - x;
    }

    for (let i = 0; i <= steps; i += 1) {
      const time = i * dt;
      tau.push(time);
      y.push(pos);
      force.push(Math.cos(freqRatio * time));

      const k1x = vel;
      const k1v = acc(time, pos);
      const k2x = vel + 0.5 * dt * k1v;
      const k2v = acc(time + 0.5 * dt, pos + 0.5 * dt * k1x);
      const k3x = vel + 0.5 * dt * k2v;
      const k3v = acc(time + 0.5 * dt, pos + 0.5 * dt * k2x);
      const k4x = vel + dt * k3v;
      const k4v = acc(time + dt, pos + dt * k3x);

      pos += (dt / 6) * (k1x + 2 * k2x + 2 * k3x + k4x);
      vel += (dt / 6) * (k1v + 2 * k2v + 2 * k3v + k4v);
    }
    const maxAbs = Math.max(0.001, ...y.map((value) => Math.abs(value)));
    const motionScale = Math.min(baseMotionScale, maxVisualTravel / maxAbs);
    return { tau, y, force, maxAbs, motionScale };
  }

  function buildSpringPoints(startX, endX, y) {
    const points = [];
    const straight = 18;
    const turns = 11;
    const amplitude = 18;
    points.push(`${startX},${y}`);
    points.push(`${startX + straight},${y}`);
    const usable = Math.max(endX - startX - straight * 2, 28);
    const step = usable / (turns * 2);
    for (let i = 0; i < turns * 2; i += 1) {
      const x = startX + straight + step * (i + 1);
      points.push(`${x},${y + (i % 2 === 0 ? -amplitude : amplitude)}`);
    }
    points.push(`${endX - straight},${y}`);
    points.push(`${endX},${y}`);
    return points.join(" ");
  }

  function buildPlot(index) {
    return [
      {
        x: response.tau,
        y: response.y,
        mode: "lines",
        name: "完整响应",
        line: { color: "rgba(15, 23, 42, 0.20)", width: 2 },
        hovertemplate: "τ = %{x:.2f}<br>x/xst = %{y:.3f}<extra></extra>",
      },
      {
        x: response.tau.slice(0, index + 1),
        y: response.y.slice(0, index + 1),
        mode: "lines",
        name: "已绘响应",
        line: { color: accent, width: 3.2 },
        hovertemplate: "τ = %{x:.2f}<br>x/xst = %{y:.3f}<extra></extra>",
      },
      {
        x: [response.tau[index]],
        y: [response.y[index]],
        mode: "markers",
        name: "当前点",
        marker: { size: 10, color: "#f97316", line: { color: "white", width: 2 } },
        hovertemplate: "τ = %{x:.2f}<br>x/xst = %{y:.3f}<extra></extra>",
      }
    ];
  }

  function buildLayout() {
    const yLimit = Math.max(1.4, response.maxAbs * 1.12);
    return {
      margin: { l: 68, r: 18, t: 18, b: 52 },
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff",
      xaxis: {
        title: "<b>无量纲时间 τ = ωt</b>",
        range: [0, tauMax],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: false,
        showline: true,
        linecolor: "rgba(15, 23, 42, 0.25)",
      },
      yaxis: {
        title: "<b>归一化位移 x / x<sub>st</sub></b>",
        range: [-yLimit, yLimit],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: true,
        zerolinecolor: "rgba(15, 23, 42, 0.22)",
        zerolinewidth: 1.5,
      },
      legend: { orientation: "h", y: 1.02, x: 0.98, xanchor: "right", yanchor: "bottom" },
      hoverlabel: { bgcolor: "#ffffff", bordercolor: "rgba(37,99,235,0.30)", font: { color: "#0f172a" } },
    };
  }

  function betaRaw(freqRatio) {
    const denom = 1 - freqRatio * freqRatio;
    return Math.abs(denom) < 1e-10 ? Infinity : Math.abs(1 / denom);
  }

  function betaLabel(value) {
    return Number.isFinite(value) ? value.toFixed(3) : "∞";
  }

  function buildBetaLayout() {
    const finiteValues = betaSamples.map((sample) => sample.beta).filter(Number.isFinite);
    const maxFinite = Math.max(5, ...finiteValues);
    const yMax = Math.min(Math.max(5, maxFinite * 1.12), 80);
    return {
      margin: { l: 68, r: 18, t: 14, b: 52 },
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff",
      xaxis: {
        title: "<b>频率比 r = θ / ω</b>",
        range: [0.05, 3.05],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: false,
        showline: true,
        linecolor: "rgba(15, 23, 42, 0.25)",
      },
      yaxis: {
        title: "<b>动力放大系数 β</b>",
        range: [0, yMax],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: true,
        zerolinecolor: "rgba(15, 23, 42, 0.18)",
      },
      showlegend: false,
      hoverlabel: { bgcolor: "#ffffff", bordercolor: "rgba(37,99,235,0.30)", font: { color: "#0f172a" } },
    };
  }

  function buildBetaData() {
    const finiteValues = betaSamples.map((sample) => sample.beta).filter(Number.isFinite);
    const maxFinite = Math.max(5, ...finiteValues);
    const cap = Math.min(Math.max(5, maxFinite * 1.05), 80);
    return [{
      x: betaSamples.map((sample) => sample.r),
      y: betaSamples.map((sample) => Number.isFinite(sample.beta) ? Math.min(sample.beta, cap) : cap),
      mode: "markers",
      type: "scatter",
      marker: {
        size: 10,
        color: betaSamples.map((sample) => Number.isFinite(sample.beta) ? "#2563eb" : "#ef4444"),
        line: { color: "white", width: 1.5 },
      },
      customdata: betaSamples.map((sample) => betaLabel(sample.beta)),
      hovertemplate: "r = %{x:.2f}<br>β = %{customdata}<extra></extra>",
    }];
  }

  function renderBetaPlot() {
    Plotly.react(betaPlotEl, buildBetaData(), buildBetaLayout(), {
      displayModeBar: false,
      responsive: true,
    });
  }

  function addBetaSample() {
    betaSamples.push({ r, beta: betaRaw(r) });
    renderBetaPlot();
  }

  function updateReadouts(index) {
    const tau = response.tau[index];
    const y = response.y[index];
    const force = response.force[index];
    const beta = betaRaw(r);
    rValue.textContent = `r = ${r.toFixed(2)}`;
    thetaValue.textContent = `θ = ${r.toFixed(2)}ω`;
    betaValue.textContent = `β = ${Number.isFinite(beta) ? beta.toFixed(2) : "∞"}`;
    stateValue.textContent = classify(r);
    timeValue.textContent = `τ = ${tau.toFixed(2)}`;
    xValue.textContent = `x / xst = ${y.toFixed(3)}`;

    const massCenterX = equilibriumX + y * response.motionScale;
    const massLeft = massCenterX - massWidth / 2;
    massEl.setAttribute("x", massLeft.toFixed(2));
    springEl.setAttribute("points", buildSpringPoints(40, massLeft + 2, 120));

    const arrowStart = massLeft + massWidth + 18;
    const arrowLength = 48 * Math.abs(force);
    const arrowEnd = arrowStart + (force >= 0 ? arrowLength : -arrowLength);
    arrowEl.setAttribute("x1", arrowStart.toFixed(2));
    arrowEl.setAttribute("x2", arrowEnd.toFixed(2));
    if (force >= 0) {
      headEl.setAttribute("points", `${arrowEnd},70 ${arrowEnd - 12},62 ${arrowEnd - 12},78`);
    } else {
      headEl.setAttribute("points", `${arrowEnd},70 ${arrowEnd + 12},62 ${arrowEnd + 12},78`);
    }
    forceLabel.setAttribute("x", Math.min(500, Math.max(48, arrowStart)).toFixed(2));
  }

  function render(index, fullRedraw = false) {
    currentIndex = index;
    updateReadouts(index);
    if (fullRedraw) {
      Plotly.react(plotEl, buildPlot(index), buildLayout(), { displayModeBar: false, responsive: true });
    } else {
      Plotly.restyle(plotEl, { x: [response.tau.slice(0, index + 1)], y: [response.y.slice(0, index + 1)] }, [1]);
      Plotly.restyle(plotEl, { x: [[response.tau[index]]], y: [[response.y[index]]] }, [2]);
    }
  }

  function stop() {
    isPlaying = false;
    startTimestamp = null;
    if (rafId) cancelAnimationFrame(rafId);
    rafId = null;
    playBtn.textContent = "播放";
  }

  function tick(timestamp) {
    if (!isPlaying) return;
    if (startTimestamp === null) {
      startTimestamp = timestamp - (currentIndex / steps) * playbackDuration;
    }
    const progress = Math.min((timestamp - startTimestamp) / playbackDuration, 1);
    const nextIndex = Math.min(Math.floor(progress * steps), steps);
    if (nextIndex !== currentIndex) render(nextIndex, false);
    if (progress >= 1) {
      stop();
      return;
    }
    rafId = requestAnimationFrame(tick);
  }

  function play(fromStart = false) {
    if (fromStart) render(0, true);
    isPlaying = true;
    startTimestamp = null;
    playBtn.textContent = "暂停";
    rafId = requestAnimationFrame(tick);
  }

  slider.addEventListener("input", (event) => {
    r = Number(event.target.value);
    tauMax = getTauMax(r);
    response = computeResponse(r);
    stop();
    render(0, true);
    addBetaSample();
    play(false);
  });
  playBtn.addEventListener("click", () => {
    if (isPlaying) stop();
    else play(currentIndex >= steps);
  });
  resetBtn.addEventListener("click", () => {
    stop();
    play(true);
  });

  betaResetBtn.addEventListener("click", () => {
    betaSamples = [];
    renderBetaPlot();
  });

  render(0, true);
  addBetaSample();
  play(false);
})();
</script>
"""

display(HTML(html))
```



## 有阻尼强迫振动

单自由度系统考虑阻尼作用的运动方程为式{eq}`eq:1-3`的形式, 即

$$
m\ddot{x}(t)+c\dot{x}(t)+kx(t)=F_{\mathrm{e}}(t)
$$ (eq:1-68a)

或

$$
\ddot{x}(t)+2\varepsilon\dot{x}(t)+\omega^{2}x(t)=\frac{1}{m}F_{\mathrm{e}}(t)
$$ (eq:1-68b)

$$
\ddot{x}(t)+2\zeta\omega\dot{x}(t)+\omega^{2}x(t)=\frac{1}{m}F_{\mathrm{e}}(t)
$$ (eq:1-68c)

方程{eq}`eq:1-68a`的通解仍是对应齐次方程的通解和非齐次方程的特解之和。前者与式{eq}`eq:1-36`或式{eq}`eq:1-39`完全相同。假定干扰力 $F_{\mathrm{e}}(t)=P\sin\theta t$, 则考虑阻尼作用的单自由度系统运动方程为[以方程{eq}`eq:1-68b`为例]

$$
\ddot{x}+2\varepsilon\dot{x}+\omega^{2}x=\frac{P}{m}\sin\theta t
$$ (eq:1-69)

用待定系数法求该微分方程的特解, 令

$$
x=A_{1}\cos\theta t+A_{2}\sin\theta t
$$ (eq:1-70)

则

$$
\dot{x}=-A_{1}\theta\sin\theta t+A_{2}\theta\cos\theta t,\quad
\ddot{x}=-A_{1}\theta^{2}\cos\theta t-A_{2}\theta^{2}\sin\theta t
$$

将 $x$、$\dot{x}$ 和 $\ddot{x}$ 的表达式代入方程{eq}`eq:1-69`, 得

$$
\begin{aligned}
-A_{1}\theta^{2}\cos\theta t &-A_{2}\theta^{2}\sin\theta t
+2\varepsilon(-A_{1}\theta\sin\theta t+A_{2}\theta\cos\theta t) \\
&+\omega^{2}(A_{1}\cos\theta t+A_{2}\sin\theta t)
=\frac{P}{m}\sin\theta t
\end{aligned}
$$

分别令等号两边 $\cos\theta t$ 和 $\sin\theta t$ 的系数相等, 可得

$$
-A_{1}\theta^{2}+2\varepsilon A_{2}\theta+\omega^{2}A_{1}=0,\quad
-A_{2}\theta^{2}-2\varepsilon A_{1}\theta+\omega^{2}A_{2}=\frac{P}{m}
$$

由此可解出 $A_{1}$ 和 $A_{2}$ 分别为

$$
A_{1}=\frac{-2P\varepsilon\theta}{m\left[(\theta^{2}-\omega^{2})^{2}+4\varepsilon^{2}\theta^{2}\right]},\quad
A_{2}=\frac{P(\omega^{2}-\theta^{2})}{m\left[(\theta^{2}-\omega^{2})^{2}+4\varepsilon^{2}\theta^{2}\right]}
$$ (eq:1-71)

若将特解{eq}`eq:1-70`改为

$$
x(t)=A\sin(\theta t-\phi)
$$ (eq:1-72)

则

$$
A=\sqrt{A_{1}^{2}+A_{2}^{2}}=\frac{P}{m\sqrt{(\theta^{2}-\omega^{2})^{2}+4\varepsilon^{2}\theta^{2}}}
$$ (eq:1-73)

$$
\tan\phi=\frac{2\varepsilon\theta}{\omega^{2}-\theta^{2}}
$$ (eq:1-74)

由以上推导, 可知方程{eq}`eq:1-69`的通解为

$$
x=e^{-\varepsilon t}\left(B_{1}\sin\omega_{\varepsilon}t+B_{2}\cos\omega_{\varepsilon}t\right)+A\sin(\theta t-\phi)
$$ (eq:1-75a)

或

$$
x=e^{-\varepsilon t}\left(B_{1}\sin\omega_{\varepsilon}t+B_{2}\cos\omega_{\varepsilon}t\right)+A_{1}\cos\theta t+A_{2}\sin\theta t
$$ (eq:1-75b)

将初始条件 $x(0)=x_{0}$ 与 $\dot{x}(0)=\dot{x}_{0}$ 代入式{eq}`eq:1-75a`, 即可解出 $B_{1}$ 与 $B_{2}$。则

$$
\begin{aligned}
x(t)=&e^{-\varepsilon t}\left(\frac{\dot{x}_{0}+\varepsilon x_{0}}{\omega_{\varepsilon}}\sin\omega_{\varepsilon}t+x_{0}\cos\omega_{\varepsilon}t\right)+ \\
&Ae^{-\varepsilon t}\left(\sin\phi\cos\omega_{\varepsilon}t+\frac{\varepsilon\sin\phi-\theta\cos\phi}{\omega_{\varepsilon}}\sin\omega_{\varepsilon}t\right)+A\sin(\theta t-\phi)
\end{aligned}
$$ (eq:1-76)

上式的第 1 项为决定于初始条件的自由振动, 第 2 项表示伴生的自由振动。这两项皆因有阻尼的存在而逐渐衰减。第 3 项是纯强迫振动, 其振幅与周期都不随时间而变, 是稳定的周期运动。式{eq}`eq:1-76`所表示的系统的运动如{numref}`fig:1-17`所示。

```{figure} figures/fig1-17.png
---
name: fig:1-17
width: 80%
---
有阻尼强迫振动
```

也可将初始条件 $x(0)=x_{0}$ 与 $\dot{x}(0)=\dot{x}_{0}$ 代入式\eqref{eq:1-75b}, 从而得到式{eq}`eq:1-76`的另一种形式

$$
\begin{aligned}
x(t)=&e^{-\varepsilon t}\left[\frac{\dot{x}_{0}+\varepsilon(x_{0}-A_{1})}{\omega_{\varepsilon}}\sin\omega_{\varepsilon}t+x_{0}\cos\omega_{\varepsilon}t\right]- \\
&e^{-\varepsilon t}\left(\frac{A_{2}\theta}{\omega_{\varepsilon}}\sin\omega_{\varepsilon}t+A_{1}\cos\omega_{\varepsilon}t\right)+A_{1}\cos\theta t+A_{2}\sin\theta t
\end{aligned}
$$ (eq:1-77)

由式{eq}`eq:1-76`知, 纯强迫振动的振幅

$$
|X_{\mathrm{dyn}}|=A=\frac{P}{m\sqrt{(\theta^{2}-\omega^{2})^{2}+4\varepsilon^{2}\theta^{2}}}
=\frac{1}{\sqrt{(1-r^{2})^{2}+4\zeta^{2}r^{2}}}x_{\mathrm{st}}
$$ (eq:1-78)

式中, $r=\frac{\theta}{\omega}$ 为干扰力频率与系统自振频率之比。上式还用到了关系式{eq}`eq:1-10`, 即 $\omega^{2}=\frac{k}{m}$ 以及关系式 $x_{\mathrm{st}}=\frac{P}{k}$。则动力放大系数

$$
\beta=\frac{|X_{\mathrm{dyn}}|}{x_{\mathrm{st}}}=\frac{1}{\sqrt{(1-r^{2})^{2}+4\zeta^{2}r^{2}}}
$$ (eq:1-79)

```{note}
**例1-6** 在例1-5中, 若阻尼特性系数 $\varepsilon=0.8\ \mathrm{s^{-1}}$, 试求其有阻尼自振频率及动力放大系数。又若发动机的转动频率改为 $550\ \mathrm{r/min}$ 及 $596\ \mathrm{r/min}$ 时, 求其动力放大系数。

解 在例1-5中已求出系统的自振频率 $\omega=62.6\ \mathrm{s^{-1}}$, 因此, 系统的阻尼比为

$$
\zeta=\frac{\varepsilon}{\omega}=\frac{0.8}{62.6}=0.0128 \tag{1}
$$

考虑阻尼的自振频率为

$$
\omega_{\varepsilon}=\omega\sqrt{1-\zeta^{2}}=62.6\sqrt{1-0.0128^{2}}=62.495\ \mathrm{s^{-1}} \tag{2}
$$

可见, 当考虑了阻尼($\zeta=0.0128$)之后, 自振频率仅降低了 $0.01\%$。干扰力频率与系统自振频率之比

$$
r=\frac{\theta}{\omega}=\frac{52.3}{62.6}=0.8368 \tag{3}
$$

由式{eq}`eq:1-79`可得动力放大系数

$$
\beta=\frac{1}{\sqrt{(1-0.8368^{2})^{2}+4\times0.0128^{2}\times0.8368^{2}}}\approx3.33 \tag{4}
$$

若发动机的转动频率为 $550\ \mathrm{r/min}$, 则

$$
\theta=57.6\ \mathrm{s^{-1}},\quad r=\frac{\theta}{\omega}=\frac{57.6}{62.6}=0.9216 \tag{5}
$$

所以

$$
\beta=\frac{1}{\sqrt{(1-0.9216^{2})^{2}+4\times0.0128^{2}\times0.9216^{2}}}\approx6.56 \tag{6}
$$

若发动机的转动频率为 $596\ \mathrm{r/min}$, 则 $\theta=62.41\ \mathrm{s^{-1}}$, 同样可算得

$$
\beta\approx38.87 \tag{7}
$$
```

考虑阻尼时, 动力放大系数 $\beta$ 与 $r=\frac{\theta}{\omega}$ 的关系如{numref}`fig:1-18`所示。

```{figure} figures/fig1-18.png
---
name: fig:1-18
width: 80%
---
考虑阻尼时的动力放大系数
```

由{numref}`fig:1-18`可见, 在共振区附近, 阻尼对于降低动力放大系数或强迫振动振幅之值起着很大的作用。阻尼 $\zeta$ 之值略为增加, 就可以使动力放大系数 $\beta$ 之值降低很多; 但在远离共振区的地方, 阻尼的作用就很小了。

## 交互探索：有阻尼强迫振动

```{tip}
在有阻尼系统中，改变频率比 $r=\theta/\omega$ 时，共振峰不再无限增大；阻尼会耗散能量，使瞬态响应逐渐消失，系统最终趋向稳定的简谐强迫振动。
```

请点击或拖动下方滑动条，改变简谐激励频率与系统自振频率之比 $r=\theta/\omega$。动画和响应曲线会实时更新，可观察瞬态衰减、稳态响应以及共振附近幅值放大的过程。

```{code-cell} python
:tags: [remove-input]

from IPython.display import display, HTML

html = r"""
<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>
<style>
  #damped-forced-demo input[type="range"] {
    -webkit-appearance: none;
    appearance: none;
    width: 100%;
    height: 8px;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.34);
    outline: none;
  }
  #damped-forced-demo input[type="range"]::-webkit-slider-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: #0f766e;
    border: 3px solid #ffffff;
    box-shadow: 0 4px 12px rgba(15, 118, 110, 0.30);
    cursor: pointer;
  }
  #damped-forced-demo input[type="range"]::-moz-range-thumb {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: #0f766e;
    border: 3px solid #ffffff;
    box-shadow: 0 4px 12px rgba(15, 118, 110, 0.30);
    cursor: pointer;
  }
</style>

<div id="damped-forced-demo" style="
    max-width: 1080px; margin: 1.4rem auto; padding: 1.1rem;
    border-radius: 22px; background: linear-gradient(135deg, #f7fffd 0%, #eef7ff 100%);
    box-shadow: 0 18px 42px rgba(15, 23, 42, 0.09);
    border: 1px solid rgba(148, 163, 184, 0.20);
    font-family: 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
">
  <div style="display:grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap:0.75rem; margin-bottom:0.95rem;">
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(15,118,110,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">阻尼比</div>
      <div id="df-zeta-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f766e;">ζ = 0.10</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(15,118,110,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">频率比</div>
      <div id="df-r-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f766e;">r = 0.70</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(15,118,110,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">动力放大系数</div>
      <div id="df-beta-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">β = 1.87</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(15,118,110,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">相位滞后</div>
      <div id="df-phase-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">φ = 15.5°</div>
    </div>
    <div style="padding:0.78rem 0.86rem; border-radius:14px; background:rgba(255,255,255,0.86); border:1px solid rgba(15,118,110,0.12);">
      <div style="font-size:12px; color:#64748b; font-weight:700;">当前状态</div>
      <div id="df-state-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">低于共振</div>
    </div>
  </div>

  <div style="display:grid; grid-template-columns:repeat(2, minmax(0, 1fr)); gap:1.1rem; padding:0.4rem 0.55rem 1rem;">
    <div>
      <label for="df-zeta-slider" style="display:flex; justify-content:space-between; gap:1rem; flex-wrap:wrap; font-size:14px; color:#334155; font-weight:700;">
        <span>调节阻尼比 ζ</span>
        <span style="font-weight:500; color:#64748b;">0.10 ~ 1.00</span>
      </label>
      <input id="df-zeta-slider" type="range" min="0.10" max="1.00" step="0.10" value="0.10">
    </div>
    <div>
      <label for="df-r-slider" style="display:flex; justify-content:space-between; gap:1rem; flex-wrap:wrap; font-size:14px; color:#334155; font-weight:700;">
        <span>调节频率比 r = θ / ω</span>
        <span style="font-weight:500; color:#64748b;">0.10 ~ 3.00</span>
      </label>
      <input id="df-r-slider" type="range" min="0.10" max="3.00" step="0.01" value="0.70">
    </div>
  </div>

  <div style="display:grid; grid-template-columns:minmax(360px, 1.45fr) minmax(260px, 0.75fr); gap:1rem; align-items:stretch;">
    <div style="padding:0.9rem; border-radius:18px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
      <svg id="df-oscillator" viewBox="0 0 560 220" style="width:100%; height:250px; display:block; border-radius:14px; background:linear-gradient(180deg, #ffffff 0%, #f8fffd 100%);">
        <rect x="0" y="0" width="560" height="220" rx="14" fill="transparent"></rect>
        <rect x="22" y="38" width="18" height="140" rx="5" fill="#94a3b8"></rect>
        <line x1="40" y1="96" x2="55" y2="96" stroke="rgba(15,23,42,0.22)" stroke-width="4" stroke-linecap="round"></line>
        <line x1="55" y1="84" x2="290" y2="84" stroke="#94a3b8" stroke-width="3" stroke-linecap="round"></line>
        <line x1="55" y1="108" x2="290" y2="108" stroke="#94a3b8" stroke-width="3" stroke-linecap="round"></line>
        <line x1="55" y1="84" x2="55" y2="108" stroke="#94a3b8" stroke-width="3" stroke-linecap="round"></line>
        <line id="df-damper-piston" x1="104" y1="86" x2="104" y2="106" stroke="#64748b" stroke-width="4" stroke-linecap="round"></line>
        <line id="df-damper-rod" x1="104" y1="96" x2="178" y2="96" stroke="#64748b" stroke-width="4" stroke-linecap="round"></line>
        <line x1="40" y1="142" x2="520" y2="142" stroke="rgba(148,163,184,0.32)" stroke-width="6" stroke-linecap="round"></line>
        <line x1="340" y1="32" x2="340" y2="188" stroke="rgba(249,115,22,0.42)" stroke-width="2" stroke-dasharray="6 5"></line>
        <polyline id="df-spring" fill="none" stroke="#0f766e" stroke-width="4" stroke-linejoin="round" stroke-linecap="round"></polyline>
        <rect id="df-mass" x="306" y="83" width="68" height="68" rx="12" fill="#2dd4bf" stroke="#0f766e" stroke-width="3"></rect>
        <line id="df-force-arrow" x1="392" y1="70" x2="450" y2="70" stroke="#ef4444" stroke-width="5" stroke-linecap="round"></line>
        <polygon id="df-force-head" points="450,70 438,62 438,78" fill="#ef4444"></polygon>
        <text x="292" y="204" fill="#64748b" style="font-size:13px;">平衡位置</text>
        <text id="df-force-label" x="392" y="54" fill="#ef4444" style="font-size:13px; font-weight:700;">F(t)</text>
      </svg>
    </div>
    <div style="display:grid; grid-template-columns:1fr; gap:0.75rem;">
      <div style="padding:0.85rem; border-radius:16px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
        <div style="font-size:12px; color:#64748b; font-weight:700;">无量纲时间</div>
        <div id="df-time-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">τ = 0.00</div>
      </div>
      <div style="padding:0.85rem; border-radius:16px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
        <div style="font-size:12px; color:#64748b; font-weight:700;">归一化位移</div>
        <div id="df-x-value" style="margin-top:0.25rem; font-size:18px; font-weight:800; color:#0f172a;">x / x<sub>st</sub> = 0.000</div>
      </div>
      <div style="display:flex; gap:0.65rem;">
        <button id="df-play" style="flex:1; padding:0.85rem 0.7rem; border:none; border-radius:999px; background:#0f766e; color:white; font-weight:800; cursor:pointer;">播放</button>
        <button id="df-reset" style="flex:1; padding:0.85rem 0.7rem; border:1px solid rgba(15,118,110,0.20); border-radius:999px; background:white; color:#0f172a; font-weight:800; cursor:pointer;">重播</button>
      </div>
    </div>
  </div>

  <div style="margin-top:1rem; padding:0.95rem; border-radius:18px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
    <div id="df-plot" style="width:100%; height:390px;"></div>
  </div>

  <div style="margin-top:1rem; padding:0.95rem; border-radius:18px; background:#ffffff; border:1px solid rgba(148,163,184,0.18);">
    <div style="display:flex; justify-content:space-between; align-items:center; gap:1rem; margin-bottom:0.55rem; flex-wrap:wrap;">
      <div style="font-size:14px; font-weight:800; color:#0f172a;">动力放大系数采样图</div>
      <button id="df-beta-reset" style="padding:0.6rem 0.95rem; border:1px solid rgba(15,118,110,0.20); border-radius:999px; background:white; color:#0f172a; font-weight:800; cursor:pointer;">重置采样点</button>
    </div>
    <div id="df-beta-plot" style="width:100%; height:360px;"></div>
  </div>
</div>

<script>
(function() {
  const container = document.getElementById("damped-forced-demo");
  if (!container || container.dataset.initialized === "true") return;
  container.dataset.initialized = "true";

  const steps = 2000;
  const playbackDuration = 16000;
  const accent = "#0f766e";
  const equilibriumX = 340;
  const massWidth = 68;
  const baseMotionScale = 30;
  const maxVisualTravel = 120;
  const damperRodLength = 132;

  const rSlider = container.querySelector("#df-r-slider");
  const zetaSlider = container.querySelector("#df-zeta-slider");
  const zetaValue = container.querySelector("#df-zeta-value");
  const rValue = container.querySelector("#df-r-value");
  const betaValue = container.querySelector("#df-beta-value");
  const phaseValue = container.querySelector("#df-phase-value");
  const stateValue = container.querySelector("#df-state-value");
  const timeValue = container.querySelector("#df-time-value");
  const xValue = container.querySelector("#df-x-value");
  const playBtn = container.querySelector("#df-play");
  const resetBtn = container.querySelector("#df-reset");
  const betaResetBtn = container.querySelector("#df-beta-reset");
  const plotEl = container.querySelector("#df-plot");
  const betaPlotEl = container.querySelector("#df-beta-plot");
  const springEl = container.querySelector("#df-spring");
  const massEl = container.querySelector("#df-mass");
  const pistonEl = container.querySelector("#df-damper-piston");
  const rodEl = container.querySelector("#df-damper-rod");
  const arrowEl = container.querySelector("#df-force-arrow");
  const headEl = container.querySelector("#df-force-head");
  const forceLabel = container.querySelector("#df-force-label");

  let r = Number(rSlider.value);
  let zeta = Number(zetaSlider.value);
  let tauMax = getTauMax(r, zeta);
  let currentIndex = 0;
  let isPlaying = false;
  let startTimestamp = null;
  let rafId = null;
  let response = computeResponse(r);
  let betaSamples = [];

  function classify(value) {
    const peak = Math.sqrt(Math.max(0, 1 - 2 * zeta * zeta));
    if (Math.abs(value - peak) < 0.06) return "共振附近";
    if (value < peak) return "低于共振";
    return "高于共振";
  }

  function beta(freqRatio) {
    return 1 / Math.sqrt((1 - freqRatio * freqRatio) ** 2 + (2 * zeta * freqRatio) ** 2);
  }

  function phase(freqRatio) {
    let angle = Math.atan2(2 * zeta * freqRatio, 1 - freqRatio * freqRatio);
    if (angle < 0) angle += Math.PI;
    return angle * 180 / Math.PI;
  }

  function getTauMax(freqRatio, dampingRatio) {
    const forcePeriodWindow = 16 * 2 * Math.PI / freqRatio;
    const decayWindow = 5 / dampingRatio;
    return Math.max(45, Math.min(160, Math.max(forcePeriodWindow, decayWindow)));
  }

  function computeResponse(freqRatio) {
    const tau = [];
    const y = [];
    const force = [];
    const dt = tauMax / steps;
    let pos = 0;
    let vel = 0;

    function acc(time, x, v) {
      return Math.cos(freqRatio * time) - 2 * zeta * v - x;
    }

    for (let i = 0; i <= steps; i += 1) {
      const time = i * dt;
      tau.push(time);
      y.push(pos);
      force.push(Math.cos(freqRatio * time));

      const k1x = vel;
      const k1v = acc(time, pos, vel);
      const k2x = vel + 0.5 * dt * k1v;
      const k2v = acc(time + 0.5 * dt, pos + 0.5 * dt * k1x, vel + 0.5 * dt * k1v);
      const k3x = vel + 0.5 * dt * k2v;
      const k3v = acc(time + 0.5 * dt, pos + 0.5 * dt * k2x, vel + 0.5 * dt * k2v);
      const k4x = vel + dt * k3v;
      const k4v = acc(time + dt, pos + dt * k3x, vel + dt * k3v);

      pos += (dt / 6) * (k1x + 2 * k2x + 2 * k3x + k4x);
      vel += (dt / 6) * (k1v + 2 * k2v + 2 * k3v + k4v);
    }
    const maxAbs = Math.max(0.001, ...y.map((value) => Math.abs(value)));
    const motionScale = Math.min(baseMotionScale, maxVisualTravel / maxAbs);
    return { tau, y, force, maxAbs, motionScale };
  }

  function buildSpringPoints(startX, endX, y) {
    const points = [];
    const straight = 18;
    const turns = 10;
    const amplitude = 16;
    points.push(`${startX},${y}`);
    points.push(`${startX + straight},${y}`);
    const usable = Math.max(endX - startX - straight * 2, 28);
    const step = usable / (turns * 2);
    for (let i = 0; i < turns * 2; i += 1) {
      const x = startX + straight + step * (i + 1);
      points.push(`${x},${y + (i % 2 === 0 ? -amplitude : amplitude)}`);
    }
    points.push(`${endX - straight},${y}`);
    points.push(`${endX},${y}`);
    return points.join(" ");
  }

  function buildPlot(index) {
    return [
      {
        x: response.tau,
        y: response.y,
        mode: "lines",
        name: "完整响应",
        line: { color: "rgba(15, 23, 42, 0.20)", width: 2 },
        hovertemplate: "τ = %{x:.2f}<br>x/xst = %{y:.3f}<extra></extra>",
      },
      {
        x: response.tau.slice(0, index + 1),
        y: response.y.slice(0, index + 1),
        mode: "lines",
        name: "已绘响应",
        line: { color: accent, width: 3.2 },
        hovertemplate: "τ = %{x:.2f}<br>x/xst = %{y:.3f}<extra></extra>",
      },
      {
        x: [response.tau[index]],
        y: [response.y[index]],
        mode: "markers",
        name: "当前点",
        marker: { size: 10, color: "#f97316", line: { color: "white", width: 2 } },
        hovertemplate: "τ = %{x:.2f}<br>x/xst = %{y:.3f}<extra></extra>",
      }
    ];
  }

  function buildLayout() {
    const yLimit = Math.max(1.4, response.maxAbs * 1.12);
    return {
      margin: { l: 68, r: 18, t: 18, b: 52 },
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff",
      xaxis: {
        title: "<b>无量纲时间 τ = ωt</b>",
        range: [0, tauMax],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: false,
        showline: true,
        linecolor: "rgba(15, 23, 42, 0.25)",
      },
      yaxis: {
        title: "<b>归一化位移 x / x<sub>st</sub></b>",
        range: [-yLimit, yLimit],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: true,
        zerolinecolor: "rgba(15, 23, 42, 0.22)",
        zerolinewidth: 1.5,
      },
      legend: { orientation: "h", y: 1.02, x: 0.98, xanchor: "right", yanchor: "bottom" },
      hoverlabel: { bgcolor: "#ffffff", bordercolor: "rgba(15,118,110,0.30)", font: { color: "#0f172a" } },
    };
  }

  function zetaColor(value) {
    const minZeta = Number(zetaSlider.min);
    const maxZeta = Number(zetaSlider.max);
    const ratio = (value - minZeta) / (maxZeta - minZeta);
    const hue = 210 - ratio * 165;
    return `hsl(${hue.toFixed(0)}, 72%, 45%)`;
  }

  function buildBetaLayout() {
    const maxBeta = Math.max(5, ...betaSamples.map((sample) => sample.beta));
    return {
      margin: { l: 68, r: 18, t: 14, b: 52 },
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff",
      xaxis: {
        title: "<b>频率比 r = θ / ω</b>",
        range: [0.05, 3.05],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: false,
        showline: true,
        linecolor: "rgba(15, 23, 42, 0.25)",
      },
      yaxis: {
        title: "<b>动力放大系数 β</b>",
        range: [0, maxBeta * 1.12],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: true,
        zerolinecolor: "rgba(15, 23, 42, 0.18)",
      },
      legend: {
        orientation: "h",
        y: 1.02,
        x: 0.98,
        xanchor: "right",
        yanchor: "bottom",
        bgcolor: "rgba(255,255,255,0.72)",
      },
      hoverlabel: { bgcolor: "#ffffff", bordercolor: "rgba(15,118,110,0.30)", font: { color: "#0f172a" } },
    };
  }

  function buildBetaData() {
    const groups = new Map();
    betaSamples.forEach((sample) => {
      const key = sample.zeta.toFixed(2);
      if (!groups.has(key)) groups.set(key, []);
      groups.get(key).push(sample);
    });

    return Array.from(groups.entries())
      .sort((a, b) => Number(a[0]) - Number(b[0]))
      .map(([key, samples]) => ({
        x: samples.map((sample) => sample.r),
        y: samples.map((sample) => sample.beta),
        mode: "markers",
        type: "scatter",
        name: `ζ = ${key}`,
        marker: {
          size: 10,
          color: zetaColor(Number(key)),
          line: { color: "white", width: 1.5 },
        },
        customdata: samples.map((sample) => sample.zeta),
        hovertemplate: "r = %{x:.2f}<br>β = %{y:.3f}<br>ζ = %{customdata:.2f}<extra></extra>",
      }));
  }

  function renderBetaPlot() {
    Plotly.react(betaPlotEl, buildBetaData(), buildBetaLayout(), {
      displayModeBar: false,
      responsive: true,
    });
  }

  function addBetaSample() {
    betaSamples.push({ r, zeta, beta: beta(r) });
    renderBetaPlot();
  }

  function updateReadouts(index) {
    const tau = response.tau[index];
    const y = response.y[index];
    const force = response.force[index];
    zetaValue.textContent = `ζ = ${zeta.toFixed(2)}`;
    rValue.textContent = `r = ${r.toFixed(2)}`;
    betaValue.textContent = `β = ${beta(r).toFixed(2)}`;
    phaseValue.textContent = `φ = ${phase(r).toFixed(1)}°`;
    stateValue.textContent = classify(r);
    timeValue.textContent = `τ = ${tau.toFixed(2)}`;
    xValue.textContent = `x / xst = ${y.toFixed(3)}`;

    const massCenterX = equilibriumX + y * response.motionScale;
    const massLeft = massCenterX - massWidth / 2;
    const pistonX = massLeft - damperRodLength;
    massEl.setAttribute("x", massLeft.toFixed(2));
    springEl.setAttribute("points", buildSpringPoints(40, massLeft + 2, 142));
    pistonEl.setAttribute("x1", pistonX.toFixed(2));
    pistonEl.setAttribute("x2", pistonX.toFixed(2));
    rodEl.setAttribute("x1", pistonX.toFixed(2));
    rodEl.setAttribute("x2", massLeft.toFixed(2));

    const arrowStart = massLeft + massWidth + 18;
    const arrowLength = 48 * Math.abs(force);
    const arrowEnd = arrowStart + (force >= 0 ? arrowLength : -arrowLength);
    arrowEl.setAttribute("x1", arrowStart.toFixed(2));
    arrowEl.setAttribute("x2", arrowEnd.toFixed(2));
    if (force >= 0) {
      headEl.setAttribute("points", `${arrowEnd},70 ${arrowEnd - 12},62 ${arrowEnd - 12},78`);
    } else {
      headEl.setAttribute("points", `${arrowEnd},70 ${arrowEnd + 12},62 ${arrowEnd + 12},78`);
    }
    forceLabel.setAttribute("x", Math.min(500, Math.max(48, arrowStart)).toFixed(2));
  }

  function render(index, fullRedraw = false) {
    currentIndex = index;
    updateReadouts(index);
    if (fullRedraw) {
      Plotly.react(plotEl, buildPlot(index), buildLayout(), { displayModeBar: false, responsive: true });
    } else {
      Plotly.restyle(plotEl, { x: [response.tau.slice(0, index + 1)], y: [response.y.slice(0, index + 1)] }, [1]);
      Plotly.restyle(plotEl, { x: [[response.tau[index]]], y: [[response.y[index]]] }, [2]);
    }
  }

  function stop() {
    isPlaying = false;
    startTimestamp = null;
    if (rafId) cancelAnimationFrame(rafId);
    rafId = null;
    playBtn.textContent = "播放";
  }

  function tick(timestamp) {
    if (!isPlaying) return;
    if (startTimestamp === null) {
      startTimestamp = timestamp - (currentIndex / steps) * playbackDuration;
    }
    const progress = Math.min((timestamp - startTimestamp) / playbackDuration, 1);
    const nextIndex = Math.min(Math.floor(progress * steps), steps);
    if (nextIndex !== currentIndex) render(nextIndex, false);
    if (progress >= 1) {
      stop();
      return;
    }
    rafId = requestAnimationFrame(tick);
  }

  function play(fromStart = false) {
    if (fromStart) render(0, true);
    isPlaying = true;
    startTimestamp = null;
    playBtn.textContent = "暂停";
    rafId = requestAnimationFrame(tick);
  }

  function applyParameters(recordSample = true) {
    tauMax = getTauMax(r, zeta);
    response = computeResponse(r);
    stop();
    render(0, true);
    if (recordSample) addBetaSample();
    play(false);
  }

  rSlider.addEventListener("input", (event) => {
    r = Number(event.target.value);
    applyParameters(true);
  });

  zetaSlider.addEventListener("input", (event) => {
    zeta = Number(event.target.value);
    applyParameters(true);
  });

  playBtn.addEventListener("click", () => {
    if (isPlaying) stop();
    else play(currentIndex >= steps);
  });
  resetBtn.addEventListener("click", () => {
    stop();
    play(true);
  });

  betaResetBtn.addEventListener("click", () => {
    betaSamples = [];
    renderBetaPlot();
  });

  render(0, true);
  addBetaSample();
  play(false);
})();
</script>
"""

display(HTML(html))
```

为了确定 $r$ 为何值时, $\beta$ 达到最大值, 可令

$$
\frac{\mathrm{d}\beta}{\mathrm{d}r}=0
$$

由式{eq}`eq:1-79`可求得

$$
r=\sqrt{1-2\zeta^{2}}
$$ (eq:1-80)

由上式知, 动力放大系数 $\beta$ 的最大值, 并不是发生在 $r=1$ (即 $\theta=\omega$) 时, 而是发生在 $\theta$ 略小于 $\omega$ 时。但通常 $\zeta\ll1$, 故式{eq}`eq:1-80`等号右端总是非常接近于 1。因此, 在考虑阻尼的情况下, 仍可认为共振是发生在干扰力频率与系统无阻尼自振频率相等的时候。因此, 既然共振时 $\theta\approx\omega$, 将其代入式{eq}`eq:1-79`, 可得最大动力放大系数 $\beta$ 的近似值。

$$
\beta_{\max}=\frac{1}{2\zeta}
$$ (eq:1-81)

因

$$
\beta_{\max}=\frac{x_{\max}}{x_{\mathrm{st}}}
$$ (eq:1-82)

故不难得到

$$
\zeta=\frac{1}{2\beta_{\max}}=\frac{x_{\mathrm{st}}}{2x_{\max}}
$$ (eq:1-83)

因此, 在简谐干扰力作用下, 如能算出 $x_{\mathrm{st}}$, 只要再测出最大动力位移值 $x_{\max}$, 便不难由式{eq}`eq:1-83`计算阻尼比。测定阻尼系数的这一方法通常称为**共振振幅法**(Resonance Amplification Method)。

```{note}
**例1-7** 某部件在液体中运动, 干扰力 $P=49\sin\theta t\ (\mathrm{N})$, 在周期 $\tau=0.2\ \mathrm{s}$ 时发生共振, 振幅为 $0.50\ \mathrm{cm}$, 求阻尼系数 $c$。

解 发生共振时干扰力的频率
$$
\theta=\frac{2\pi}{\tau}=\frac{2\pi}{0.2}=10\pi \tag{1}
$$

因此, 可判断该部件的自振频率为 $\omega=10\pi$。由式{eq}`eq:1-27`、式{eq}`eq:1-42`和式{eq}`eq:1-83`知

$$
c=2m\varepsilon=2m\omega\zeta=\frac{m\omega x_{\mathrm{st}}}{x_{\max}} \tag{2}
$$

上式中 $x_{\max}=0.50\ \mathrm{cm}=0.005\ \mathrm{m}$ 为题目中给定。由式{eq}`eq:1-65`和式{eq}`eq:1-10`，有

$$
x_{\mathrm{st}}=\frac{P}{k}=\frac{P}{m\omega^{2}} \tag{3}
$$

因此

$$
c=\frac{P}{x_{\max}\omega}=\frac{49}{0.005\times10\pi}=311.9\ \mathrm{N\cdot s/m} \tag{4}
$$
```

纯强迫位移响应与简谐干扰力的相位差 $\phi$ 由式{eq}`eq:1-74`求得为

$$
\phi=\arctan\left(\frac{2\varepsilon\theta}{\omega^{2}-\theta^{2}}\right)=\arctan\frac{2\zeta r}{1-r^{2}}
$$ (eq:1-84)

它与 $r=\frac{\theta}{\omega}$ 的关系如{numref}`fig:1-19`所示。在零阻尼情况下, 当 $r<1$ (即 $\theta<\omega$) 时, 强迫振动正好与干扰力同相($\phi=0$); 当 $r>1$ (即 $\theta>\omega$) 时, 强迫振动则刚好与干扰力反相($\phi=\pi$); 当共振时, 可以看出相位 $\phi$ 是不定的。当存在阻尼时, 纯强迫位移响应总是滞后于干扰力。阻尼不同, 滞后相位角也不同。但是当 $r=1$ (共振) 时, 无论阻尼是多少, 相位角都等于 $\frac{\pi}{2}$。因此也可以利用这一性质由实验测定系统的固有频率。

```{figure} figures/fig1-19.png
---
name: fig:1-19
width: 80%
---
相位差与频率比的关系
```

对于远离共振区域的 $r$ 值, 当阻尼很小时, 相位 $\phi$ 与不考虑阻尼时相差不大。亦即在 $r$ 低于共振区很远时, 相位 $\phi$ 接近于 $0$; 在 $r$ 远高于共振区时, 相位 $\phi$ 接近于 $\pi$。因而阻尼对相位的影响常忽略不计, 但在共振区域附近除外。

在实际测振时, 还常用下面的**带宽法**(Band-Width Method)或**半功率法**(Half-Power Method)来求得系统的阻尼系数。假定在某项实测中, 得到一条频率-振幅曲线, 如{numref}`fig:1-20`所示。自振频率为 $\omega$, 它对应的振幅峰值为 $A_{0}$, 而与振幅 $0.707A_{0}$ 对应的频率共有 2 个, 分别记为 $\omega_{1}$ 和 $\omega_{2}$。则阻尼比的近似表达式为

```{figure} figures/fig1-20.png
---
name: fig:1-20
width: 80%
---
半功率法
```

$$
\zeta=\frac{\omega_{2}-\omega_{1}}{2\omega}
$$ (eq:1-85)

当 $\theta=\omega$, 即 $r=1$ 时, 由式{eq}`eq:1-82`和式{eq}`eq:1-83`得

$$
A_{0}=x_{\mathrm{st}}\beta_{\max}=\frac{x_{\mathrm{st}}}{2\zeta} \tag{1}
$$

由式{eq}`eq:1-79`可知

$$
A_{1}^{2}=A_{2}^{2}=\beta^{2}x_{\mathrm{st}}^{2}=\frac{x_{\mathrm{st}}^{2}}{(1-r^{2})^{2}+4r^{2}\zeta^{2}} \tag{2}
$$

注意到 $A_{1}=A_{2}=0.707A_{0}$, 再由式(1)和式(2), 可知与 $A_{1}$、$A_{2}$ 所对应的 $r$ 值应满足方程

$$
8\zeta^{2}=(1-r^{2})^{2}+4r^{2}\zeta^{2} \tag{3}
$$

为由上式求得 $r$ 的两个正根 $r_{1}$ 与 $r_{2}$, 不妨先移项将其改写为

$$
(1-r^{2})^{2}-4\zeta^{2}(1-r^{2})-4\zeta^{2}=0 \tag{4}
$$

即得

$$
1-r^{2}=2\zeta^{2}\pm2\zeta\sqrt{1+\zeta^{2}} \tag{5}
$$

通常 $\zeta^{2}\ll1$, 故上式可展开根号而成

$$
r^{2}=1-2\zeta^{2}\mp2\zeta\left(1+\frac{\zeta^{2}}{2}\right) \tag{6}
$$

于是可得

$$
r_{1}^{2}=(1-\zeta)^{2}-3\zeta^{2}-\zeta^{3},\quad
r_{2}^{2}=(1+\zeta)^{2}-3\zeta^{2}+\zeta^{3} \tag{7}
$$

略去高阶小量, 可得

$$
r_{1}=1-\zeta,\quad r_{2}=1+\zeta \tag{8}
$$

所以

$$
r_{2}-r_{1}=2\zeta \tag{9}
$$

或

$$
\frac{\omega_{2}}{\omega}-\frac{\omega_{1}}{\omega}=2\zeta \tag{10}
$$

于是得到式{eq}`eq:1-85`。
