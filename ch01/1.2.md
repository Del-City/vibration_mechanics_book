---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
---

# 自由振动

若系统不受外部的干扰(阻尼除外)作用，仅由初始条件（初位移和初速度）引起的振动，称为**自由振动**（FreeVibration）。反之，系统因持续地受到外部干扰作用而发生的振动,称为**强迫振动**（Forced Vibration）。
由于系统在自由振动中不受外力作用，由方程{eq}`eq:1-5`可知响应的特解为0。因此齐次方程通解$x_h(t)$就是自由振动响应，式{eq}`eq:1-4`就是自由振动方程，即

$$
m\ddot{x}(t)+c\dot{x}(t)+kx(t)=0
$$ (eq:1-8)

自由振动方程{eq}`eq:1-8`考虑了阻尼的影响，称为**有阻尼自由振动**（Damped Free Vibration）。如不考虑阻尼影响，则称为**无阻尼自由振动**（Undamped Free Vibration）。

## 无阻尼自由振动

当不考虑阻尼作用时，自由振动方程{eq}`eq:1-8`退化为

$$
m\ddot{x}(t)+kx(t)=0
$$ (eq:1-9)

若令

$$
\omega^2 = \dfrac{k}{m}
$$ (eq:1-10)

则方程{eq}`eq:1-9`可以改写为

$$
\ddot{x}(t) + \omega^2 x(t) = 0
$$ (eq:1-11)

常微分方程{eq}`eq:1-11`的通解很容易得到，可表示为：

$$
x(t) = A_1 \sin \omega t + A_2 \cos \omega t = A \sin(\omega t + \phi)
$$ (eq:1-12)

则

$$
\dot{x}(t) = A_1 \omega \cos \omega t - A_2 \omega \sin \omega t = A\omega \cos(\omega t + \phi)
$$ (eq:1-13)

式中,$A1$、$A2$（或$A$、$\phi$）是待定常数,取决于初始条件。如将初始条件,即式{eq}`eq:1-7`,分别代入式{eq}`eq:1-12`和式{eq}`eq:1-13`,就可以得到

$$
A_1 = \frac{\dot{x}_0}{\omega}, \quad A_2 = x_0
$$ (eq:1-14)

则

$$
x(t) = \frac{\dot{x}_0}{\omega} \sin \omega t + x_0 \cos \omega t
$$ (eq:1-15)

从式{eq}`eq:1-12`或式{eq}`eq:1-15`可以看出，当不考虑阻尼作用时，$x$ 的值总是随着时间的增加周而复始地改变。这种振动称为**简谐**（或**谐和**）**振动**（Harmonic Vibration）。$A$ 称为振动的**振幅**（Amplitude），$\omega t+\phi$ 称为**相角**（Phase Angle），$\phi$ 称为**初相角**（$t=0$ 时的**相角**）。容易证明

$$
A=\sqrt{A_1^2+A_2^2}=\sqrt{\frac{\dot{x}_0^2}{\omega^2}+x_0^2}, \quad \tan\phi=\frac{A_2}{A_1}=\frac{x_0\omega}{\dot{x}_0}
$$ (eq:1-16)

只要知道了 $A_1$、$A_2$，就容易求出 $A$ 与 $\phi$，反之亦然。式{eq}`eq:1-15`表示的无阻尼自由振动如{numref}`fig:1-4`所示。

```{figure} figures/fig1-4.png
---
name: fig:1-4
width: 80%
---
无阻尼自由振动
```

简谐振动属于周期性振动。从式{eq}`eq:1-12`不难看出，$\omega$ 是周期振动的**角频率**（Angular Frequency）或**圆频率**（Circular Frequency），称为单自由度系统的**无阻尼自振（或固有）频率**（Undamped Natural Frequency of Vibration）。单位为弧度/秒（rad/s）或 1/秒（1/s）。由式{eq}`eq:1-10`知

$$
\omega = \sqrt{\frac{k}{m}}
$$ (eq:1-17)

工程上常用每秒内系统振动的次数来表示自振频率，也称**自振（或固有）频率**（Natural Frequency），即

$$
f=\frac{\omega}{2\pi}
$$ (eq:1-18)

其单位为周/秒（或赫兹、Hz）。区别于 $f$，$\omega$ 通常称为自振（或固有）圆（或角）频率（Natural Circular/Angular Frequency）。系统振动一次所需时间

$$
T=\frac{2\pi}{\omega}=\frac{1}{f}
$$ (eq:1-19)

称为自振（或固有）周期（Natural Period of Vibration），单位为秒（s）。

从上面的讨论可知，振幅 $A$ 与初相角 $\phi$ 是由初始条件确定的；但自振频率 $f$（或 $\omega$）和自振周期 $T$ 则与初始条件无关，而仅取决于系统本身的刚度和质量。

由式{eq}`eq:1-17`可知：当振动系统的质量 $m$ 增加时，将引起自振频率的下降，而当刚度 $k$ 增加时，将引起自振频率的增加。一切弹性振动系统都是这样。因此，在处理实际工程问题时，如果想提高某结构的自振频率，可以从两方面设法：或者提高结构的刚度（如增加约束，采用刚度大的构件等）；或者减小结构的重量（如改用轻质材料等）。

```{figure} figures/fig1-5.png
---
name: fig:1-5
width: 50%
---
悬挂质量系统
```

考虑如{numref}`fig:1-5`(a) 所示悬挂质量系统（不考虑弹簧的质量），它可看做是如{numref}`fig:1-1`(a) 所示系统旋转了 90°。假定质量块在重力 $G$ 作用下的静伸长为 $x_{\text{st}}$。如果质量块偏离静平衡位置的位移为 $x$，此时作用在质量块上的力如{numref}`fig:1-5`(b) 所示。仿照方程{eq}`eq:1-3`的推导，系统的动力平衡方程可以写做

$$
m \ddot{\tilde{x}}(t) + c \dot{\tilde{x}}(t) + k \tilde{x}(t) = F_e(t) + G
$$ (eq:1-20)

其中：

$$
\tilde{x}(t)=x(t)+x_{st}
$$ (eq:1-21)

为质量块的绝对位移。注意到

$$
G=kx_{st}
$$ (eq:1-22)

将式{eq}`eq:1-22`和式{eq}`eq:1-21`代入方程{eq}`eq:1-20`, 同时注意到 $x_{\text{st}}$ 是不随时间变化的, 则方程{eq}`eq:1-20`可化为

$$
m \ddot{x}(t)+c \dot{x}(t)+k x(t)=F_{\mathrm{e}}(t)
$$ (eq:1-23)

这说明相对于振动系统静平衡位置写出的运动方程不受重力影响。因此, 在本书以后的讨论中, 动力响应一般以静平衡位置作为基准。求总的位移、内力响应时, 只要把动力分析的结果与相应的静力结果相加即可。

对于如{numref}`fig:1-5`(a)所示的悬挂质量系统, 可以推得其自振频率的另一种表达方式。由式{eq}`eq:1-17`可知

$$
\omega=\sqrt{\frac{k}{m}}=\sqrt{\frac{k g}{G}}=\sqrt{\frac{k g}{k x_{\mathrm{st}}}}=\sqrt{\frac{g}{x_{\mathrm{st}}}}
$$ (eq:1-24)

虽然我们并不知道重物的质量 $m$ 是多少, 也不知道弹簧的刚度 $k$ 是多少, 但很容易根据式{eq}`eq:1-24`由其静伸长求出系统的自振频率。

## 交互探索：质量和刚度如何影响振动？

```{tip}
在无阻尼系统中，系统的运动完全由其自身的物理属性决定：
* **质量 ($m$) 增加**：系统惯性变大，运动状态更难改变，导致振荡变慢，固有频率降低。
* **刚度 ($k$) 增加**：系统更“硬”，恢复力变大，导致振荡变快，固有频率增加。
```

请尝试点击或拖动下方的滑动条，观察质量和刚度是如何改变系统固有频 $\omega$和自振周期 $T$的。

```{code-cell} python
:tags: [remove-input]

import numpy as np
from IPython.display import display, HTML

# 1. 基础物理参数设置
x0_init = 1.00  # 初始位移 (m)
v0_init = 4.00  # 初始速度 (m/s)
m_init = 1.00   # 初始质量 (kg)
k_init = 100.00 # 初始刚度 (N/m)

html = f"""
<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>
<style>
  .physics-slider-container input[type="range"] {{
    -webkit-appearance: none; appearance: none; width: 100%; height: 8px;
    border-radius: 999px; background: rgba(148, 163, 184, 0.35); outline: none; margin: 10px 0;
  }}
  .physics-slider-container input[type="range"]::-webkit-slider-thumb {{
    -webkit-appearance: none; appearance: none; width: 20px; height: 20px;
    border-radius: 50%; background: #2563eb; border: 3px solid #ffffff;
    box-shadow: 0 4px 10px rgba(37, 99, 235, 0.3); cursor: pointer; transition: transform 0.1s;
  }}
  .physics-slider-container input[type="range"]::-webkit-slider-thumb:hover {{
    transform: scale(1.15);
  }}
  .physics-data-card {{
    padding: 0.8rem 1rem; border-radius: 16px; background: rgba(255,255,255,0.85); 
    border: 1px solid rgba(37, 99, 235, 0.15); box-shadow: 0 4px 6px rgba(0,0,0,0.02);
    display: flex; flex-direction: column; justify-content: center;
  }}
  .physics-label {{ font-size: 13px; color: #64748b; font-weight: 600; letter-spacing: 0.02em; }}
  .physics-value {{ margin-top: 0.3rem; font-size: 18px; font-weight: 700; color: #0f172a; font-family: 'JetBrains Mono', monospace; }}
  .physics-btn {{
    flex: 1; border-radius: 16px; font-weight: bold; cursor: pointer; font-size: 16px; transition: all 0.2s;
  }}
  .physics-btn-primary {{
    border: none; background: #2563eb; color: white; box-shadow: 0 4px 6px rgba(37,99,235,0.2);
  }}
  .physics-btn-secondary {{
    border: 1px solid #cbd5e1; background: white; color: #334155;
  }}
</style>

<div id="advanced-undamped-demo" style="
    max-width: 1080px; margin: 1.5rem auto; padding: 1.2rem;
    border-radius: 24px; background: linear-gradient(135deg, #f8fafc 0%, #eff6ff 100%);
    box-shadow: 0 20px 40px rgba(15, 23, 42, 0.08); border: 1px solid rgba(148, 163, 184, 0.2);
    font-family: 'Segoe UI', system-ui, sans-serif;
">
  <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; margin-bottom: 1rem;">
    <div class="physics-data-card">
      <div class="physics-label">系统质量 (m)</div>
      <div id="adv-m-display" class="physics-value" style="color: #2563eb;">1.00 kg</div>
    </div>
    <div class="physics-data-card">
      <div class="physics-label">弹簧刚度 (k)</div>
      <div id="adv-k-display" class="physics-value" style="color: #2563eb;">100.00 N/m</div>
    </div>
    <div class="physics-data-card" style="background: rgba(37, 99, 235, 0.06);">
      <div class="physics-label" style="color: #1d4ed8;">固有圆频率 (ω)</div>
      <div id="adv-omega-display" class="physics-value" style="color: #1e40af;">10.00 rad/s</div>
    </div>
    <div class="physics-data-card" style="background: rgba(37, 99, 235, 0.06);">
      <div class="physics-label" style="color: #1d4ed8;">自振周期 (T)</div>
      <div id="adv-period-display" class="physics-value" style="color: #1e40af;">0.63 s</div>
    </div>
  </div>

  <div class="physics-slider-container" style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; padding: 0.5rem 1rem 1.5rem;">
    <div>
      <label style="font-size: 14px; font-weight: 700; color: #334155; display: flex; justify-content: space-between;">
        <span>调节质量 m</span>
        <span style="font-weight: 400; color: #64748b;">(0.5 ~ 10.0 kg)</span>
      </label>
      <input id="adv-m-slider" type="range" min="0.5" max="10.0" step="0.1" value="{m_init}">
    </div>
    <div>
      <label style="font-size: 14px; font-weight: 700; color: #334155; display: flex; justify-content: space-between;">
        <span>调节刚度 k</span>
        <span style="font-weight: 400; color: #64748b;">(10 ~ 1000 N/m)</span>
      </label>
      <input id="adv-k-slider" type="range" min="10" max="1000" step="1" value="{k_init}">
    </div>
  </div>

  <div style="display: grid; grid-template-columns: 3fr 1fr 1fr; gap: 1rem; align-items: stretch; margin-bottom: 1rem;">
    
    <div style="padding: 1rem; border-radius: 18px; background: #ffffff; border: 1px solid rgba(148, 163, 184, 0.2); display: flex; flex-direction: column; justify-content: center; min-height: 180px;">
      <svg id="adv-oscillator" viewBox="0 0 600 240" style="width:100%; height:100%; display:block; border-radius:12px; background: #f8fafc;">
        <rect x="0" y="0" width="600" height="240" rx="12" fill="transparent"></rect>
        <rect x="20" y="30" width="16" height="180" rx="4" fill="#94a3b8"></rect>
        <line x1="36" y1="115" x2="50" y2="115" stroke="#cbd5e1" stroke-width="4"></line>
        <line x1="36" y1="115" x2="560" y2="115" stroke="#e2e8f0" stroke-width="6" stroke-linecap="round"></line>
        <line id="adv-equilibrium" x1="300" y1="20" x2="300" y2="230" stroke="#f97316" stroke-width="2" stroke-dasharray="6 4" opacity="0.5"></line>
        <polyline id="adv-spring" fill="none" stroke="#2563eb" stroke-width="4" stroke-linejoin="round"></polyline>
        <rect id="adv-mass" x="265" y="80" width="70" height="70" rx="12" fill="#94a3b8" stroke="#1d4ed8" stroke-width="3"></rect>
        <text x="290" y="35" fill="#f97316" style="font-size: 20px;">平衡位置</text>
      </svg>
    </div>

    <div style="display: flex; flex-direction: column; gap: 0.8rem; height: 100%; justify-content: space-between;">
      <div class="physics-data-card" style="flex: 1; border-color: #cbd5e1; padding: 0.8rem;">
        <div class="physics-label">运行时间 (t)</div>
        <div id="adv-t-display" class="physics-value" style="font-size: 16px;">0.00 s</div>
      </div>
      <div class="physics-data-card" style="flex: 1; border-color: #cbd5e1; padding: 0.8rem;">
        <div class="physics-label">实时位移 (x)</div>
        <div id="adv-x-display" class="physics-value" style="font-size: 16px;">1.00 m</div>
      </div>
      <div class="physics-data-card" style="flex: 1; border-color: #cbd5e1; padding: 0.8rem;">
        <div class="physics-label">实时速度 (v)</div>
        <div id="adv-v-display" class="physics-value" style="font-size: 16px;">4.00 m/s</div>
      </div>
    </div>

    <div style="display: flex; flex-direction: column; gap: 0.8rem; height: 100%;">
      <button id="adv-play" class="physics-btn physics-btn-primary">启动仿真</button>
      <button id="adv-reset" class="physics-btn physics-btn-secondary">重置初始状态</button>
    </div>

  </div>

  <div style="padding: 1rem; border-radius: 18px; background: #ffffff; border: 1px solid rgba(148, 163, 184, 0.2);">
    <div id="adv-plot" style="width: 100%; height: 400px;"></div>
  </div>
</div>

<script>
(function() {{
  const container = document.getElementById("advanced-undamped-demo");
  if (!container || container.dataset.initialized === "true") return;
  container.dataset.initialized = "true";

  // 动画与绘图常量
  const equilibriumX = 300; 
  const massWidth = 70;
  const motionScale = 30; // 动画位移缩放比
  const timeWindow = 10;  // 图表横轴显示的窗口宽度(秒)

  // 物理状态持久化变量 (用于保证参数突变时位移、速度绝对连续)
  let globalTime = 0;
  let localTime = 0;
  let stateX = {x0_init};
  let stateV = {v0_init};
  let currentM = {m_init};
  let currentK = {k_init};
  let omegaN = Math.sqrt(currentK / currentM);

  // 历史轨迹数组
  let historyT = [];
  let historyX = [];

  // DOM 元素绑定
  const elDisplayM = document.getElementById("adv-m-display");
  const elDisplayK = document.getElementById("adv-k-display");
  const elDisplayOmega = document.getElementById("adv-omega-display");
  const elDisplayPeriod = document.getElementById("adv-period-display");
  const elDisplayT = document.getElementById("adv-t-display");
  const elDisplayX = document.getElementById("adv-x-display");
  const elDisplayV = document.getElementById("adv-v-display");
  
  const sliderM = document.getElementById("adv-m-slider");
  const sliderK = document.getElementById("adv-k-slider");
  const btnPlay = document.getElementById("adv-play");
  const btnReset = document.getElementById("adv-reset");
  const plotDiv = document.getElementById("adv-plot");
  
  const svgSpring = document.getElementById("adv-spring");
  const svgMass = document.getElementById("adv-mass");

  let isPlaying = false;
  let lastTimestamp = 0;
  let rafId = null;

  // 根据当前所在的时标，解析位移和速度
  function getInstantState(t) {{
    const x = stateX * Math.cos(omegaN * t) + (stateV / omegaN) * Math.sin(omegaN * t);
    const v = -stateX * omegaN * Math.sin(omegaN * t) + stateV * Math.cos(omegaN * t);
    return {{x, v}};
  }}

  // 初始生成 Plotly 数据集
  function getPlotData(currentPointX, currentPointY) {{
    return [
      {{ // 行进中的实时轨迹线
        x: historyT, y: historyX, mode: "lines", name: "实测位移",
        line: {{ color: "#2563eb", width: 3.5 }}, hoverinfo: "skip"
      }},
      {{ // 运动质点标识
        x: [currentPointX], y: [currentPointY], mode: "markers", name: "当前位置",
        marker: {{ size: 12, color: "#f97316", line: {{ color: "white", width: 2 }} }},
        hoverinfo: "skip"
      }}
    ];
  }}

  // 初始图表布局结构
  function getPlotLayout(xMin, xMax, yLimit) {{
    return {{
      margin: {{ l: 60, r: 20, t: 20, b: 50 }},
      paper_bgcolor: "rgba(0,0,0,0)", plot_bgcolor: "#ffffff",
      xaxis: {{ 
        title: "<b>时间 t (s)</b>", 
        range: [xMin, xMax], 
        showgrid: true, gridcolor: "#f1f5f9", linecolor: "#cbd5e1",
        tickmode: "linear", dtick: 2, tickformat: ".2f"
      }},
      yaxis: {{ 
        title: "<b>位移 x (m)</b>", 
        range: [-yLimit, yLimit], 
        showgrid: true, gridcolor: "#f1f5f9", zeroline: true, zerolinecolor: "#94a3b8", zerolinewidth: 2,
        tickmode: "linear", dtick: Math.max(1, Math.floor(yLimit / 2)), tickformat: ".2f"
      }},
      showlegend: false
    }};
  }}

  // 初始化绘图引擎
  function initPlotly() {{
    const maxAmplitude = Math.sqrt(stateX*stateX + Math.pow(stateV/omegaN, 2));
    const yLimit = Math.max(1.5, maxAmplitude * 1.3);
    Plotly.newPlot(plotDiv, getPlotData(0, stateX), getPlotLayout(0, timeWindow, yLimit), {{ displayModeBar: false }});
  }}

  // 绘制动态弹簧形态
  function updateSpring(startX, endX, y) {{
    const points = [];
    const straight = 20;
    const turns = 14; 
    const amplitude = 20;
    points.push(`${{startX}},${{y}}`);
    points.push(`${{startX + straight}},${{y}}`);
    const usable = Math.max(endX - startX - straight * 2, 20);
    const step = usable / (turns * 2);
    for (let i = 0; i < turns * 2; i += 1) {{
      const px = startX + straight + step * (i + 1);
      const py = i % 2 === 0 ? y - amplitude : y + amplitude;
      points.push(`${{px}},${{py}}`);
    }}
    points.push(`${{endX - straight}},${{y}}`);
    points.push(`${{endX}},${{y}}`);
    svgSpring.setAttribute("points", points.join(" "));
  }}

  // 高效局部增量刷新视图 
  function syncView() {{
    const currentState = getInstantState(localTime);
    
    // 1. 更新文本仪表盘
    elDisplayT.textContent = globalTime.toFixed(2) + " s";
    elDisplayX.textContent = currentState.x.toFixed(2) + " m";
    elDisplayV.textContent = currentState.v.toFixed(2) + " m/s";

    // 2. 更新 SVG 振子位置
    const massCenterX = equilibriumX + currentState.x * motionScale;
    const massLeft = massCenterX - massWidth / 2;
    svgMass.setAttribute("x", massLeft.toFixed(2));
    updateSpring(36, massLeft, 115);

    // 3. 高性能增量重绘 Plotly 曲线
    const xMax = Math.max(timeWindow, globalTime);
    const xMin = Math.max(0, globalTime - timeWindow);
    const maxAmplitude = Math.sqrt(stateX*stateX + Math.pow(stateV/omegaN, 2));
    const yLimit = Math.max(1.5, maxAmplitude * 1.3);

    const currentPointX = historyT.length > 0 ? historyT[historyT.length - 1] : globalTime;
    const currentPointY = historyX.length > 0 ? historyX[historyX.length - 1] : currentState.x;

    Plotly.update(plotDiv, {{
        x: [historyT, [currentPointX]],
        y: [historyX, [currentPointY]]
    }}, {{
        'xaxis.range': [xMin, xMax],
        'yaxis.range': [-yLimit, yLimit],
        'yaxis.dtick': Math.max(1, Math.floor(yLimit / 2))
    }});
  }}

  // 参数变更监听器：保证完全无缝物理切换
  function onSliderDrag() {{
    const currentState = getInstantState(localTime);
    
    stateX = currentState.x;
    stateV = currentState.v;
    localTime = 0; 
    
    currentM = parseFloat(sliderM.value);
    currentK = parseFloat(sliderK.value);
    omegaN = Math.sqrt(currentK / currentM);
    
    elDisplayM.textContent = currentM.toFixed(2) + " kg";
    elDisplayK.textContent = currentK.toFixed(2) + " N/m";
    elDisplayOmega.textContent = omegaN.toFixed(2) + " rad/s";
    elDisplayPeriod.textContent = ((2 * Math.PI) / omegaN).toFixed(2) + " s";

    if (!isPlaying) syncView();
  }}

  sliderM.addEventListener("input", onSliderDrag);
  sliderK.addEventListener("input", onSliderDrag);

  // 核心动画主循环
  function animateFrame(timestamp) {{
    if (!isPlaying) return;
    if (lastTimestamp === 0) lastTimestamp = timestamp;
    
    const dt = (timestamp - lastTimestamp) / 1000.0;
    lastTimestamp = timestamp;
    
    if (dt < 0.1) {{
        // 引入计算子步 (Sub-stepping) 来提高采样密度
        const numSubSteps = 5; // 子步数：改为 5 意味着每帧采集 5 个点，每秒采集 300 个点，曲线将非常光滑！您可以尝试改为 10 获得极致平滑。
        const subDt = dt / numSubSteps;

        for (let i = 0; i < numSubSteps; i++) {{
            globalTime += subDt;
            localTime += subDt;
            
            const currentState = getInstantState(localTime);
            historyT.push(globalTime);
            historyX.push(currentState.x);
        }}
        
        // 清理超出视野范围的历史数据，防止内存溢出卡顿
        while (historyT.length > 0 && historyT[0] < globalTime - timeWindow - 2) {{
            historyT.shift();
            historyX.shift();
        }}
    }}
    
    syncView();
    rafId = requestAnimationFrame(animateFrame);
  }}

  btnPlay.addEventListener("click", () => {{
    if (isPlaying) {{
      isPlaying = false;
      cancelAnimationFrame(rafId);
      btnPlay.textContent = "恢复仿真";
      btnPlay.className = "physics-btn physics-btn-primary";
    }} else {{
      isPlaying = true;
      lastTimestamp = performance.now();
      btnPlay.textContent = "暂停仿真";
      btnPlay.className = "physics-btn physics-btn-primary";
      btnPlay.style.background = "#ef4444"; 
      btnPlay.style.boxShadow = "0 4px 6px rgba(239, 68, 68, 0.2)";
      rafId = requestAnimationFrame(animateFrame);
    }}
  }});

  btnReset.addEventListener("click", () => {{
    isPlaying = false;
    cancelAnimationFrame(rafId);
    btnPlay.textContent = "启动仿真";
    btnPlay.style.background = ""; // 恢复默认主色
    btnPlay.style.boxShadow = "";
    btnPlay.className = "physics-btn physics-btn-primary";
    
    globalTime = 0;
    localTime = 0;
    stateX = {x0_init};
    stateV = {v0_init};
    historyT = [0];
    historyX = [{x0_init}];
    lastTimestamp = 0;
    
    sliderM.value = {m_init};
    sliderK.value = {k_init};
    onSliderDrag();
    syncView();
  }});

  // 初始化执行
  historyT.push(0);
  historyX.push({x0_init});
  initPlotly();
  onSliderDrag();
}})();
</script>
"""

display(HTML(html))
```

## 有阻尼自由振动

前面已经提过, 系统在振动过程中, 总要受到各种阻尼的作用, 所以自由振动将逐渐衰减。1.2.1节中所述的简谐振动只是一种理想化情况。

单自由度系统考虑阻尼作用的自由振动方程为式{eq}`eq:1-8`, 即

$$
m\ddot{x}(t)+c\dot{x}(t)+kx(t)=0
$$ (eq:1-25)

或写为

$$
\ddot{x}(t)+2\varepsilon\dot{x}(t)+\omega^{2}x(t)=0
$$ (eq:1-26)

其中

$$
\varepsilon=\frac{c}{2m}
$$ (eq:1-27)

可称为阻尼特性系数(Damping Coefficient), 单位为 $1/$秒($1/\text{s}$)；$\omega^{2}$仍由式{eq}`eq:1-10`定义。

常微分方程{eq}`eq:1-26`的特征方程为

$$
s^{2}+2\varepsilon s+\omega^{2}=0
$$ (eq:1-28)

特征方程的根为

$$
s_{1,2}=-\varepsilon\pm\sqrt{\varepsilon^{2}-\omega^{2}}
$$ (eq:1-29)

下面针对阻尼取值的几种情况来讨论方程{eq}`eq:1-26`的解。

### 超临界阻尼系统(Over Critically Damped System)

阻尼很大, 即 $\varepsilon>\omega$, 此时特征方程{eq}`eq:1-28`有两个不等的实根, 则微分方程的通解为

$$
x(t)=c_{1}\mathrm{e}^{s_{1}t}+c_{2}\mathrm{e}^{s_{2}t}=c_{1}\mathrm{e}^{\left(-\varepsilon+\sqrt{\varepsilon^{2}-\omega^{2}}\right)t}+c_{2}\mathrm{e}^{\left(-\varepsilon-\sqrt{\varepsilon^{2}-\omega^{2}}\right)t}
$$ (eq:1-30)

对于初始条件 $x(0)=x_{0},\dot{x}(0)=\dot{x}_{0}$, 不难得到

$$
\begin{align}
x(t)=&\frac{\left(\varepsilon+\sqrt{\varepsilon^{2}-\omega^{2}}\right)x_{0}+\dot{x}_{0}}{2\sqrt{\varepsilon^{2}-\omega^{2}}}\mathrm{e}^{\left(-\varepsilon+\sqrt{\varepsilon^{2}-\omega^{2}}\right)t}+ \\
&\frac{\left(-\varepsilon+\sqrt{\varepsilon^{2}-\omega^{2}}\right)x_{0}-\dot{x}_{0}}{2\sqrt{\varepsilon^{2}-\omega^{2}}}\mathrm{e}^{\left(-\varepsilon-\sqrt{\varepsilon^{2}-\omega^{2}}\right)t}
\end{align}
$$ (eq:1-31)

### 临界阻尼系统(Critically Damped System)

阻尼很大, 但 $\varepsilon=\omega$。由式{eq}`eq:1-29`知 $s_{1}=s_{2}=-\varepsilon$, 特征方程有两个相等的实根, 则微分方程的通解为

$$
x(t)=\mathrm{e}^{-\varepsilon t}\left(c_{1}+c_{2} t\right)
$$ (eq:1-32)

由初始条件 $x(0)=x_{0},\dot{x}(0)=\dot{x}_{0}$, 不难得到

$$
x(t)=\mathrm{e}^{-\varepsilon t}\left[x_{0}+\left(\dot{x}_{0}+\varepsilon x_{0}\right) t\right]
$$ (eq:1-33)

式{eq}`eq:1-31`和式{eq}`eq:1-33`所表示的运动都没有振动的特征。不难验证, 当 $t$ 增大时, $x$ 值单调地趋于零, 如{numref}`fig:1-8`所示。

```{figure} figures/fig1-8.png
---
name: fig:1-8
width: 80%
---
临界阻尼和过阻尼系统的自由振动
```

### 低临界阻尼系统(Under Critically Damped System)

阻尼不大, $\varepsilon<\omega$, 这时特征方程的根为

$$
s_{1,2}=-\varepsilon\pm\mathrm{i}\omega_{\varepsilon}
$$ (eq:1-34)

其中

$$
\omega_{\varepsilon}=\sqrt{\omega^{2}-\varepsilon^{2}}
$$ (eq:1-35)

此时微分方程的通解为

$$
x(t)=\mathrm{e}^{-\varepsilon t}\left(B_{1}\sin\omega_{\varepsilon}t+B_{2}\cos\omega_{\varepsilon}t\right)
$$ (eq:1-36)

由初始条件 $x(0)=x_{0},\dot{x}(0)=\dot{x}_{0}$, 不难求得

$$
B_{1}=\frac{\dot{x}_{0}+\varepsilon x_{0}}{\omega_{\varepsilon}},\quad B_{2}=x_{0}
$$ (eq:1-37)

故式{eq}`eq:1-36`可写为

$$
x(t)=\mathrm{e}^{-\varepsilon t}\left(\frac{\dot{x}_{0}+\varepsilon x_{0}}{\omega_{\varepsilon}}\sin\omega_{\varepsilon}t+x_{0}\cos\omega_{\varepsilon}t\right)
$$ (eq:1-38)

还可将式{eq}`eq:1-38`写为

$$
x(t)=B\mathrm{e}^{-\varepsilon t}\sin(\omega_{\varepsilon}t+\phi)
$$ (eq:1-39)

其中

$$
B=\sqrt{B_{1}^{2}+B_{2}^{2}}=\frac{\sqrt{\left(\dot{x}_{0}+\varepsilon x_{0}\right)^{2}+\omega_{\varepsilon}^{2} x_{0}^{2}}}{\omega_{\varepsilon}},
\quad
\tan\phi=\frac{B_{2}}{B_{1}}=\frac{\omega_{\varepsilon} x_{0}}{\dot{x}_{0}+\varepsilon x_{0}}
$$ (eq:1-40)

式{eq}`eq:1-38`表示的运动如{numref}`fig:1-9`所示。由{numref}`fig:1-9`可见, 由于阻尼的影响, 振动逐渐衰减至静止。由此可以看出, 有阻尼的自由振动不是谐和振动或周期振动。

```{figure} figures/fig1-9.png
---
name: fig:1-9
width: 80%
---
有阻尼自由振动
```

习惯上称 $\omega_{\varepsilon}$ 为有阻尼自振频率(Damped Natural Frequency of Vibration), 称

$$
T_{\varepsilon}=\frac{2\pi}{\omega_{\varepsilon}}
$$ (eq:1-41)

为有阻尼自振周期(Damped Natural Period of Vibration)。

如果系统的阻尼特性系数 $\varepsilon=\omega$, 称这时的阻尼为临界阻尼(Critical Damping)。令

$$
\zeta=\frac{\varepsilon}{\omega}
$$ (eq:1-42)

称为系统的阻尼比(Damping Ratio), 是系统实际阻尼与其临界阻尼之比。显然 $\zeta=1$时的阻尼就是临界阻尼。容易证明

$$
\omega_{\varepsilon}=\omega\sqrt{1-\zeta^{2}}
$$ (eq:1-43)

将式{eq}`eq:1-42`代入方程{eq}`eq:1-26`, 可得到考虑阻尼作用的自由振动方程的另一种形式, 即

$$
\ddot{x}(t)+2\zeta\omega\dot{x}(t)+\omega^{2}x(t)=0
$$ (eq:1-44)

对于通常的工程结构, $\zeta$的值一般在$1\%\sim10\%$之间。因此, 式{eq}`eq:1-43`等号右端根号内的值非常接近 $1$, 故常近似地认为 $\omega_{\varepsilon}=\omega$。{numref}`fig:1-10`给出了 $\omega_{\varepsilon}$ 与 $\omega$ 的比值随 $\zeta$ 的变化曲线, 可以看到：在 $0\leqslant\zeta\leqslant0.1$ 区间, $\frac{\omega_{\varepsilon}}{\omega}$ 非常接近 $1$。因此, 在计算结构的自振频率时, 可不考虑阻尼的影响。这样计算的 $\omega$ 与实际的 $\omega_{\varepsilon}$ 是很接近的, 但却大大简化了计算。例如, 在例 2-4 中将会看到, 系统的振幅每周期衰减 $50\%$ (实际结构物一般没有这么大的阻尼)。即使在这样的情况下, 容易验证 $\omega$ 与 $\omega_{\varepsilon}$ 的差别亦仅在 $0.6\%$ 左右。当然, 在工程中也有一些阻尼相对比较大的结构或机械系统。例如，为了减小结构或机械系统的振动而人为地在系统中加入一些阻尼器或耗能元件。又如, 为了使仪器有较大的量程, 大多加速度计将其弹簧振子的阻尼比 $\zeta$ 取为 $0.7$。

```{figure} figures/fig1-10.png
---
name: fig:1-10
width: 70%
---
有阻尼自振频率与无阻尼自振频率的比值
```

如以 $B_n$ 表示在某一时刻 $t_n$ 时的振幅,$B_{n+1}$ 表示经过一个周期后(即时刻 $t=t_n+T_{\varepsilon}$)的振幅。由式{eq}`eq:1-39`知, 当振动达到振幅时, 必有 $\sin(\omega_{\varepsilon}t+\phi)=1$, 故有

$$
B_n=x\big|_{t=t_n}=B\mathrm{e}^{-\varepsilon t_n},\quad B_{n+1}=x\big|_{t=t_n+T_{\varepsilon}}=B\mathrm{e}^{-\varepsilon(t_n+T_{\varepsilon})}
$$

所以

$$
\frac{B_{n}}{B_{n+1}}=\mathrm{e}^{\varepsilon T_{\varepsilon}}
$$ (eq:1-45)

若令

$$
\lambda=\ln\frac{B_{n}}{B_{n+1}}
$$ (eq:1-46)

则

$$
\lambda=\varepsilon T_{\varepsilon}=\frac{c}{2m}T_{\varepsilon}=\text{常数}
$$ (eq:1-47)

$\lambda$ 称为**对数递减量或对数递减率(Logarithmic Decrement)**, 则

$$
\frac{B_{n}}{B_{n+1}}=\mathrm{e}^{\lambda}=\text{常数}
$$ (eq:1-48)

因此, 对于有黏滞阻尼情况下的自由振动, 其振幅是逐渐衰减的; 且任意“相邻”二振幅之间的比值是一常数。利用这个性质, 可以由实验得出阻尼系数 $\varepsilon$ 或 $c$ 的值。

由式{eq}`eq:1-47`与式{eq}`eq:1-42`, 知

$$
\zeta=\frac{\lambda}{T_{\varepsilon}\omega}
$$ (eq:1-49)

进而由式{eq}`eq:1-41`与式{eq}`eq:1-43`可得到

$$
\zeta=\frac{\lambda\omega_{\varepsilon}}{2\pi\omega}=\frac{\lambda}{2\pi}\sqrt{1-\zeta^{2}}
$$ (eq:1-50)

```{note}
**例1-4** 设单自由度振动系统的质量 $m=1\ \mathrm{kg}$, 刚度 $k=4900\ \mathrm{N/m}$。假定由实验测得系统振动时的位移曲线如{numref}`fig:1-9`所示。量得某相邻两次振幅为 $B_n=2.5\ \mathrm{cm}$, $B_{n+1}=1.25\ \mathrm{cm}$。求 $\varepsilon$ 与 $c$ 的值。

解 由式{eq}`eq:1-46`知对数衰减量

$$
\lambda=\ln\frac{2.5}{1.25}=0.6931 \tag{1}
$$

再由式{eq}`eq:1-50`, 建立关于 $\zeta$ 的方程

$$
\zeta=\frac{0.6931}{2\pi}\sqrt{1-\zeta^{2}} \tag{2}
$$

由此可解得

$$
\zeta=0.1097 \tag{3}
$$

系统自振频率为

$$
\omega=\sqrt{\frac{k}{m}}=\sqrt{\frac{4900}{1}}=70\ \text{s}^{-1} \tag{4}
$$

则

$$
\varepsilon=\zeta\omega=7.679\ \text{s}^{-1},\quad c=2m\varepsilon=15.36\ \text{N}\cdot\text{s/m} \tag{5}
$$
```

由于式{eq}`eq:1-50`右端根号内值非常接近 $1$, 因此, 式{eq}`eq:1-50`可近似为

$$
\zeta=\frac{\lambda}{2\pi}
$$ (eq:1-51)

用式{eq}`eq:1-51`计算 $\zeta$ 值比用式{eq}`eq:1-50`要容易得多, 而两种方法所得到的结果却非常接近。例如, 用式{eq}`eq:1-51`计算得到例2-4中的 $\zeta$ 值为 $0.1103$, 和用式{eq}`eq:1-50`的计算结果仅相差 $0.5\%$。

当用自由振动衰减曲线求阻尼系数时, 为了提高计算精度, 总是量出相距 $l$ 个周期的振幅 $B_n$ 与 $B_{n+l}$, 算出

$$
\lambda_l=\ln\frac{B_n}{B_{n+l}}
$$ (eq:1-52)

然后按下式计算阻尼比

$$
\zeta=\frac{\lambda_l}{2\pi l}
$$ (eq:1-53)

上述测定阻尼系数的方法称为**自振衰减法(Free Vibration Decay Method)**。

由本节的讨论可知, 在不考虑阻尼作用时, 系统自由振动的振幅不随时间而衰减。亦即在振动过程中, 不发生能量的损耗(或说能量不向外界散逸), 这样的系统, 通常称为保守系统(Conservative System)。反之, 在考虑阻尼作用时, 系统的振幅随时间逐渐衰减, 甚至根本振动不起来, 质量单调地趋于平衡位置。亦即系统在振动过程中, 发生了能量的损耗(或能量向外界散逸), 这样的系统则是非保守系统(Non-Conservative System)。

## 交互探索：阻尼如何影响振动？

```{tip}
**阻尼比 $\zeta$ 的大小直接决定了系统的运动形态：**
1. $\zeta = 0$：**无阻尼状态**。系统发生持续等幅的周期性振荡。
2. $0 < \zeta < 1$：**欠阻尼状态**。系统发生振幅按指数衰减的周期性振荡。
3. $\zeta = 1$：**临界阻尼状态**。系统不发生振荡，以最快速度回到平衡位置。
4. $\zeta > 1$：**过阻尼状态**。系统不发生振荡，极其缓慢地回到平衡位置。
```

下面是一个基于单自由度系统精确解析解构建的仿真模型（设初始位移 $x_0=1$，初始速度 $\dot{x}=0$，固有频率 $\omega=10$）。

请尝试点击或拖动下方的滑动条，观察阻尼比 $\zeta$ 从 0 增加到 1.5 时，振动曲线是如何从“等幅振荡”过渡到“迅速衰减”，最终变为“缓慢爬行”的过阻尼状态的。

```{code-cell} python
:tags: [remove-input]

import numpy as np
import json
from IPython.display import display, HTML

# 1. 基础物理参数设置
t = np.linspace(0, 10, 1200)  # 时间从 0 到 10 秒
omega_n = 10.0              # 固有频率
x0 = 1.0                    # 初始位移
zeta_init = 0.06

zeta_values = np.round(np.arange(0.0, 1.5001, 0.02), 2)
init_active = int(np.argmin(np.abs(zeta_values - zeta_init)))
t_values = np.round(t, 5).tolist()
tick_labels = [f"{value:.1f}" for value in np.arange(0.0, 1.51, 0.1)]
tick_values = np.round(np.arange(0.0, 1.51, 0.1), 1).tolist()
minor_tick_values = [float(value) for value in zeta_values if not np.isclose((value * 100) % 10, 0)]

html = f"""
<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>
<style>
  #free-vibration-demo input[type="range"] {{
    -webkit-appearance: none;
    appearance: none;
    width: 100%;
    height: 6px;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.35);
    outline: none;
  }}

  #free-vibration-demo input[type="range"]::-webkit-slider-runnable-track {{
    height: 6px;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.35);
  }}

  #free-vibration-demo input[type="range"]::-webkit-slider-thumb {{
    -webkit-appearance: none;
    appearance: none;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #0f766e;
    border: 3px solid #ffffff;
    box-shadow: 0 4px 12px rgba(15, 118, 110, 0.28);
    margin-top: -6px;
    cursor: pointer;
  }}

  #free-vibration-demo input[type="range"]::-moz-range-track {{
    height: 6px;
    border: none;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.35);
  }}

  #free-vibration-demo input[type="range"]::-moz-range-progress {{
    height: 6px;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.35);
  }}

  #free-vibration-demo input[type="range"]::-moz-range-thumb {{
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #0f766e;
    border: 3px solid #ffffff;
    box-shadow: 0 4px 12px rgba(15, 118, 110, 0.28);
    cursor: pointer;
  }}
</style>
<div id="free-vibration-demo" style="
    max-width: 1080px;
    margin: 1.2rem auto;
    padding: 1rem 1rem 0.8rem;
    border-radius: 22px;
    background: linear-gradient(145deg, #f7fffd 0%, #eef7ff 100%);
    box-shadow: 0 18px 45px rgba(15, 23, 42, 0.10);
    border: 1px solid rgba(148, 163, 184, 0.18);
    font-family: 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
">
  <div style="
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 0.75rem;
      margin: 0 0 0.9rem;
  ">
    <div style="padding: 0.8rem 0.95rem; border-radius: 16px; background: rgba(255,255,255,0.78); border: 1px solid rgba(15,118,110,0.10);">
      <div style="font-size: 12px; color: #64748b; text-transform: uppercase; letter-spacing: 0.08em;">无阻尼</div>
      <div style="margin-top: 0.3rem; font-size: 14px; color: #0f172a;">持续等幅振荡，系统不耗能。</div>
    </div>
    <div style="padding: 0.8rem 0.95rem; border-radius: 16px; background: rgba(255,255,255,0.78); border: 1px solid rgba(15,118,110,0.10);">
      <div style="font-size: 12px; color: #64748b; text-transform: uppercase; letter-spacing: 0.08em;">欠阻尼</div>
      <div style="margin-top: 0.3rem; font-size: 14px; color: #0f172a;">仍会振荡，但振幅会随时间逐渐衰减。</div>
    </div>
    <div style="padding: 0.8rem 0.95rem; border-radius: 16px; background: rgba(255,255,255,0.78); border: 1px solid rgba(15,118,110,0.10);">
      <div style="font-size: 12px; color: #64748b; text-transform: uppercase; letter-spacing: 0.08em;">临界阻尼</div>
      <div style="margin-top: 0.3rem; font-size: 14px; color: #0f172a;">不振荡，并以最快速度回到平衡位置。</div>
    </div>
    <div style="padding: 0.8rem 0.95rem; border-radius: 16px; background: rgba(255,255,255,0.78); border: 1px solid rgba(15,118,110,0.10);">
      <div style="font-size: 12px; color: #64748b; text-transform: uppercase; letter-spacing: 0.08em;">过阻尼</div>
      <div style="margin-top: 0.3rem; font-size: 14px; color: #0f172a;">不振荡，极其缓慢地回到平衡位置。</div>
    </div>
  </div>

  <div style="
      padding: 0.2rem 0.35rem 0.8rem;
  ">
    <div style="display:flex; justify-content:space-between; align-items:center; gap:0.8rem; margin-bottom:0.45rem; flex-wrap:wrap;">
      <label for="fv-zeta-slider" style="font-size: 14px; color: #334155; font-weight: 700;">
        当前阻尼比
        <span id="fv-zeta" style="margin-left:0.5rem; color:#0f766e;">ζ = 0.20</span>
        <span id="fv-state" style="margin-left:0.7rem; color:#0f172a;">系统状态：欠阻尼</span>
      </label>
    </div>
    <input id="fv-zeta-slider" type="range" min="0" max="{len(zeta_values) - 1}" step="1" value="{init_active}">
    <div style="position:relative; height:14px; margin-top:0.25rem;">
      <div style="position:absolute; left:9px; right:9px; top:0; bottom:0;">
        {"".join(
            f"<span style='position:absolute; left:{(value / 1.5) * 100:.4f}%; transform:translateX(-50%); top:4px; width:1px; height:6px; background:rgba(148,163,184,0.65);'></span>"
            for value in minor_tick_values
        )}
        {"".join(
            f"<span style='position:absolute; left:{(value / 1.5) * 100:.4f}%; transform:translateX(-50%); top:1px; width:1.6px; height:11px; background:rgba(100,116,139,0.86);'></span>"
            for value in tick_values
        )}
      </div>
    </div>
    <div style="position:relative; height:18px; margin-top:0.35rem; font-size:12px; color:#64748b;">
      <div style="position:absolute; left:9px; right:9px; top:0; bottom:0;">
        {"".join(
            (
                f"<span style='position:absolute; left:{(value / 1.5) * 100:.4f}%; "
                + ("transform:translateX(-50%);" if value not in (0.0, 1.5) else ("transform:none;" if value == 0.0 else "transform:translateX(-100%);"))
                + f" white-space:nowrap;'>{label}</span>"
            )
            for value, label in zip(tick_values, tick_labels)
        )}
      </div>
    </div>
  </div>

  <div style="
      display: grid;
      grid-template-columns: 1fr;
      gap: 1rem;
      align-items: stretch;
      margin-top: 0.2rem;
  ">
    <div style="
        padding: 0.8rem 0.95rem;
        border-radius: 18px;
        background: rgba(255,255,255,0.86);
        border: 1px solid rgba(148, 163, 184, 0.16);
    ">
      <div style="display:grid; grid-template-columns:minmax(360px, 1.75fr) minmax(220px, 0.72fr); gap:0.75rem; align-items:center;">
        <svg id="fv-oscillator" viewBox="0 0 360 172" style="width:100%; height:200px; display:block; border-radius:16px; background:linear-gradient(180deg, #ffffff 0%, #f7fbff 100%);">
          <rect x="0" y="0" width="360" height="172" rx="16" fill="transparent"></rect>
          <rect x="18" y="34" width="18" height="104" rx="6" fill="#94a3b8"></rect>
          <line x1="36" y1="68" x2="50" y2="68" stroke="rgba(15,23,42,0.22)" stroke-width="4" stroke-linecap="round"></line>
          <line x1="50" y1="56" x2="170" y2="56" stroke="#94a3b8" stroke-width="3" stroke-linecap="round"></line>
          <line x1="50" y1="80" x2="170" y2="80" stroke="#94a3b8" stroke-width="3" stroke-linecap="round"></line>
          <line x1="50" y1="56" x2="50" y2="80" stroke="#94a3b8" stroke-width="3" stroke-linecap="round"></line>
          <line id="fv-damper-piston" x1="92" y1="58" x2="92" y2="78" stroke="#64748b" stroke-width="4" stroke-linecap="round"></line>
          <line id="fv-damper-rod" x1="92" y1="68" x2="170" y2="68" stroke="#64748b" stroke-width="4" stroke-linecap="round"></line>
          <line x1="36" y1="108" x2="320" y2="108" stroke="rgba(148,163,184,0.35)" stroke-width="6" stroke-linecap="round"></line>
          <line id="fv-equilibrium" x1="258" y1="28" x2="258" y2="144" stroke="rgba(15,118,110,0.18)" stroke-width="2" stroke-dasharray="5 6"></line>
          <line id="fv-initial" x1="318" y1="28" x2="318" y2="144" stroke="rgba(249,115,22,0.45)" stroke-width="2" stroke-dasharray="4 5"></line>
          <polyline id="fv-spring" fill="none" stroke="#0f766e" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"></polyline>
          <rect id="fv-mass" x="226" y="54" width="64" height="64" rx="14" fill="#14b8a6" stroke="#0f766e" stroke-width="3"></rect>
          <text x="20" y="24" fill="#64748b" style="font-size:12px;">固定端</text>
          <text x="295" y="22" fill="#f97316" style="font-size:12px;">初始位置</text>
          <text x="235" y="164" fill="#64748b" style="font-size:12px;">平衡位置</text>
        </svg>
        <div style="display:grid; grid-template-columns:minmax(0, 1fr) auto; gap:0.65rem; align-items:center;">
          <div style="display:grid; gap:0.7rem;">
            <div style="padding:0.65rem 0.72rem; border-radius:14px; background:#f8fffd; border:1px solid rgba(15,118,110,0.12);">
              <div style="font-size:12px; color:#64748b;">动画时间</div>
              <div id="fv-time" style="margin-top:0.2rem; font-size:16px; font-weight:700; color:#0f172a;">t = 0.00 s</div>
            </div>
            <div style="padding:0.65rem 0.72rem; border-radius:14px; background:#f8fffd; border:1px solid rgba(15,118,110,0.12);">
              <div style="font-size:12px; color:#64748b;">实时位移</div>
              <div id="fv-displacement" style="margin-top:0.2rem; font-size:16px; font-weight:700; color:#0f172a;">x = 1.000</div>
            </div>
          </div>
          <div style="display:flex; flex-direction:column; gap:0.55rem;">
            <button id="fv-play" style="padding: 0.72rem 1rem; border: none; border-radius: 999px; background: #0f766e; color: white; font-weight: 700; cursor: pointer;">播放</button>
            <button id="fv-reset" style="padding: 0.72rem 1rem; border: 1px solid rgba(15,118,110,0.18); border-radius: 999px; background: white; color: #0f172a; font-weight: 700; cursor: pointer;">重播</button>
          </div>
        </div>
      </div>
    </div>

    <div style="
        padding: 0.95rem;
        border-radius: 18px;
        background: rgba(255,255,255,0.86);
        border: 1px solid rgba(148, 163, 184, 0.16);
    ">
      <div id="fv-plot" style="width:100%; height:400px;"></div>
    </div>
  </div>
</div>

<script>
(function() {{
  const container = document.getElementById("free-vibration-demo");
  if (!container || container.dataset.initialized === "true") return;
  container.dataset.initialized = "true";

  const tValues = {json.dumps(t_values, ensure_ascii=False)};
  const zetaValues = {json.dumps(zeta_values.tolist(), ensure_ascii=False)};
  const omegaN = {omega_n};
  const x0 = {x0};
  const initActive = {init_active};
  const accent = "#0f766e";
  const playbackDuration = 8000;
  const equilibriumX = 258;
  const massWidth = 64;
  const motionScale = 60;
  const damperRodLength = 116;

  const stateEl = container.querySelector("#fv-state");
  const descEl = container.querySelector("#fv-desc");
  const zetaEl = container.querySelector("#fv-zeta");
  const timeEl = container.querySelector("#fv-time");
  const displacementEl = container.querySelector("#fv-displacement");
  const sliderEl = container.querySelector("#fv-zeta-slider");
  const playBtn = container.querySelector("#fv-play");
  const resetBtn = container.querySelector("#fv-reset");
  const plotEl = container.querySelector("#fv-plot");
  const springEl = container.querySelector("#fv-spring");
  const massEl = container.querySelector("#fv-mass");
  const damperPistonEl = container.querySelector("#fv-damper-piston");
  const damperRodEl = container.querySelector("#fv-damper-rod");

  let currentStep = initActive;
  let currentIndex = 0;
  let isPlaying = false;
  let startTimestamp = null;
  let rafId = null;
  let response = computeResponse(zetaValues[currentStep]);

  function computeResponse(zeta) {{
    const yMain = [];
    const yUp = [];
    const yDown = [];
    let state = "";
    let description = "";

    if (Math.abs(zeta) < 1e-12) {{
      state = "无阻尼";
      description = "等幅简谐振荡";
      for (const time of tValues) {{
        const value = x0 * Math.cos(omegaN * time);
        yMain.push(value);
        yUp.push(x0);
        yDown.push(-x0);
      }}
    }} else if (zeta < 1.0) {{
      state = "欠阻尼";
      description = "振荡且振幅指数衰减";
      const omegaD = omegaN * Math.sqrt(1 - zeta * zeta);
      for (const time of tValues) {{
        const decay = Math.exp(-zeta * omegaN * time);
        const value = decay * (
          x0 * Math.cos(omegaD * time) +
          (zeta * omegaN * x0 / omegaD) * Math.sin(omegaD * time)
        );
        const envelope = x0 * decay;
        yMain.push(value);
        yUp.push(envelope);
        yDown.push(-envelope);
      }}
    }} else if (Math.abs(zeta - 1.0) < 1e-12) {{
      state = "临界阻尼";
      description = "不振荡且最快回到平衡";
      for (const time of tValues) {{
        const value = (x0 + omegaN * x0 * time) * Math.exp(-omegaN * time);
        yMain.push(value);
        yUp.push(null);
        yDown.push(null);
      }}
    }} else {{
      state = "过阻尼";
      description = "不振荡但回到平衡更慢";
      const root = Math.sqrt(zeta * zeta - 1);
      const s1 = -omegaN * (zeta - root);
      const s2 = -omegaN * (zeta + root);
      const c1 = (s2 * x0) / (s2 - s1);
      const c2 = (-s1 * x0) / (s2 - s1);
      for (const time of tValues) {{
        const value = c1 * Math.exp(s1 * time) + c2 * Math.exp(s2 * time);
        yMain.push(value);
        yUp.push(null);
        yDown.push(null);
      }}
    }}

    return {{ zeta, state, description, yMain, yUp, yDown }};
  }}

  function buildPlotData(index) {{
    const partialX = tValues.slice(0, index + 1);
    const partialY = response.yMain.slice(0, index + 1);
    const markerX = [tValues[index]];
    const markerY = [response.yMain[index]];

    return [
      {{
        x: tValues,
        y: response.yDown,
        mode: "lines",
        line: {{ color: "rgba(15, 118, 110, 0.42)", width: 1.2, dash: "dot" }},
        hoverinfo: "skip",
        showlegend: false,
      }},
      {{
        x: tValues,
        y: response.yUp,
        mode: "lines",
        fill: "tonexty",
        fillcolor: "rgba(15, 118, 110, 0.18)",
        line: {{ color: "rgba(15, 118, 110, 0.42)", width: 1.2, dash: "dot" }},
        name: "衰减包络",
        hoverinfo: "skip",
      }},
      {{
        x: tValues,
        y: response.yMain,
        mode: "lines",
        name: "完整响应",
        line: {{ color: "rgba(15, 23, 42, 0.18)", width: 2 }},
        hovertemplate: "t = %{{x:.2f}} s<br>x = %{{y:.3f}}<extra></extra>",
      }},
      {{
        x: partialX,
        y: partialY,
        mode: "lines",
        name: "已绘响应",
        line: {{ color: accent, width: 3.2 }},
        hovertemplate: "t = %{{x:.2f}} s<br>x = %{{y:.3f}}<extra></extra>",
      }},
      {{
        x: markerX,
        y: markerY,
        mode: "markers",
        name: "当前点",
        marker: {{ size: 10, color: accent, line: {{ color: "white", width: 2 }} }},
        hovertemplate: "t = %{{x:.2f}} s<br>x = %{{y:.3f}}<extra></extra>",
      }}
    ];
  }}

  function buildLayout() {{
    return {{
      margin: {{ l: 62, r: 18, t: 18, b: 52 }},
      paper_bgcolor: "rgba(0,0,0,0)",
      plot_bgcolor: "#ffffff",
      xaxis: {{
        title: "<b>时间 t (s)</b>",
        range: [0, 10],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: false,
        showline: true,
        linecolor: "rgba(15, 23, 42, 0.28)",
        ticks: "outside",
      }},
      yaxis: {{
        title: "<b>归一化位移 x</b>",
        range: [-1.2, 1.2],
        showgrid: true,
        gridcolor: "rgba(148, 163, 184, 0.22)",
        zeroline: true,
        zerolinecolor: "rgba(15, 23, 42, 0.18)",
        zerolinewidth: 1.5,
      }},
      legend: {{
        orientation: "h",
        yanchor: "bottom",
        y: 1.02,
        xanchor: "right",
        x: 0.98,
        bgcolor: "rgba(255,255,255,0.72)",
        bordercolor: "rgba(148, 163, 184, 0.20)",
        borderwidth: 1,
      }},
      hoverlabel: {{
        bgcolor: "#ffffff",
        bordercolor: "rgba(15, 118, 110, 0.30)",
        font: {{ color: "#0f172a" }},
      }},
    }};
  }}

  function updateReadouts(index) {{
    stateEl.textContent = `系统状态：${{response.state}}`;
    if (descEl) descEl.textContent = response.description;
    zetaEl.textContent = `ζ = ${{response.zeta.toFixed(2)}}`;
    timeEl.textContent = `t = ${{tValues[index].toFixed(2)}} s`;
    displacementEl.textContent = `x = ${{response.yMain[index].toFixed(3)}}`;
  }}

  function buildSpringPoints(startX, endX, y) {{
    const points = [];
    const straight = 16;
    const turns = 9;
    const amplitude = 18;
    points.push(`${{startX}},${{y}}`);
    points.push(`${{startX + straight}},${{y}}`);
    const usable = Math.max(endX - startX - straight * 2, 30);
    const step = usable / (turns * 2);
    for (let i = 0; i < turns * 2; i += 1) {{
      const x = startX + straight + step * (i + 1);
      const offset = i % 2 === 0 ? -amplitude : amplitude;
      points.push(`${{x}},${{y + offset}}`);
    }}
    points.push(`${{endX - straight}},${{y}}`);
    points.push(`${{endX}},${{y}}`);
    return points.join(" ");
  }}

  function updateOscillator(index) {{
    const displacement = response.yMain[index];
    const massX = equilibriumX + displacement * motionScale;
    const massLeft = massX - massWidth / 2;
    const damperAnchorX = massLeft;
    const pistonX = damperAnchorX - damperRodLength;
    massEl.setAttribute("x", massLeft.toFixed(2));
    springEl.setAttribute("points", buildSpringPoints(36, massLeft + 2, 108));
    damperPistonEl.setAttribute("x1", pistonX.toFixed(2));
    damperPistonEl.setAttribute("x2", pistonX.toFixed(2));
    damperPistonEl.setAttribute("y1", "58");
    damperPistonEl.setAttribute("y2", "78");
    damperRodEl.setAttribute("x1", pistonX.toFixed(2));
    damperRodEl.setAttribute("x2", damperAnchorX.toFixed(2));
    damperRodEl.setAttribute("y1", "68");
    damperRodEl.setAttribute("y2", "68");
  }}

  function renderPlot(index, fullRedraw) {{
    const partialX = tValues.slice(0, index + 1);
    const partialY = response.yMain.slice(0, index + 1);
    const markerX = [tValues[index]];
    const markerY = [response.yMain[index]];

    if (fullRedraw) {{
      Plotly.react(plotEl, buildPlotData(index), buildLayout(), {{
        displayModeBar: false,
        responsive: true,
      }});
    }} else {{
      Plotly.restyle(plotEl, {{ x: [partialX], y: [partialY] }}, [3]);
      Plotly.restyle(plotEl, {{ x: [markerX], y: [markerY] }}, [4]);
    }}
  }}

  function syncScene(index, fullRedraw = false) {{
    currentIndex = index;
    updateReadouts(index);
    updateOscillator(index);
    renderPlot(index, fullRedraw);
  }}

  function stopAnimation() {{
    isPlaying = false;
    startTimestamp = null;
    if (rafId) cancelAnimationFrame(rafId);
    rafId = null;
    playBtn.textContent = "播放";
  }}

  function stepAnimation(timestamp) {{
    if (!isPlaying) return;
    if (startTimestamp === null) {{
      startTimestamp = timestamp - (currentIndex / (tValues.length - 1)) * playbackDuration;
    }}
    const progress = Math.min((timestamp - startTimestamp) / playbackDuration, 1);
    const nextIndex = Math.min(Math.floor(progress * (tValues.length - 1)), tValues.length - 1);
    if (nextIndex !== currentIndex) {{
      syncScene(nextIndex, false);
    }}
    if (progress >= 1) {{
      stopAnimation();
      return;
    }}
    rafId = requestAnimationFrame(stepAnimation);
  }}

  function startAnimation(fromBeginning = false) {{
    if (fromBeginning) {{
      stopAnimation();
      syncScene(0, true);
    }}
    isPlaying = true;
    startTimestamp = null;
    playBtn.textContent = "暂停";
    rafId = requestAnimationFrame(stepAnimation);
  }}

  function toggleAnimation() {{
    if (isPlaying) {{
      stopAnimation();
    }} else {{
      if (currentIndex >= tValues.length - 1) {{
        syncScene(0, true);
      }}
      startAnimation(false);
    }}
  }}

  function applyZeta(stepIndex) {{
    currentStep = stepIndex;
    sliderEl.value = String(stepIndex);
    response = computeResponse(zetaValues[stepIndex]);
    stopAnimation();
    syncScene(0, true);
    startAnimation(false);
  }}

  sliderEl.addEventListener("input", (event) => {{
    applyZeta(Number(event.target.value));
  }});

  playBtn.addEventListener("click", () => {{
    toggleAnimation();
  }});

  resetBtn.addEventListener("click", () => {{
    applyZeta(currentStep);
  }});

  syncScene(0, true);
  startAnimation(false);
}})();
</script>
"""

display(HTML(html))
```
